% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-prevention.R
\name{apply_prevention_multipliers}
\alias{apply_prevention_multipliers}
\title{Apply prevention multipliers to a service-volume tibble}
\usage{
apply_prevention_multipliers(volumes, multipliers)
}
\arguments{
\item{volumes}{Tibble with at least columns \code{service} (character) and
\code{volume} (numeric). Typically the output of \code{\link[=example_service_volumes]{example_service_volumes()}} or
\code{\link[=lifecourse_demand_trajectory]{lifecourse_demand_trajectory()}}. A \code{year} column is preserved if present.}

\item{multipliers}{Named numeric vector from \code{\link[=conservative_management_multipliers]{conservative_management_multipliers()}}.}
}
\value{
\code{volumes} with the \code{volume} column scaled by the per-service
multipliers. An attribute \code{prevention_multipliers} is attached to the
returned tibble for audit purposes.
}
\description{
Multiplies each service's \code{volume} column by the corresponding multiplier
from \code{\link[=conservative_management_multipliers]{conservative_management_multipliers()}}. The result is the demand-side
input to \code{\link[=convert_workload_to_fte]{convert_workload_to_fte()}} after the conservative management
scenario shift.
}
\details{
Services absent from \code{multipliers} are passed through unchanged (multiplier
defaults to 1). Services in \code{multipliers} but absent from \code{volumes} are
silently ignored.
}
\examples{
vols <- tibble::tibble(
  year    = rep(2025:2027, each = 3),
  service = rep(c("sling_procedure", "prolapse_procedure", "new_consultation"), 3),
  volume  = c(1000, 800, 5000, 1050, 840, 5250, 1100, 880, 5500)
)
m <- conservative_management_multipliers(ui_uptake = 0.25, pop_uptake = 0.25)
apply_prevention_multipliers(vols, m)
}
\seealso{
\code{\link[=conservative_management_multipliers]{conservative_management_multipliers()}},
\code{\link[=prevention_demand_trajectory]{prevention_demand_trajectory()}}

Other prevention: 
\code{\link{URPS_PREVENTION_SCENARIOS}},
\code{\link{apply_named_prevention_scenario}()},
\code{\link{conservative_management_multipliers}()},
\code{\link{prevention_volume_summary}()}
}
\concept{demand}
\concept{prevention}
