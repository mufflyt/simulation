% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-sources.R
\name{urps_age_productivity_curve}
\alias{urps_age_productivity_curve}
\title{Age-productivity curve for effective-FTE validation}
\usage{
urps_age_productivity_curve(mode = resolve_reproducibility_mode())
}
\arguments{
\item{mode}{Reproducibility mode.}
}
\value{
Tibble \code{age}, \code{rel_to_peak}, \code{work_usd}.
}
\description{
The current model defines FTE as an hours threshold (R/supply-provider_lifecycle), not a productivity
step function, so this is a VALIDATION reference (and an override for the
deprecated \code{legacy_weight} FTE method) rather than the default.
}
\examples{
# The curve resolves through the canonical source registry, and config/ is
# .Rbuildignore'd -- so this runs in a source checkout and is skipped inside
# <pkg>.Rcheck/, where config/ does not exist. Same guard the tests use.
curve <- tryCatch(urps_age_productivity_curve(), error = function(e) NULL)
if (!is.null(curve)) head(curve)
}
\seealso{
Other sources: 
\code{\link{nrmp_entrant_series}()},
\code{\link{nrmp_entrants}()},
\code{\link{nrmp_track_split}()},
\code{\link{urps_empirical_hazard_by_ageband}()},
\code{\link{urps_empirical_retirement_schedule}()},
\code{\link{urps_hazard_exposure}()}
}
\concept{calibration}
\concept{sources}
