% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-hwsm_parameters.R
\name{hwsm_retirement_hazard_table}
\alias{hwsm_retirement_hazard_table}
\title{HRSA HWSM retirement hazard table (Surgery proxy)}
\usage{
hwsm_retirement_hazard_table(min_age = 50L, max_age = 90L)
}
\arguments{
\item{min_age}{Age below which retirement probability is 0. Default 50.}

\item{max_age}{Highest age row emitted. Default 90.}
}
\value{
Data frame with \code{age}, \code{sex} ("Female"/"Male"), \code{prob_exit},
\code{calibration_tier}, carrying a \code{provenance} attribute.
}
\description{
The HRSA Surgery workforce-survival curve as a per-age, per-sex one-year exit
probability, in the same shape \code{\link[=build_urps_exit_hazard]{build_urps_exit_hazard()}} returns (\code{age},
\code{sex}, \code{prob_exit}) -- an HRSA-Surgery alternative to the Weibull / cliff exit
hazard. Ages below \code{min_age} carry \code{prob_exit = 0} because HRSA models
permanent retirement only for ages 50+, and pre-50 attrition is a separate
career-change process here.
}
\examples{
\dontrun{
hz <- hwsm_retirement_hazard_table()
head(hz)   # (age, sex, prob_exit): the shape build_urps_exit_hazard() returns
}
}
\seealso{
Other hwsm supply: 
\code{\link{add_hwsm_supply_parameters}()},
\code{\link{hwsm_provenance}()}
}
\concept{hwsm supply}
\concept{supply}
