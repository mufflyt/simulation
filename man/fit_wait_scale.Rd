% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-access.R
\name{fit_wait_scale}
\alias{fit_wait_scale}
\title{Fit the wait-mapping constant \code{wait_scale} to an observed wait}
\usage{
fit_wait_scale(catchments, observed_wait)
}
\arguments{
\item{catchments}{A per-catchment table for \code{\link[=clear_access]{clear_access()}} (needs
\code{demand_workload} and \code{accessible_capacity}).}

\item{observed_wait}{The observed national wait to fit to. Positive scalar,
same time unit as the appointment window.}
}
\value{
A list: \code{wait_scale} (the fitted constant), \code{unit_wait} (national wait
at \code{wait_scale = 1}), \code{observed_wait}, \code{n_catchments_used}, and
\code{censored_demand_share} (demand share in saturated queues the fit cannot
see). Feed \code{wait_scale} back into \code{\link[=clear_access]{clear_access()}}.
}
\description{
The national demand-weighted wait is LINEAR in \code{wait_scale}
(\code{wait = wait_scale * rho/(1-rho)}), so the fit is closed-form: clear the
catchments at \code{wait_scale = 1}, take the demand-weighted mean wait over the
catchments with a finite (non-censored) wait, and scale it to the observation.
The result makes \code{access_outcomes_national()}'s \code{A1} (\code{wait_time}) equal
\code{observed_wait} by construction. Supply \code{observed_wait} from a specialty wait
survey (see \code{\link[=access_validation_targets]{access_validation_targets()}}); this does not invent it.
}
