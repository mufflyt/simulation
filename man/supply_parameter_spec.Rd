% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-parameter_uncertainty.R
\name{supply_parameter_spec}
\alias{supply_parameter_spec}
\title{Specify which supply parameters vary across Monte Carlo iterations}
\usage{
supply_parameter_spec(
  entrant_series = NULL,
  entrant_mean = NULL,
  departures = 0,
  hazard_cv = 0,
  hours_model = NULL,
  entrant_regime = NULL,
  entrant_regime_include = c("trend", "dispersion", "break", "release_timing")
)
}
\arguments{
\item{entrant_series}{Observed annual entrant counts behind the entrant rate.
Supplying it turns the entrant rate into a drawn parameter.}

\item{entrant_mean}{Point estimate of gross annual entrants.}

\item{departures}{Expected annual departures, recorded for reporting only.
NOT added to \code{entrant_mean}: the certification series is a gross flow that
never nets departures out, so adding them double-counts (see
\code{\link[=observed_entrant_rate]{observed_entrant_rate()}}).}

\item{hazard_cv}{Coefficient of variation on a multiplicative factor applied
to the whole retirement schedule. 0 means the hazard is treated as known,
which it is not -- see the module note.}

\item{hours_model}{Optional fitted hours model; its coefficients are drawn
through the shared internal \code{.param_draw()} helper.}

\item{entrant_regime}{Optional \code{\link[=fit_entrant_regime_model]{fit_entrant_regime_model()}} object. Supplying
it REPLACES the scalar entrant draw with a whole per-year entrant path, so
the iteration carries trend-coefficient, overdispersion, and regime-break
uncertainty rather than sampling variation in a three-year mean. This is the
component the back-test addendum found missing: variation WITHIN the fitting
window cannot represent a change of regime BETWEEN windows.}

\item{entrant_regime_include}{Components of the regime draw to switch on; see
\code{\link[=draw_entrant_paths]{draw_entrant_paths()}}.}
}
\value{
An object of class \code{urps_param_spec}.
}
\description{
Specify which supply parameters vary across Monte Carlo iterations
}
