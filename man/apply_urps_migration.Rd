% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_migration.R
\name{apply_urps_migration}
\alias{apply_urps_migration}
\title{Apply one year of URPS-specific provider migration}
\usage{
apply_urps_migration(
  agents,
  year,
  migration_matrix = NULL,
  hazards = URPS_MIGRATION_HAZARD,
  urbanicity = CONUS_STATE_URBANICITY,
  ...
)
}
\arguments{
\item{agents}{Agent tibble with at least \code{state} and \code{entry_year}.}

\item{year}{Current calendar year.}

\item{migration_matrix}{Pre-built matrix from \code{\link[=urps_migration_matrix]{urps_migration_matrix()}}.
When \code{NULL}, one is built on the fly from the agent states using equal
destination weights — useful for prototyping but slower in a loop.}

\item{hazards}{Named migration hazard vector. Defaults to
\link{URPS_MIGRATION_HAZARD}.}

\item{urbanicity}{State urbanicity lookup. Defaults to
\link{CONUS_STATE_URBANICITY}.}

\item{...}{Additional arguments passed to \code{\link[=urps_migration_matrix]{urps_migration_matrix()}} when
\code{migration_matrix = NULL}.}
}
\value{
The agent tibble with updated \code{state}, \code{n_moves}, and \code{left_country}.
}
\description{
Convenience wrapper that selects per-agent hazards from \link{URPS_MIGRATION_HAZARD}
and dispatches to \code{\link[=apply_provider_migration_matrix]{apply_provider_migration_matrix()}} using a pre-built or
on-the-fly origin-by-destination matrix. Agents without a \code{state} column
are returned unchanged.
}
\examples{
\dontrun{
# Build matrix once before the loop, reuse every year
states <- unique(na.omit(agents$state))
mat    <- urps_migration_matrix(states)
for (yr in 2026:2050) {
  agents <- apply_urps_migration(agents, yr, migration_matrix = mat)
  agents <- agents[!isTRUE(agents$left_country), ]   # filter gone providers
}
}
}
