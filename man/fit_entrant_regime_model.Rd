% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-entrant_regime.R
\name{fit_entrant_regime_model}
\alias{fit_entrant_regime_model}
\title{Fit the entrant rate with explicit disruption and deferral handling}
\usage{
fit_entrant_regime_model(
  series,
  fit_through_year,
  trend_family = "loglinear",
  screen_alpha = 0.01,
  backlog_multiple = 2,
  release_years = 2L,
  break_concentration = 2,
  break_surviving_share_prior = NA_real_,
  verbose = TRUE
)
}
\arguments{
\item{series}{Data frame of \code{year} and \code{count} (see \code{\link[=urps_entrant_series]{urps_entrant_series()}}).}

\item{fit_through_year}{Cutoff. Later years are dropped and never read.}

\item{trend_family}{\code{"loglinear"} or \code{"intercept"}. Use \code{"intercept"} when
fewer than four steady years survive the screen.}

\item{screen_alpha, backlog_multiple}{Passed to
\code{\link[=classify_certification_regimes]{classify_certification_regimes()}}.}

\item{release_years}{Years over which a deferred deficit clears.}

\item{break_concentration}{Beta concentration for the surviving-share prior.
The default 2 is weak on purpose: one observed disruption does not identify
the shape of the distribution of future ones.}

\item{break_surviving_share_prior}{Surviving share to assume when the fitting
window contains no disruption at all. \code{NA} (the default) simulates no break
and says so, matching this package's rule that an unquantified parameter is
reported rather than invented. Supplying a value is an explicit modelling
choice by the caller.}

\item{verbose}{Emit progress via \code{\link[base:message]{base::message()}}.}
}
\value{
An object of class \code{urps_entrant_regime}.
}
\description{
Fits the steady-state trend on steady years only, measures the deficit left
by disrupted years, and estimates how often a break occurs and how deep it is
-- all from years at or before \code{fit_through_year}.
}
\details{
The deferred deficit is \code{sum(expected - observed)} over disrupted years and is
released over the first \code{release_years} projected years. It is added back
because a cancelled examination defers candidates rather than removing them.

Break frequency uses the Jeffreys-style \code{(k + 0.5) / (n + 1)} rather than
\code{k / n}: with one break in five screened years, \code{k / n} would state 0.2 with
false precision and, had no break been observed, would state that a break is
impossible.
}
\examples{
s <- data.frame(year = 2013:2020,
                count = c(655, 175, 102, 36, 33, 40, 48, 10))
fit_entrant_regime_model(s, 2020L, verbose = FALSE)$deferred_backlog > 0
}
\seealso{
Other entrant regime:
\code{\link[=classify_certification_regimes]{classify_certification_regimes()}},
\code{\link[=draw_entrant_paths]{draw_entrant_paths()}},
\code{\link[=entrant_regime_rolling_validation]{entrant_regime_rolling_validation()}},
\code{\link[=project_entrant_path]{project_entrant_path()}}
}
\concept{entrant regime}
\concept{supply}
