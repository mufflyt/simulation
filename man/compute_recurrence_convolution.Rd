% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-recurrence_convolution.R
\name{compute_recurrence_convolution}
\alias{compute_recurrence_convolution}
\title{Compute recurrence from historical treatment cohorts}
\usage{
compute_recurrence_convolution(
  treatment_cohorts,
  recurrence_kernel,
  forecast_years,
  group_cols = "condition",
  tail_policy = base::c("error", "zero_after_kernel")
)
}
\arguments{
\item{treatment_cohorts}{Data frame with grouping columns, \code{treatment_year},
\code{treated_n}.}

\item{recurrence_kernel}{Data frame with grouping columns,
\code{years_since_treatment}, \code{recurrence_prob}.}

\item{forecast_years}{Years for which recurrence should be computed.}

\item{group_cols}{Columns defining separate recurrence processes. Supply
\code{c("condition", "index_treatment")} unless evidence shows treatments may
share a kernel.}

\item{tail_policy}{\code{"error"} (default) or \code{"zero_after_kernel"}.}
}
\value{
List with \code{annual} totals and per-cohort \code{contributions}.
}
\description{
Implements \verb{R_t = sum_k C_(t-k) * g_k}.
}
\details{
\code{tail_policy = "error"} is the scientific default and is deliberately
inconvenient: if the evidence describes recurrence through five years and a
cohort treated eight years earlier is supplied, this REFUSES rather than
silently assuming years 6+ contribute nothing. Assuming zero is a claim about
the disease, not a default.

The returned \code{contributions} table makes any predicted count traceable --
for a 2028 total you can see which treatment years produced it and in what
proportion, which \verb{this_year_operations x 0.12} never permitted.
}
\seealso{
Other recurrence:
\code{\link{RECURRENCE_MEASURE_TYPES}},
\code{\link[=assert_recurrence_kernel_compatible]{assert_recurrence_kernel_compatible()}},
\code{\link[=build_recurrence_kernel]{build_recurrence_kernel()}},
\code{\link[=recurrence_evidence_register]{recurrence_evidence_register()}},
\code{\link[=recurrence_mass_from_cumulative]{recurrence_mass_from_cumulative()}},
\code{\link[=recurrence_parameter_status]{recurrence_parameter_status()}}
}
\concept{demand}
\concept{recurrence}
