% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting-canonical_readiness_audit.R
\name{.repo_source_root}
\alias{.repo_source_root}
\title{Locate the repository source tree}
\usage{
.repo_source_root()
}
\value{
Absolute path to the repository root, or \code{NA_character_}.
}
\description{
\verb{.github/}, like \verb{config/} and \verb{scripts/}, is excluded from the built
package (see .Rbuildignore) and only exists in a source checkout. Returns
\code{NA_character_} when running from an installed package with no source tree
available, so callers can degrade gracefully instead of misreporting.
}
\details{
Shared by every scorecard audit_*() function that needs to shell out to a
\verb{.github/scripts/} or \verb{scripts/} gate -- defined once here rather than per
file, so it cannot drift into two different resolutions of "the repo root".
}
\keyword{internal}
