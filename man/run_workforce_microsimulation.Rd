% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-run_workforce_microsimulation.R
\name{run_workforce_microsimulation}
\alias{run_workforce_microsimulation}
\title{Run the integrated workforce microsimulation}
\usage{
run_workforce_microsimulation(
  baseline_supply = NULL,
  supply_geography = c("national", "conus"),
  roster = NULL,
  use_certification_cohorts = TRUE,
  years = 2025:2050,
  subspecialty = "FPMRS",
  pop_by_band = NULL,
  population_series = "mid",
  supply_scenarios = NULL,
  demand_scenarios = NULL,
  baseline_gap_estimate = NULL,
  n_iterations = 200,
  baseline_entrants = NULL,
  retirement_source = c("urps_empirical", "hwsm"),
  calibration = NULL,
  placement_shares = NULL,
  seed_base_geography = FALSE,
  parameter_spec = NULL,
  allow_analogy = FALSE,
  brfss_cells = NULL,
  d5_estimand = NULL,
  d6_estimand = NULL,
  prevention_scenario = NULL,
  setting_scenario = NULL,
  output_dir = NULL,
  seed = 20260801L,
  verbose = TRUE
)
}
\arguments{
\item{baseline_supply}{Base-year supplied FTE, or NULL to read it from the
\code{mufflyaccess} contract. Never hard-code a workforce count.}

\item{supply_geography}{"national" or "conus": national for supply reporting,
CONUS for the geographic access layer.}

\item{roster}{Optional production provider roster; when NULL the base cohort
is built from the observed certification series where available.}

\item{use_certification_cohorts}{Build the base cohort from
\code{mufflyaccess::urps_counts_long()} when no roster is supplied.}

\item{years}{Integer projection years.}

\item{subspecialty}{Subspecialty label.}

\item{pop_by_band}{Age-banded female population. When NULL, resolved from the
canonical Census-NPP series (\code{\link[=resolve_demand_population]{resolve_demand_population()}}), falling back to
\code{\link[=example_female_population_by_band]{example_female_population_by_band()}} if the file is absent.}

\item{population_series}{Census-NPP series when \code{pop_by_band} is NULL: one of
"mid" (default), "low", "hi".}

\item{supply_scenarios, demand_scenarios}{Scenario registries.}

\item{baseline_gap_estimate}{A \code{\link[=baseline_gap]{baseline_gap()}} object; NULL triggers the
fail-closed guard.}

\item{n_iterations}{Monte-Carlo replicates per scenario.}

\item{baseline_entrants}{Baseline annual entrants. \code{NULL} (default) resolves
the real NRMP-matched value via \code{\link[=nrmp_entrants]{nrmp_entrants()}} (70/yr for URPS) and
records \code{entrants_source = "nrmp_matched"} in the run metadata. Pass a
number to override; \code{55} reproduces runs made before the default became
measured rather than assumed.}

\item{retirement_source}{Base retirement hazard: "urps_empirical" (default,
cliff's observed URPS hazards for ages 50-69 with the HWSM tail past 70) or
"hwsm" (the HWSM/FutureDocs literature curve, an external analogue fitted on
a different physician population). Scenario age-shifts apply on top of
whichever base is chosen. The empirical hazards are preferred whenever they
are derivable: they are measured on this subspecialty rather than borrowed.
Selecting "hwsm" is an affirmative choice to model URPS attrition with
another population's curve, and is recorded in the run metadata.}

\item{calibration}{Demand calibration. \code{NULL} (default) leaves demand
uncalibrated, which \code{\link[=assert_demand_calibrated]{assert_demand_calibrated()}} warns about and strict
mode refuses -- this is the uncalibrated comparator. The string \code{"namcs"}
fits scalars internally from the base-year volumes against the NAMCS
national anchor (\code{\link[=namcs_demand_calibration]{namcs_demand_calibration()}}). A tibble from
\code{\link[=fit_calibration_scalars]{fit_calibration_scalars()}} is used as supplied. Scalars are APPLIED to the
comparable service rows, not merely recorded.}

\item{placement_shares}{Optional tibble of \code{geo} and \code{share} that enables the
geographic layer: entrants are placed by this distribution and providers may
migrate mid-career. Build it with \code{\link[=opportunity_placement_shares]{opportunity_placement_shares()}} for the
HWSM rule (demand growth plus retirements, which lets existing
maldistribution persist), \code{\link[=historical_placement_shares]{historical_placement_shares()}} for the
reproduce-today's-distribution comparison. (An opportunity-responsive
blend lived in \code{blend_placement_shares()}, now in inst/archive/geography.R.)
Requires a cohort carrying \code{state}.}

\item{seed_base_geography}{When \code{placement_shares} is supplied but the cohort
has no \code{state} column, draw the base cohort's states from those shares
rather than leaving the layer inert. A declared assumption, not an
observation: geographic output is then conditional on it.}

\item{parameter_spec}{Optional \code{\link[=supply_parameter_spec]{supply_parameter_spec()}}; defaults to one
built from the observed certification series.}

\item{allow_analogy}{Permit inputs derived by analogy from another specialty:
the delegation matrix, and a base-year gap whose \code{calibration_status} is
\code{derived_by_analogy} or \code{assumed_with_evidence}. Declared in the run
metadata either way. Defaults to FALSE: passing TRUE suppresses the
strict-mode stop in \code{\link[=assert_publishable_workload]{assert_publishable_workload()}} and
\code{\link[=assert_baseline_gap_estimated]{assert_baseline_gap_estimated()}}, so a publication-facing run must opt in
deliberately rather than inherit the exemption from the default.}

\item{brfss_cells}{Optional population cell table from
\code{\link[=build_urps_population_cells]{build_urps_population_cells()}}.  When non-NULL, a fourth demand estimand
D4 (BRFSS survey-weighted UI care-seeking demand) is appended to
\code{demand_long} and flows through concordance assessment alongside D1-D3.
Requires that \code{\link[=brfss_pfd_prevalence_for_demand_bands]{brfss_pfd_prevalence_for_demand_bands()}} is available (i.e.
that R/data-urps_population.R is loaded).}

\item{d5_estimand}{Optional precomputed D5 tibble from
\code{build_d5_namcs_estimand()$estimand} (NAMCS visit-rate demand, pooled
2015-2019 with ICD-9/ICD-10 coding).  Must have columns \code{year},
\code{estimand}, \code{demand_visits}.  When non-NULL, appended to \code{demand_long}
(as \code{demand_cases = demand_visits}) for concordance assessment.}

\item{d6_estimand}{Optional precomputed D6 tibble from
\code{build_d6_nhanes_estimand()$estimand} (NHANES UI prevalence × care-seeking
demand).  Must have columns \code{year}, \code{estimand}, \code{demand_visits}.
When non-NULL, appended to \code{demand_long} for concordance assessment.}

\item{prevention_scenario}{Character key in \link{URPS_PREVENTION_SCENARIOS}, or
\code{NULL} (default). When non-NULL, service volumes are adjusted by
\code{\link[=apply_named_prevention_scenario]{apply_named_prevention_scenario()}} before \code{\link[=convert_workload_to_fte]{convert_workload_to_fte()}},
modelling conservative PFD management (PT/pessary) as a demand-side
multiplier. Use \code{"baseline"} to confirm the no-shift reference.}

\item{setting_scenario}{Character key in \link{URPS_SETTING_SCENARIOS}, or \code{NULL}
(default). When non-NULL, \code{\link[=apply_setting_scenario]{apply_setting_scenario()}} redistributes service
volumes across care-delivery settings and applies per-setting productivity
adjustments before \code{\link[=convert_workload_to_fte]{convert_workload_to_fte()}}. Scenarios include
\code{"telehealth_10pct"}, \code{"asc_migration_30pct"}, \code{"combined_shift"}.}

\item{output_dir}{If non-NULL, write provenance-tagged artifacts here.}

\item{seed}{RNG seed.}

\item{verbose}{Logical.}
}
\value{
List with supply, demand, required FTE, gap, growth adequacy,
concordance, outlook, validation, and run metadata.
}
\description{
Run the integrated workforce microsimulation
}
\examples{
# A small, fast run. n_iterations is 200 by default; two is enough to show
# the shape of the result.
result <- run_workforce_microsimulation(
  baseline_supply = 120,
  use_certification_cohorts = FALSE,
  years = 2025:2027,
  n_iterations = 2,
  supply_scenarios = supply_scenario_registry(20)["baseline"],
  pop_by_band = example_female_population_by_band(2025:2027),
  baseline_gap_estimate = baseline_gap(
    120, 0.95, method = "assumed",
    calibration_status = "uncalibrated_illustrative",
    evidence = "illustrative example"
  ),
  # An analogy-derived input must be DECLARED by the caller rather than
  # inherited silently; the run refuses to proceed otherwise.
  allow_analogy = TRUE,
  verbose = FALSE
)
names(result)
# Every run records whether its demand side was calibrated to an
# independent anchor. This one was not.
result$scenario_meta$demand_calibrated
}
\seealso{
Other run workforce microsimulation:
\code{\link[=example_capacity_survey]{example_capacity_survey()}},
\code{\link[=example_female_population_by_band]{example_female_population_by_band()}},
\code{\link[=example_service_volumes]{example_service_volumes()}}
}
\concept{core}
\concept{run workforce microsimulation}
