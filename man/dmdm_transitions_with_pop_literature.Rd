% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-pop_transitions.R
\name{dmdm_transitions_with_pop_literature}
\alias{dmdm_transitions_with_pop_literature}
\title{A DMDM transition object with literature-derived POP dynamics}
\usage{
dmdm_transitions_with_pop_literature(
  base = dmdm_default_transitions(),
  params = pop_transition_parameters()
)
}
\arguments{
\item{base}{A base transition object to overlay. Defaults to
\code{\link[=dmdm_default_transitions]{dmdm_default_transitions()}}.}

\item{params}{The POP parameter table. Defaults to \code{\link[=pop_transition_parameters]{pop_transition_parameters()}}.}
}
\value{
A transition list as in \code{\link[=dmdm_default_transitions]{dmdm_default_transitions()}}, with the POP
\code{onset}/\code{remission} replaced by literature values, plus \code{pop_progression},
\code{pop_regression}, \code{calibration_status} and \code{provenance}.
}
\description{
Overlays the placeholder POP row of a base transition object (default
\code{\link[=dmdm_default_transitions]{dmdm_default_transitions()}}) with the cited parameters from
\code{\link[=pop_transition_parameters]{pop_transition_parameters()}}: literature onset log-odds (cumulative vaginal
deliveries primary), a binary remission probability, and -- because POP is
graded and regresses -- explicit per-stage \code{pop_progression} and
\code{pop_regression} annual probabilities. UI and AI are left untouched.
}
\details{
The engines (\code{\link[=simulate_dmdm]{simulate_dmdm()}}, \code{\link[=simulate_dmdm_open]{simulate_dmdm_open()}}) read only the fields
they know, so the extra \code{pop_progression}/\code{pop_regression} elements are inert
for the two-state run and available to a staged consumer. The object is marked
\code{calibration_status = "derived_by_analogy"}, and \code{provenance} records the
per-condition status so the mixed pedigree (POP literature-derived; UI/AI still
placeholder) is explicit.
}
\seealso{
Other pop transitions: 
\code{\link{pop_transition_parameters}()}
}
\concept{demand}
\concept{pop transitions}
