% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-interval_coverage.R
\name{solve_interval_inflation}
\alias{solve_interval_inflation}
\title{Solve the interval inflation factor the historical series implies}
\usage{
solve_interval_inflation(
  certification_series,
  minimum_train_years = 6L,
  forecast_horizon = 3L,
  nominal_coverage = 0.95,
  maximum_inflation = 10,
  verbose = TRUE
)
}
\arguments{
\item{certification_series}{Data frame with \code{year} and \code{certification_count}.}

\item{minimum_train_years}{Integer. Default 6.}

\item{forecast_horizon}{Integer. Default 3.}

\item{nominal_coverage}{Numeric in \verb{(0, 1)}. Default 0.95.}

\item{maximum_inflation}{Numeric upper bound of search grid. Default 10.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
List with \code{solved_inflation}, \code{attained_coverage}, \code{reached_nominal},
\code{tier}, and \code{search_grid}.
}
\description{
Searches a grid for the smallest inflation factor whose rolling-origin
empirical coverage reaches \code{nominal_coverage}. When no factor in the grid
reaches it, returns the best attainable and flags \code{reached_nominal = FALSE}.
}
\details{
Searches for the smallest factor whose rolling-origin empirical coverage
reaches nominal. If no factor in the grid gets there, it returns the best
attainable and sets \code{reached_nominal = FALSE} rather than silently reporting
the grid maximum as a solution.

AN INFLATION FACTOR IS A POST HOC WIDENING, not a repair. It buys nominal
coverage by making every interval wider, including the ones that were already
right, and it does nothing about the reason the intervals were too narrow --
here, that parameters were held fixed across iterations. Prefer fixing the
generating model (see \code{\link[=supply_parameter_spec]{supply_parameter_spec()}}); use this to quantify how
far off the intervals are, and report the factor alongside any interval it
widened.
}
\seealso{
Other interval coverage:
\code{\link[=assert_interval_coverage_publishable]{assert_interval_coverage_publishable()}},
\code{\link[=fit_certification_baseline]{fit_certification_baseline()}},
\code{\link[=forecast_certification]{forecast_certification()}},
\code{\link[=rolling_origin_coverage]{rolling_origin_coverage()}}
}
\concept{interval coverage}
\concept{validation}
