% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-forecast_probabilities.R
\name{workforce_gap_probabilities}
\alias{workforce_gap_probabilities}
\title{Probability report for the workforce FTE gap}
\usage{
workforce_gap_probabilities(
  psa,
  metric = c("gap_pct", "gap_fte"),
  shortage_thresholds = c(0, 5, 10, 15),
  prob_level = 0.95
)
}
\arguments{
\item{psa}{A \code{\link[=psa_workforce_gap]{psa_workforce_gap()}} result (a list with \verb{$draws}), or a numeric
vector of gap draws.}

\item{metric}{Column of \verb{$draws} to summarise when \code{psa} is a PSA result;
\code{"gap_pct"} (percent of demand, default) or \code{"gap_fte"}.}

\item{shortage_thresholds}{Numeric thresholds for the shortage probabilities,
in the units of \code{metric} (percent for \code{"gap_pct"}). \code{0} gives P(any shortage).}

\item{prob_level}{Central prediction-interval mass.}
}
\value{
A list: \code{summary} (one-row data frame) and \code{probabilities} (a data
frame of \code{threshold}, \code{probability}, \code{statement}).
}
\description{
Applies \code{\link[=forecast_probabilities]{forecast_probabilities()}} to a \code{\link[=psa_workforce_gap]{psa_workforce_gap()}} result (or a raw
vector of gap draws) and phrases the exceedance probabilities as shortage
statements: the median gap, its prediction interval, and P(shortage exceeds
X\%) at each threshold. A threshold of \code{0} reads as "P(any shortage)".
}
\examples{
# Decision-relevant probabilities from a vector of gap draws (percent):
# P(any shortage) and P(shortage exceeds 10\%).
set.seed(1)
workforce_gap_probabilities(rnorm(500, mean = 8, sd = 4),
                            metric = "gap_pct", shortage_thresholds = c(0, 10))
}
\seealso{
\code{\link[=psa_workforce_gap]{psa_workforce_gap()}}, \code{\link[=forecast_probabilities]{forecast_probabilities()}}

Other forecast probabilities: 
\code{\link{forecast_probabilities}()}
}
\concept{forecast probabilities}
\concept{validation}
