% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-acgme_fellows.R
\name{entrant_to_cert_ratio}
\alias{entrant_to_cert_ratio}
\title{Entry-to-certification conversion, estimated against a chosen entry source}
\usage{
entrant_to_cert_ratio(
  source = c("acgme", "nrmp"),
  through_year = NULL,
  cert_lag = URPS_FELLOWSHIP_YEARS,
  exclude_disrupted = TRUE,
  pooled = TRUE,
  allow_implausible = FALSE
)
}
\arguments{
\item{source}{Entry source, passed to \code{\link[=entrant_source_series]{entrant_source_series()}}.}

\item{through_year}{Latest year either series may be read to.}

\item{cert_lag}{Years from entry to certification.}

\item{exclude_disrupted}{Drop backlog years, and -- when not pooling --
disrupted and release years too. Under pooling the disruption is retained on
purpose, because dropping half of a deferral pair biases the ratio.}

\item{pooled}{Sum both sides before dividing.}

\item{allow_implausible}{Return a conversion above 1.0 instead of erroring.
Only for deliberately demonstrating a misalignment -- a rate above one means
more people reached the outcome than entered, so the default refuses it.}
}
\value{
List with \code{ratio}, \code{source}, \code{years}, \code{n_years}, \code{excluded},
\code{cert_lag}, \code{pooled}, and \code{annual}.
}
\description{
Certifications in year \code{Y} are divided by entrants in \code{Y - cert_lag}.
}
\details{
\code{pooled = TRUE} (the default) sums both sides over the usable window before
dividing, rather than averaging annual ratios. That matters here because a
disruption and its release are the SAME fellows counted in two different
years: a pooled window spanning both nets the deferral out, while a mean of
annual ratios is dominated by whichever side of the break the window lands on.
It is why this estimator is stable where the NRMP-based one drifted from 0.755
to 1.197 across successive windows.
}
\seealso{
Other acgme fellows: 
\code{\link{acgme_entering_cohort}()},
\code{\link{acgme_urps_fellows}()},
\code{\link{assert_denominator_estimand}()},
\code{\link{denominator_estimands}()},
\code{\link{entrant_source_series}()}
}
\concept{acgme fellows}
\concept{supply}
