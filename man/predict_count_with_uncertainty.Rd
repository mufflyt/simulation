% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/26-utilization_models.R
\name{predict_count_with_uncertainty}
\alias{predict_count_with_uncertainty}
\title{Predict a log-link count model with parameter uncertainty}
\usage{
predict_count_with_uncertainty(model, newdata, n_draws = 1000L, offset = 0)
}
\arguments{
\item{model}{Fitted log-link model (e.g. from \code{\link[=fit_sling_rate_model]{fit_sling_rate_model()}}).}

\item{newdata}{Data to predict on; factor levels are aligned to the model.}

\item{n_draws}{Number of parameter draws (columns).}

\item{offset}{Optional numeric offset on the LINK scale (length 1 or
\code{nrow(newdata)}); default 0 gives the per-person rate for an offset model.}
}
\value{
Numeric matrix, \verb{nrow(newdata) x n_draws}, on the response scale.
}
\description{
Returns an \verb{nrow(newdata) x n_draws} matrix of response-scale predictions, one
column per multivariate-normal coefficient draw, so callers can take row-wise
quantiles for prediction intervals (combine with a \code{rpois}/\code{rnbinom} draw per
cell to add individual-level uncertainty on top of parameter uncertainty).
}
