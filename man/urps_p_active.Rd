% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-urps_flows.R
\name{urps_p_active}
\alias{urps_p_active}
\title{Probability of being active in a given projection year}
\usage{
urps_p_active(
  age,
  sex = "female",
  years_certified = 10,
  scenario_id = NULL,
  coef = URPS_P_ACTIVE_COEF,
  registry = NULL
)
}
\arguments{
\item{age}{Numeric age(s) in completed years. Must be in [18, 100].}

\item{sex}{Character sex: \code{"male"} or \code{"female"} (case-insensitive).
Recycled to \code{length(age)}.}

\item{years_certified}{Numeric years since first board certification (≥ 0).
Recycled to \code{length(age)}.}

\item{scenario_id}{Character scenario identifier from the supply scenario
registry (e.g. \code{"status_quo"}, \code{"retire_2yr_later"}). \code{NULL}
(default) applies no age-axis shift.}

\item{coef}{Named list of logistic coefficients; see \link{URPS_P_ACTIVE_COEF}.
Pass a list returned by \code{\link[=fit_p_active_model]{fit_p_active_model()}} to use empirically fitted
parameters.}

\item{registry}{Optional pre-fetched scenario registry list. When \code{NULL}
(default) the function calls \code{\link[=supply_scenario_registry]{supply_scenario_registry()}} once per call.}
}
\value{
Numeric vector of probabilities in [0, 1], one per provider.
Returns 0 for ages ≥ \code{MICROSIM_TERMINAL_AGE} and for ages < 18.
}
\description{
Logistic regression estimate of P(clinician is actively practising) given
current age, sex, and years since initial board certification. The function
is vectorised; all arguments except \code{scenario_id}, \code{coef}, and \code{registry}
are recycled to the longest input.
}
\details{
The model is calibrated to three HWSM Exhibit 17 survival anchors for a
male physician at the reference entry age (33):
P(35) ≈ 0.985, P(65) ≈ 0.55, P(75) ≈ 0.12.
Female sex is entered as OR ≈ 0.77 (HWSM Exhibit 16 analog for physician
subspecialists). See \link{URPS_P_ACTIVE_COEF} for coefficient details.

\code{scenario_id} resolves to \code{retirement_shift_years} from the supply scenario
registry and applies an age-axis shift—the same mechanism as
\code{\link[=shift_retirement_schedule]{shift_retirement_schedule()}}—rather than a multiplicative hazard knob:
"Retire 2 years later" treats a 65-year-old as age 63 in the predictor.
}
\examples{
# Single provider: male, age 45, 12 years certified, status quo
urps_p_active(45, "male", 12)

# Vectorised: compare female providers at key career stages
urps_p_active(c(35, 50, 65, 75), "female", c(2, 17, 32, 42))

# Scenario: retiring 2 years later raises P(active) at age 65
urps_p_active(65, "male", 32, scenario_id = "retire_2yr_later")
}
\seealso{
\code{\link[=departure_hazard]{departure_hazard()}}, \code{\link[=shift_retirement_schedule]{shift_retirement_schedule()}},
\code{\link[=participation_fte]{participation_fte()}}, \link{URPS_P_ACTIVE_COEF}, \code{\link[=fit_p_active_model]{fit_p_active_model()}}

Other urps flows: 
\code{\link{p_active_by_age}()}
}
\concept{supply}
\concept{urps flows}
