% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-entrant_regime.R
\name{draw_entrant_paths}
\alias{draw_entrant_paths}
\title{Draw entrant paths carrying regime-break uncertainty}
\usage{
draw_entrant_paths(
  model,
  years,
  n_draws = 1000L,
  include = c("trend", "dispersion", "break", "release_timing")
)
}
\arguments{
\item{model}{A \code{\link[=fit_entrant_regime_model]{fit_entrant_regime_model()}} object.}

\item{years}{Integer vector of projection years, all after the cutoff.}

\item{n_draws}{Number of paths.}

\item{include}{Character vector of components to switch on.}
}
\value{
Numeric matrix, \code{n_draws} rows by \code{length(years)} columns.
}
\description{
Each draw layers four sources, and each can be switched off to show what it
contributes:
}
\details{
\describe{
\item{\code{trend}}{Coefficients of the steady trend, drawn from \code{coef}/\code{vcov}
through the same internal \code{.param_draw()} the demand side uses. This is
the dominant term at a three-year horizon and the one the shipped engine
omitted entirely.}
\item{\code{dispersion}}{Negative-binomial count noise at the fitted dispersion,
rather than the Poisson noise that assumes variance equals mean.}
\item{\code{break}}{Per year, a Bernoulli draw at \code{break_probability}. A break
multiplies that year's entrants by a share drawn from the Beta prior and
DEFERS the remainder into the following year. A break late in the horizon
therefore pushes entrants past the end of the window, which is why this
term lowers the median as well as widening the band.}
\item{\code{release_timing}}{The deferred backlog clears over 1 to
\code{2 * release_years} years rather than exactly \code{release_years}. Nothing in
the pre-cutoff data pins the clearance schedule.}
}
}
