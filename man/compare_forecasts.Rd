% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-forecast_scorecard.R
\name{compare_forecasts}
\alias{compare_forecasts}
\title{Compare forecast models: scorecard, skill vs a benchmark, rank stability}
\usage{
compare_forecasts(
  data,
  model = "model",
  cutoff = "cutoff",
  observed = "observed",
  point = "predicted",
  lower = "lower",
  upper = "upper",
  interval_level = 0.95,
  benchmark = NULL,
  rank_metric = c("interval_score", "abs_pct_error")
)
}
\arguments{
\item{data}{Long data frame with a model-id column plus the forecast columns.}

\item{model}{Column naming the model.}

\item{cutoff}{Column naming the back-test origin/cutoff (required for rank
stability; \code{NULL} skips it).}

\item{observed, point, lower, upper, interval_level}{Passed to
\code{\link[=forecast_scorecard]{forecast_scorecard()}}.}

\item{benchmark}{Optional model id to score skill against (e.g. a naive/random-walk
forecast). \code{NULL} skips skill.}

\item{rank_metric}{Per-cutoff metric to rank by: \code{"interval_score"} (default,
sharpness-penalized) or \code{"abs_pct_error"}. Lower is better.}
}
\value{
A list: \code{scorecard} (per-model metrics + skill columns), \code{rank_stability}
(\code{model}, \code{mean_rank}, \code{rank_sd}, \code{best_fraction}, \code{n_cutoffs}) or \code{NULL}, and
\code{benchmark}.
}
\description{
Scores each model with \code{\link[=forecast_scorecard]{forecast_scorecard()}}, expresses accuracy relative to a
simple benchmark model (skill = 1 - model / benchmark, positive is better), and
-- the check a single aggregate hides -- measures each model's \emph{rank stability}
across cutoffs: a model that only wins on average but swings between best and
worst across origins is not a reliable forecaster.
}
