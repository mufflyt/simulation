% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_prevalence_to_incidence.R
\name{fit_prevalence_consistent_transitions}
\alias{fit_prevalence_consistent_transitions}
\title{Fit an epidemiologically consistent onset hazard to age-band prevalence.}
\usage{
fit_prevalence_consistent_transitions(
  observations,
  remission,
  ages = 18:100,
  knots = c(60, 70),
  p0 = 0,
  age_weights = NULL,
  smooth_sd = 1
)
}
\arguments{
\item{observations}{data frame of \code{\link[=prevalence_observation]{prevalence_observation()}} rows
(band, age_start, age_end, prevalence, se).}

\item{remission}{scalar external annual remission probability (held fixed).}

\item{ages}{integer age grid (default 18:100).}

\item{knots}{interior knots for the log-incidence basis (default c(60,70)).}

\item{p0}{youngest-age prevalence (default 0).}

\item{age_weights}{named numeric (by age) population weights for interval
integration; default uniform. Production use passes US-female age weights.}

\item{smooth_sd}{Normal prior SD (tau) on the non-intercept log-rate coefficients
(the smoothness prior). Smaller = smoother. Default 1.0.}
}
\value{
list: \code{beta}, \code{incidence} (by age), \code{fitted} (by band), \code{z} (per-band
standardized residual), \code{compatible} (all |z|<2), \code{loglik}, \code{worst_z},
\code{plausible} (all incidence in (0,1)), \code{remission}.
}
\description{
DisMod-style inverse problem: given an EXTERNAL remission, find the low-df
age-specific onset that makes the disease recurrence reproduce the observed
age-band prevalence, scoring the fit by a CI-informed logit-Gaussian likelihood
with population-weighted interval integration and a Normal smoothness prior on
the log-rate shape (a penalized MAP estimate).
}
\seealso{
Other prevalence-calibration: 
\code{\link{fit_prevalence_consistent_psa}()},
\code{\link{prevalence_from_incidence}()},
\code{\link{prevalence_observation}()}
}
\concept{prevalence-calibration}
