% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-access_response_bridge.R
\name{simulate_access_for_adequacy}
\alias{simulate_access_for_adequacy}
\title{Simulate national access outcomes implied by a base-year adequacy}
\usage{
simulate_access_for_adequacy(
  adequacy,
  base_supply_fte,
  wait_scale = 30,
  appointment_window = 30,
  wait_ceiling = Inf,
  insurance_fraction = 1,
  catchment = NULL
)
}
\arguments{
\item{adequacy}{Numeric, all \verb{> 0}; see \code{\link[=adequacy_access_load]{adequacy_access_load()}}.}

\item{base_supply_fte}{Numeric, all \verb{> 0}; base-year clinical FTE.}

\item{wait_scale}{Proportionality constant \code{k} in \code{wait = k * rho/(1-rho)},
passed to \code{\link[=clear_access]{clear_access()}}. Default 30 (illustrative; fit before
publishing).}

\item{appointment_window}{Window within which an appointment counts, same
time unit as \code{wait_scale}. Default 30.}

\item{wait_ceiling}{Finite value to censor saturated waits at, passed to
\code{\link[=clear_access]{clear_access()}}. Default \code{Inf}.}

\item{insurance_fraction}{Share of capacity accepting the patient, 0 to 1.
Default 1.}

\item{catchment}{Optional id vector; see \code{\link[=adequacy_access_load]{adequacy_access_load()}}.}
}
\value{
The national A-series tibble from \code{\link[=access_outcomes_national]{access_outcomes_national()}}
(\code{estimand}, \code{label}, \code{value}, \code{unit}, \code{calibration_status}).
}
\description{
Composes the forward response map end to end: \code{\link[=adequacy_access_load]{adequacy_access_load()}} turns
the adequacy into a \code{\link[=clear_access]{clear_access()}} load table, the queue clears it, and
\code{\link[=access_outcomes_national]{access_outcomes_national()}} rolls it up to the national A-series (wait,
appointment probability, unmet demand, utilization). This is the
"adequacy -> simulated appointment wait" path that did not previously exist.
}
\details{
The result is an ASSUMED response, not a fielded one: it inherits
\code{clear_access()}'s \code{calibration_status} (illustrative until \code{wait_scale} is
fitted) and does not resolve \code{\link[=capacity_status]{capacity_status()}}. It is the forward half of a
calibration loop -- vary \code{adequacy} (or \code{wait_scale}) and compare the A1 wait
against an observed distribution -- but performs no fitting itself. A scalar
\code{adequacy < 1} saturates (A1 wait \code{Inf}); pass a catchment-level adequacy
vector for a finite national wait.
}
\seealso{
\code{\link[=adequacy_access_load]{adequacy_access_load()}}, \code{\link[=clear_access]{clear_access()}},
\code{\link[=access_outcomes_national]{access_outcomes_national()}}, \code{\link[=fit_wait_scale]{fit_wait_scale()}}.

Other access response bridge:
\code{\link[=adequacy_access_load]{adequacy_access_load()}}
}
\concept{access response bridge}
\concept{calibration}
