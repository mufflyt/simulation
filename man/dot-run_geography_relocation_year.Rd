% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-end_to_end_geography_relocation_year.R
\name{.run_geography_relocation_year}
\alias{.run_geography_relocation_year}
\title{Apply one simulation year's county-endogenous geography relocation}
\usage{
.run_geography_relocation_year(
  provider_cohort,
  active_providers,
  simulation_year,
  geography_engine,
  county_market_tbl,
  geography_control,
  geography_solver
)
}
\arguments{
\item{provider_cohort}{Full provider cohort for the run.}

\item{active_providers}{Active-provider tibble for \code{simulation_year}.}

\item{simulation_year}{The calendar year being simulated.}

\item{geography_engine}{Either \code{"hrr_balance"} or \code{"county_endogenous"}.}

\item{county_market_tbl}{County market tibble, optionally year-varying
(filtered to \code{simulation_year} when it has a \code{year} column).}

\item{geography_control}{Extra arguments passed to \code{geography_solver}.}

\item{geography_solver}{Function \verb{(provider_roster, county_market_tbl, year, ...) -> data.frame | list(provider_roster, iterations, converged)}. Must return \code{provider_id} and \code{county_fips}.}
}
\value{
A list with \code{provider_cohort} (county_fips updated),
\code{active_providers} (recomputed from the updated cohort),
\code{geography_iterations}, and \code{geography_converged}.
}
\description{
Extracted from \code{\link[=run_end_to_end_simulation]{run_end_to_end_simulation()}}'s per-year loop so that
function stays under the repository's module code-line ceiling; this is a
pure code move (identical logic, now callable on its own) not a behavior
change. A no-op (returns the inputs unchanged, with \code{NA} iteration
diagnostics) unless \code{geography_engine == "county_endogenous"}.
}
\keyword{internal}
