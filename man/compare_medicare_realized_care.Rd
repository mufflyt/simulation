% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-medicare_realized_care.R,
%   R/supply-medicare_capacity.R
\name{compare_medicare_realized_care}
\alias{compare_medicare_realized_care}
\title{Compare modelled service volumes with Medicare realized care}
\usage{
compare_medicare_realized_care(predicted, observed, by = c("service"))

compare_medicare_realized_care(predicted, observed, by = c("service"))
}
\arguments{
\item{predicted}{Tibble with the grouping columns and \code{predicted_services}.}

\item{observed}{Output of \code{\link[=aggregate_medicare_realized_care]{aggregate_medicare_realized_care()}}.}

\item{by}{Grouping columns; normally \code{"service"}, or \code{c("year", "service")}
for a trajectory comparison.}
}
\value{
Tibble of \code{service}, \code{predicted_services}, \code{billed_services},
\code{calibration_ratio}, \code{comparison_estimand}.

Joined totals, calibration ratio, and residual. This ratio calibrates
\emph{realized Medicare care} only; it must not be applied to latent demand.
}
\description{
Returns a per-service CALIBRATION RATIO, observed over predicted. Deliberately
does NOT return a latent-demand scalar: both sides are realized quantities, so
their ratio says how well the model reproduces billing under the same access
and payer restrictions -- not what share of need is met. Rescaling modelled
demand by this ratio would import Medicare's access and payer truncation into
an all-payer estimand.
}
