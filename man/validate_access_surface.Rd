% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-access_surface.R
\name{validate_access_surface}
\alias{validate_access_surface}
\title{Validate an imported tract-level access surface against this model}
\usage{
validate_access_surface(
  surface,
  providers = NULL,
  need = NULL,
  surface_year = 2020L,
  access_col = "access_mean_population",
  workforce_tol = 0.1,
  max_uncovered_need = 0.02,
  provenance = NULL
)
}
\arguments{
\item{surface}{Tract-level surface with \code{GEOID} and an access column.}

\item{providers}{Provider-level table from the same run, or \code{NULL}.}

\item{need}{Tract need table with \code{GEOID} and \code{need}; defaults to the
committed \code{artifacts/tract_pfd_need.csv} when available.}

\item{surface_year}{Year the provider universe represents.}

\item{access_col}{Name of the accessibility column.}

\item{workforce_tol}{Allowed shortfall in provider count, as a share.}

\item{max_uncovered_need}{Allowed share of need with no access value.}

\item{provenance}{Optional list with \code{path} and \code{sha256}.}
}
\value{
List with \code{gates} (tibble), \code{verdict}, and \code{blocking}.
}
\description{
Fail-closed semantic check. Reports one row per gate in
\link{ACCESS_SURFACE_GATES} with the measured value, and an overall verdict.
}
\details{
MISSING IS NOT ZERO. A tract absent from the surface and a tract with zero
accessible capacity mean different things, and averaging them together
silently converts absent geography into a scarcity finding. The coverage gate
therefore reports them separately and fails if absent tracts carry more than
\code{max_uncovered_need} of modelled need.
}
\seealso{
Other access: 
\code{\link{ACCESS_PROVIDER_DISPOSITIONS}},
\code{\link{ACCESS_SURFACE_GATES}},
\code{\link{TEMPORAL_ACCESS_MODES}},
\code{\link{URPS_ACCESS_TIME_UNIT}},
\code{\link{URPS_WAIT_OBSERVATIONS}},
\code{\link{access_ascertainment_status}()},
\code{\link{access_provider_flow}()},
\code{\link{access_surface_for_demand}()},
\code{\link{access_validation_targets}()},
\code{\link{assert_access_surface_usable}()},
\code{\link{assert_temporal_access_comparison}()},
\code{\link{contemporary_access_year}()},
\code{\link{fit_wait_scale}()},
\code{\link{set_access_target}()},
\code{\link{urps_appointment_window}()},
\code{\link{urps_observed_wait_days}()},
\code{\link{validate_access_outcomes}()}
}
\concept{access}
\concept{validation}
