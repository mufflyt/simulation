% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-preregistration.R
\name{rolling_origin_evaluation}
\alias{rolling_origin_evaluation}
\title{Leakage-free rolling-origin evaluation, gated on a preregistration}
\usage{
rolling_origin_evaluation(
  data,
  time_col,
  target_col,
  origins,
  horizon = 1L,
  fit_predict,
  prereg = NULL,
  spec = NULL
)
}
\arguments{
\item{data}{Data frame with a time column and a target column (e.g. an annual
national workforce series).}

\item{time_col, target_col}{Column names for the time index and the observed
target.}

\item{origins}{Integer vector of origin times to evaluate.}

\item{horizon}{Steps ahead to forecast. Default 1.}

\item{fit_predict}{\verb{function(train, target_time)} returning a prediction for
the target time's row(s); must fit only on \code{train} and never see the target.}

\item{prereg}{Optional preregistration record or path; when given, \code{spec} is
required and verified before any evaluation.}

\item{spec}{The live spec, required when \code{prereg} is given.}
}
\value{
A list: \code{by_origin} (\code{origin}, \code{target_time}, \code{observed}, \code{predicted},
\code{signed_error}, \code{abs_pct_error}) and \code{summary} (\code{n}, \code{mape}, \code{rmse},
\code{horizon}, \code{all_targets_future}, \code{preregistered}, \code{spec_hash}).
}
\description{
For each origin, refits with \code{fit_predict} on data up to and including that
origin and predicts \code{horizon} steps ahead, then scores the prediction against
the observed target. Leakage-free by construction: an origin's fit sees only
rows at or before it, and the target is strictly in its future. When a
preregistration is supplied, the run is refused unless the live \code{spec} matches
it -- so a clean rolling-origin number cannot be produced from a spec that was
altered after the targets were seen.
}
\seealso{
\code{\link[=preregister_spec]{preregister_spec()}}, \code{\link[=assert_spec_matches_prereg]{assert_spec_matches_prereg()}}

Other preregistration: 
\code{\link{assert_spec_matches_prereg}()},
\code{\link{preregister_spec}()}
}
\concept{preregistration}
\concept{validation}
