% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-isochrone_access_response.R
\name{fit_decay_sigma}
\alias{fit_decay_sigma}
\title{Fit the E2SFCA decay parameter sigma to fielded Lizeth waits}
\usage{
fit_decay_sigma(
  lizeth_access,
  catchments_for_sigma,
  sigma_bounds = c(15, 240),
  wait_col = "wait_business_days",
  npi_col = "npi",
  weight_col = NULL,
  wait_ceiling = 365,
  bands = NULL,
  n_grid = 25L
)
}
\arguments{
\item{lizeth_access}{Call-level records with \code{npi_col} and \code{wait_col} (from
\code{\link[=prepare_lizeth_access]{prepare_lizeth_access()}}).}

\item{catchments_for_sigma}{Function mapping a scalar \code{sigma} to a
provider-catchment table (see Details).}

\item{sigma_bounds}{Length-2 positive numeric search interval for \code{sigma}, in
minutes. Default \code{c(15, 240)}.}

\item{wait_col, npi_col, weight_col, wait_ceiling}{Passed to
\code{\link[=join_lizeth_to_catchments]{join_lizeth_to_catchments()}} / \code{\link[=lizeth_wait_response_loss]{lizeth_wait_response_loss()}}.}

\item{bands}{Optional band vector; when supplied, the fitted weights
\code{gaussian_band_weights(bands, sigma)} are returned. Default \code{NULL}.}

\item{n_grid}{Number of points in the coarse grid scanned across
\code{sigma_bounds} before refinement. Must be at least 3. Default \code{25L}.}
}
\value{
A list: \code{sigma} (fitted), \code{wait_scale} (at the fitted sigma), \code{sse},
\code{rmse}, \code{n_pairs} (unsaturated matched calls), \code{weights} (if \code{bands} given),
\code{calibration_status = "fitted_to_lizeth_wait_response"}, and a
\code{summary_sentence}.
}
\description{
Fits the single Gaussian decay parameter \code{sigma} (see \code{\link[=gaussian_band_weights]{gaussian_band_weights()}})
by minimising the per-provider wait-response loss over the drive-time bands:
the sigma whose isochrone-derived access, pushed through the queue, best
predicts the observed Lizeth waits. \code{wait_scale} is refit closed-form at every
candidate sigma (\code{\link[=lizeth_wait_response_loss]{lizeth_wait_response_loss()}}), so this is a clean 1-D search,
not a joint optimisation -- one shape parameter, identified by the curvature of
wait against access, with the level absorbed by \code{wait_scale}.
}
\details{
\code{catchments_for_sigma} is a function of one argument, \code{sigma}, returning the
provider-catchment table (\code{\link[=e2sfca_catchments_from_access]{e2sfca_catchments_from_access()}}) computed with
\code{gaussian_band_weights(bands, sigma)}. Injecting it keeps this optimiser
independent of the spatial recompute: the standard closure (recompute E2SFCA
per sigma) is built in the runner, while tests supply a lightweight map. Each
evaluation joins the Lizeth calls to that sigma's catchments and scores the
unsaturated pairs; folds with fewer than two usable pairs score \code{Inf} so the
search avoids degenerate regions. The SSE surface has a sharp global well on
an otherwise flat plateau, so a coarse grid of \code{n_grid} points brackets the
minimum and \code{\link[stats:optimize]{stats::optimize()}} refines within one grid step of the best
point.
}
\seealso{
Other isochrone access response:
\code{\link[=capacity_status_with_isochrone_response]{capacity_status_with_isochrone_response()}},
\code{\link[=e2sfca_catchments_from_access]{e2sfca_catchments_from_access()}},
\code{\link[=join_lizeth_to_catchments]{join_lizeth_to_catchments()}},
\code{\link[=lizeth_wait_response_loss]{lizeth_wait_response_loss()}},
\code{\link[=wait_response_region_holdout]{wait_response_region_holdout()}}
}
\concept{calibration}
\concept{isochrone access response}
