% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-parameter_uncertainty.R
\name{draw_supply_parameters}
\alias{draw_supply_parameters}
\title{Draw one parameter set for a Monte Carlo iteration}
\usage{
draw_supply_parameters(spec, schedule = RETIREMENT_HAZARD_BY_AGE, years = NULL)
}
\arguments{
\item{spec}{A \code{\link[=supply_parameter_spec]{supply_parameter_spec()}}.}

\item{schedule}{Base retirement hazard schedule.}

\item{years}{Simulated years. Required when \code{spec} carries an entrant regime
model, because that draw is a per-year path rather than a scalar rate. The
returned \code{entrants} is then one value per TRANSITION -- \code{length(years) - 1},
element i being the cohort entering between \code{years[i]} and \code{years[i + 1]} --
which is one of the lengths \code{\link[=simulate_provider_career_once]{simulate_provider_career_once()}} accepts.}
}
\value{
List with \code{entrants}, \code{retirement_schedule}, \code{hours_coef}.
}
\description{
\code{hours_coef} is the COEFFICIENT VECTOR of the fitted weekly-clinical-hours
model (\code{clinical_hours ~ ns(age, df) + sex}), drawn from its own \code{coef}/\code{vcov}
-- not a scalar multiplier. The no-uncertainty case is therefore
\code{hours_coef == coef(spec$hours_model)}, which reproduces the point-estimate
fit exactly. Substitute it into the model with \code{.hours_model_with_coef()}
rather than applying it to anything by hand.
}
\examples{
# One draw per Monte Carlo iteration. `quantified` records WHICH parameters
# actually vary -- anything published without a standard error is held fixed
# and says so, rather than being given an invented spread.
spec <- supply_parameter_spec(entrant_series = c(50, 57, 53, 59, 59),
                              entrant_mean = 55)
draw <- draw_supply_parameters(spec)
draw$entrants_per_year
}
\seealso{
Other parameter uncertainty: 
\code{\link{assert_parameter_uncertainty}()},
\code{\link{entrant_spec_from_series}()},
\code{\link{series_mean_se}()},
\code{\link{supply_parameter_spec}()}
}
\concept{calibration}
\concept{parameter uncertainty}
