% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-chia_physician_attribution.R
\name{build_chia_hdd_discharge_procedure_views}
\alias{build_chia_hdd_discharge_procedure_views}
\title{Build the procedure-classification views (raw + specialty-share refined)}
\usage{
build_chia_hdd_discharge_procedure_views(con, min_n = 20, surgical_share_threshold = 50)
}
\arguments{
\item{con}{Open, writable DuckDB connection.}

\item{min_n}{Minimum physician-attributed occurrences before a code's
specialty share is trusted to override the raw classification.}

\item{surgical_share_threshold}{Below this \code{pct_surgical}, a raw-operative
code is downgraded to non_operative.}
}
\value{
\code{con}, invisibly.
}
\description{
Two passes, in dependency order:
\enumerate{
\item \code{v_hdd_discharge_procedure_raw}: a coding-system-level operative rule
(ICD-9-CM category 01-86 for FY<=2015; ICD-10-PCS section 0 or 1 for
FY2016+). Deliberately coarse -- both systems file bedside and
endoscopic procedures under "surgical" categories despite not being OR
surgery.
\item \code{v_procedure_specialty_share} + the final \code{v_hdd_discharge_procedure}:
empirically downgrades a raw-operative code to non_operative when fewer
than half of its physician-attributed occurrences (among non-newborn
discharges, \code{n >= 20}) are performed by a surgical-keyword specialty.
Newborn discharges are excluded from the SHARE COMPUTATION itself (not
just the final case count) because the delivering physician is recorded
as operating physician on newborn stays, which would otherwise swamp a
code's true performer mix -- confirmed empirically to wrongly downgrade
genuinely-surgical newborn-adjacent codes (e.g. circumcision) if left in.
}
}
\details{
Requires \code{\link[=build_chia_hdd_discharge_physician_view]{build_chia_hdd_discharge_physician_view()}} to have been run
first (joins against \code{chia_casemix.v_hdd_discharge_physician}) and a
\code{chia_provider.borim_stdrel_npi_straight_from_cd} table to exist.
}
\concept{chia physician attribution}
\concept{supply}
