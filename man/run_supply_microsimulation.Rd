% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-provider_microsimulation.R
\name{run_supply_microsimulation}
\alias{run_supply_microsimulation}
\title{Run the supply microsimulation with Monte-Carlo replicates}
\usage{
run_supply_microsimulation(
  initial_workforce = 1306,
  years = 2025:2050,
  entrants_per_year = 55,
  subspecialty = "FPMRS",
  n_iterations = 500,
  conversion_floor = 1,
  retirement_schedule = RETIREMENT_HAZARD_BY_AGE,
  hazard_table = NULL,
  fte_method = "hours",
  hours_model = NULL,
  hours_multiplier = 1,
  hours_intercept = HWSM_HOURS_INTERCEPT,
  late_career_fte_factor = 1,
  late_career_fte_onset_age = NA_real_,
  placement_shares = NULL,
  param_spec = NULL,
  allow_fixed_parameters = FALSE,
  ci = 0.95,
  seed = 20260801L,
  verbose = TRUE,
  p_active_coef = NULL,
  p_active_scenario_id = NULL,
  thin_by_p_active = FALSE
)
}
\arguments{
\item{initial_workforce}{Base-year workforce size, OR a prebuilt agent tibble.}

\item{years}{Integer vector of years to project.}

\item{entrants_per_year}{Annual entrants. IGNORED when \code{param_spec} carries an
\code{entrant_mean}, which takes precedence; passing both with different values
warns. A spec that carries no \code{entrant_mean} leaves this argument in force.}

\item{subspecialty}{Subspecialty label (selects the baseline hazard).}

\item{n_iterations}{Number of Monte-Carlo replicates.}

\item{conversion_floor}{cliff graduate-to-practice conversion (0.70-1.0).}

\item{retirement_schedule}{Single-year retirement hazard schedule; scenarios
supply a SHIFTED schedule (retire +/- n years) rather than a multiplier.}

\item{hazard_table}{Optional legacy age-band hazard table. Supplying it
switches back to the coarse seven-band gradient.}

\item{fte_method}{FTE method (see \code{\link[=provider_clinical_fte]{provider_clinical_fte()}}).}

\item{hours_model}{Optional fitted hours model.}

\item{hours_multiplier}{Scenario knob on hours worked.}

\item{hours_intercept}{Hours-schedule intercept; use
\code{\link[=calibrate_hours_intercept]{calibrate_hours_intercept()}} so the schedule and the FTE threshold agree.}

\item{late_career_fte_factor}{Multiplier on clinical FTE from
\code{late_career_fte_onset_age} (mufflyaccess scenario field).}

\item{late_career_fte_onset_age}{Age from which the factor applies.}

\item{placement_shares}{Optional geographic share table enabling entrant
placement and mid-career migration.}

\item{param_spec}{Optional \code{\link[=supply_parameter_spec]{supply_parameter_spec()}}. When supplied, the
entrant rate (and any other quantified parameter) is REDRAWN each
iteration, so the reported intervals carry forecast uncertainty rather than
Monte Carlo sampling noise alone. Without it the intervals are far too
narrow -- see docs/BACKTEST_2020_TO_2023.md. When the spec carries an
\code{hours_model}, its coefficients are redrawn per iteration and take
precedence over the \code{hours_model} argument; they widen \code{effective_fte} only
and leave \code{headcount} untouched.}

\item{allow_fixed_parameters}{Declare an exploratory run in which every
parameter is held fixed. The uncertainty guard runs UNCONDITIONALLY -- a
missing \code{param_spec} is precisely the case it exists to catch -- so this is
the only way to obtain a sampling-noise-only band, and it always warns. The
resulting \code{effective_fte_lo}/\verb{_hi} are not forecast intervals.}

\item{ci}{Width of the reported credible band (default 0.95).}

\item{seed}{Integer RNG seed.}

\item{verbose}{Logical.}

\item{p_active_coef}{Optional logistic coefficients for \code{\link[=urps_p_active]{urps_p_active()}},
overriding \code{URPS_P_ACTIVE_COEF}. Supplying them switches clinical FTE onto
the P(active) formulation.}

\item{p_active_scenario_id}{Scenario id whose \code{retirement_shift_years} shifts
the P(active) age axis.}

\item{thin_by_p_active}{Multiply each provider's clinical FTE by their
probability of remaining active, rather than treating the active set as a
hard in/out split.}
}
\value{
List: \code{summary} (per-year quantiles), \code{iterations} (all replicate
panels), and \code{scenario} metadata. \code{scenario$entrants_per_year} is the rate
the engine RESOLVED (the spec's mean when the spec won, not the argument),
and \code{scenario$entrants_source} names which input supplied it.
}
\description{
Repeats \code{\link[=simulate_provider_career_once]{simulate_provider_career_once()}} \code{n_iterations} times and summarises
the distribution of headcount / effective-FTE per year (median + CI band),
giving DISTRIBUTIONAL supply projections rather than a single deterministic
line. Seeded via \code{\link[=seed_microsimulation]{seed_microsimulation()}} for reproducibility.
}
