% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-research_db.R
\name{open_research_db}
\alias{open_research_db}
\title{Open a research database read-only, with schema and row-count verification}
\usage{
open_research_db(
  path,
  required_tables = character(0),
  min_rows = NULL,
  what = basename(path \%||\% "database")
)
}
\arguments{
\item{path}{Path from \code{\link[=resolve_research_db]{resolve_research_db()}}.}

\item{required_tables}{Character vector of tables that must be present.}

\item{min_rows}{Named integer vector of minimum row counts, or \code{NULL}.}

\item{what}{Human-readable name used in messages.}
}
\value{
A list with the open \code{con} (caller must disconnect) and a
\code{provenance} tibble recording what was verified.
}
\description{
Open a research database read-only, with schema and row-count verification
}
\details{
Verification is ordered deliberately. Existence is confirmed before any
connection is opened; the schema is confirmed before any count is trusted;
and a required table that is EMPTY is treated as a failure rather than as a
zero result, because an empty table is exactly what a mis-resolved path
produces.
}
\seealso{
Other research database:
\code{\link[=resolve_research_db]{resolve_research_db()}}
}
\concept{core}
\concept{research database}
