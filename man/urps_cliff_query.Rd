% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-retirement_hazard.R
\name{urps_cliff_query}
\alias{urps_cliff_query}
\title{Canonical URPS Retirement-Event Query for the NBER DuckDB}
\usage{
urps_cliff_query(min_confidence = 0.6, age_range = c(30, 80))
}
\arguments{
\item{min_confidence}{Minimum \code{retirement_confidence_final}. Default 0.60.}

\item{age_range}{Plausible age-at-exit bounds. Default \code{c(30, 80)}.}
}
\value{
A single SQL string.
}
\description{
Emits SQL producing one row per observed URPS departure, with the columns
\code{\link[=build_urps_exit_hazard]{build_urps_exit_hazard()}} expects (\code{age}, \code{sex}, \code{confidence_score}).
}
\details{
Cohort is defined by \strong{NPPES taxonomy \verb{207VF0040X}} (ObGyn / Female Pelvic
Medicine & Reconstructive Surgery) across the 2013-2024 yearly rosters, not
by the procedure-derived ML predictions in
\code{main.obgyn_subspecialty_ml_predictions}. That table is built from billing
volume, so retirees are absent by construction: only 16 of its 653 FPMRS
NPIs appear in \code{credentials.retirement_consensus} (2.4\%) against a
roster-wide rate of 34\%. \code{crosswalk.subspecialty_by_npi} is likewise
unusable — its \code{npi} column holds 4-5 character surrogate keys, not NPIs.

Age is \code{retirement_year_final - year_of_birth}, per the 2026-08-13 decision
to use Doximity \code{year_of_birth} directly rather than a graduation-year
proxy; at this sample size the retirement join, not the age join, is the
binding constraint.
}
\seealso{
Other retirement hazard: 
\code{\link{build_urps_exit_hazard}()}
}
\concept{retirement hazard}
\concept{supply}
