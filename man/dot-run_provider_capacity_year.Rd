% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-end_to_end_provider_capacity_year.R
\name{.run_provider_capacity_year}
\alias{.run_provider_capacity_year}
\title{Compute one simulation year's provider capacity}
\usage{
.run_provider_capacity_year(
  active_providers,
  simulation_year,
  productivity_engine,
  productivity_predictor,
  fitted_productivity_model
)
}
\arguments{
\item{active_providers}{Active-provider tibble for \code{simulation_year}.}

\item{simulation_year}{The calendar year being simulated.}

\item{productivity_engine}{Either \code{"benchmark"} or \code{"lmer_fitted"}.}

\item{productivity_predictor}{Optional custom predictor function
\verb{(model, providers, year) -> capacity}. When \code{NULL} and
\code{productivity_engine == "lmer_fitted"}, falls back to
\code{.lmer_fitted_predictor_bridge()} + \code{\link[=predict_provider_capacity]{predict_provider_capacity()}}.}

\item{fitted_productivity_model}{A \code{model_bundle} from
\code{\link[=fit_provider_productivity_model]{fit_provider_productivity_model()}}, required when
\code{productivity_engine == "lmer_fitted"}.}
}
\value{
A tibble with \code{provider_id} and \code{annual_patient_capacity}.
}
\description{
Extracted from \code{\link[=run_end_to_end_simulation]{run_end_to_end_simulation()}}'s per-year loop so that
function stays under the repository's module code-line ceiling; this is a
pure code move (identical logic, now callable on its own) not a behavior
change. Dispatches on \code{productivity_engine}: \code{"benchmark"} uses a flat
1,600 patients/FTE assumption, \code{"lmer_fitted"} predicts from a fitted
mixed-effects productivity model (via a caller-supplied
\code{productivity_predictor}, or the built-in
\code{.lmer_fitted_predictor_bridge()} + \code{\link[=predict_provider_capacity]{predict_provider_capacity()}} path,
which is a SYNTHETIC-FIT demonstration unless the model was trained on a
real, non-mock productivity panel).
}
\keyword{internal}
