% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geography-spatial_access_data.R
\name{real_access_surface}
\alias{real_access_surface}
\title{Compute a real access surface (tract demand x provider isochrones x supply)}
\usage{
real_access_surface(
  iso_sf,
  supply,
  weights = E2SFCA_DEFAULT_WEIGHTS,
  step2_power = 1,
  provider_col = "coord_id",
  band_col = "drive_time",
  mode = resolve_reproducibility_mode()
)
}
\arguments{
\item{iso_sf}{Provider isochrones (\code{sf}); e.g. \code{\link[=load_provider_isochrones]{load_provider_isochrones()}}.}

\item{supply}{Tibble \code{provider_id}, \code{supply} (S_j).}

\item{weights}{Cumulative band weights.}

\item{step2_power}{1 = E2SFCA, 2 = M2SFCA.}

\item{provider_col, band_col}{Column names in \code{iso_sf}.}

\item{mode}{Reproducibility mode.}
}
\value{
The \code{\link[=compute_e2sfca_access]{compute_e2sfca_access()}} result over the real tract demand.
}
\description{
Convenience composer: loads the real tract demand, builds membership from the
supplied provider isochrones, and runs the E2SFCA engine. The provider
isochrones are the only non-vendored input (see \code{\link[=load_provider_isochrones]{load_provider_isochrones()}}).
}
