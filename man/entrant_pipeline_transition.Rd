% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-review_followups.R
\name{entrant_pipeline_transition}
\alias{entrant_pipeline_transition}
\title{Project active clinical entrants from NRMP matches through a staged pipeline}
\usage{
entrant_pipeline_transition(
  matches,
  p_complete_cert = 0.86,
  p_active_practice = 0.9,
  cert_lag = URPS_FELLOWSHIP_YEARS,
  active_lag = 0L,
  defer_shortfall = TRUE,
  status = "assumed_illustrative"
)
}
\arguments{
\item{matches}{Data frame with integer \code{year} and non-negative \code{matched}
(NRMP fellowship matches per year).}

\item{p_complete_cert}{Fraction of matched fellows who complete fellowship and
achieve board certification. Either one value in 0 to 1, one value per row of
\code{matches}, or a data frame of \code{year} and \code{p_complete_cert} keyed on the
CERTIFICATION year (years it omits fall back to its median). Defaults to
0.86, the pooled entry-to-certification conversion from
\code{\link[=entrant_to_cert_ratio]{entrant_to_cert_ratio()}} against the ACGME fellow counts. Two earlier
defaults were worse: 0.95 was an assumption that over-predicted every
observed year by 8 to 18 certifications, and 0.75 was estimated on a window
containing the COVID trough but not its release, which biases it low.}

\item{p_active_practice}{Fraction of newly certified who enter active US
clinical practice. In 0 to 1. Default 0.90 (labeled assumption).}

\item{cert_lag}{Years from match to certification. Non-negative integer.
Defaults to the three-year fellowship, \code{URPS_FELLOWSHIP_YEARS}. The former
default of 1 contradicted the fellowship length this package documents, and
scored worse against every observed year.}

\item{active_lag}{Years from certification to active practice. Non-negative
integer. Default 0.}

\item{defer_shortfall}{When \code{p_complete_cert} varies by year, carry each
year's shortfall against the undisrupted conversion into the next year
instead of discarding it. A cancelled examination defers fellows; it does
not remove them from the pipeline.}

\item{status}{Calibration status stamped on the output. Default
"assumed_illustrative" -- replace the stage fractions with cited values.}
}
\value{
A tibble: \code{year}, \code{matched}, \code{certified}, \code{active_entrants},
\code{deferred_in} (fellows carried in from a suppressed earlier year), plus the
stage fractions and \code{calibration_status}. Years whose source match year is
absent yield \code{NA} (leading edge of the lag).
}
\description{
\code{active_entrants[y] = matched[y - cert_lag - active_lag] * p_complete_cert * p_active_practice}. Each stage is an explicit, labeled conversion; a fellowship
MATCH is not an active US clinical entrant.
}
\details{
Three stages: NRMP matches, board certification \code{cert_lag} years later, then
active practice after \code{active_lag}. Leading years are \code{NA} because their
source cohort predates the supplied series -- that is a missing input, not an
estimate of zero entrants, and it must not be filled with one.

\code{p_complete_cert} accepts a per-year schedule as well as a scalar, which is
what makes a cancelled examination representable: 2020's board examination
did not happen. With \code{defer_shortfall = TRUE} (the default) the fellows a
suppressed year fails to certify are carried forward into the following year
as \code{deferred_in} rather than deleted, because a cancelled exam delays a
cohort, it does not destroy it.
}
\examples{
# NRMP matches -> board certification -> active practice, with the three-year
# fellowship lag. Early years are NA because their source cohort predates the
# series rather than because nobody entered.
entrant_pipeline_transition(
  data.frame(year = 2018:2023, matched = c(55, 58, 60, 60, 62, 64))
)
}
\seealso{
Other review followups: 
\code{\link{international_migration_assumption}()},
\code{\link{nrmp_match_to_cert_ratio}()},
\code{\link{supply_uncertainty_drivers}()}
}
\concept{review followups}
\concept{supply}
