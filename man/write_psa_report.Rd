% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-psa_reporting.R
\name{write_psa_report}
\alias{write_psa_report}
\title{Write a PSA run as a provenance-guarded artifact plus readable tables}
\usage{
write_psa_report(
  psa,
  output = psa$output_names[1],
  output_dir = "outputs",
  prefix = "psa",
  inputs = NULL,
  code_paths = character(0),
  deficit_direction = c("negative", "positive"),
  deficit_threshold = 0,
  write_plot = TRUE,
  mode = resolve_reproducibility_mode()
)
}
\arguments{
\item{psa}{A \code{\link[=run_psa]{run_psa()}} result.}

\item{output}{Output column to report. Defaults to the first.}

\item{output_dir}{Directory to write into; created if absent.}

\item{prefix}{Filename prefix for every file written.}

\item{inputs}{Model inputs to record in the provenance fingerprint.}

\item{code_paths}{Source files to fingerprint alongside the inputs.}

\item{deficit_direction, deficit_threshold}{Deficit definition, recorded in the
fingerprint because \code{p_deficit} is meaningless without it.}

\item{write_plot}{Also write a tornado plot PNG. Requires ggplot2.}

\item{mode}{Reproducibility mode passed through to the artifact writer.}
}
\value{
A list of the paths written: \code{artifact}, \code{draws}, \code{summary}, \code{prcc},
\code{srrc}, and \code{plot} (NA when not written).
}
\description{
Freezes the run itself as the artifact (so \code{draws} round-trip exactly) and
writes the summary, PRCC, and SRRC tables beside it as CSV.
}
\details{
The provenance fingerprint covers the model inputs, the PSA input specs, the
chosen output, and the deficit definition. Reading the artifact back with any
of those changed fails rather than returning a stale object.
}
\seealso{
Other psa reporting: 
\code{\link{psa_outcome_summary}()}
}
\concept{calibration}
\concept{psa reporting}
