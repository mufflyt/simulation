% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_demand_contract.R
\name{export_dpmm_demand_contract}
\alias{export_dpmm_demand_contract}
\title{Export the DPMM demand trajectory as a versioned downstream contract}
\usage{
export_dpmm_demand_contract(
  dpmm_results,
  output_directory,
  model_version = DPMM_DEMAND_CONTRACT_VERSION,
  base_year = 2025L,
  calendar_year_offset = 2024L,
  us_women_40plus = 85000000,
  calibration_status = "uncalibrated_illustrative",
  verbose = TRUE
)
}
\arguments{
\item{dpmm_results}{List returned by the DPMM analysis; must contain
\code{composite_scores$population_statistics} with columns \code{year},
\code{living_population}, \code{incontinence_prevalence}, and (optionally)
\code{moderate_plus_prevalence}, \code{severe_incontinence_prevalence}.}

\item{output_directory}{Directory to write into (created if missing).}

\item{model_version}{Version string stamped on every row and the manifest.
Defaults to \code{DPMM_DEMAND_CONTRACT_VERSION}.}

\item{base_year}{Calendar year the index is normalized to (= 100). Default 2025.}

\item{calendar_year_offset}{\code{calendar_year = year + offset}. The DPMM encodes
sim \code{year} starting at 1 for calendar 2025, so offset = 2024. Default 2024.}

\item{us_women_40plus}{National denominator used to scale prevalence to case
counts (persons). Default 85e6, matching the DPMM national scale-up.}

\item{calibration_status}{Provenance guard for downstream gating. Keep
"uncalibrated_illustrative" until the transition model is validated.}

\item{verbose}{Log progress. Default TRUE.}
}
\value{
(invisibly) a list with \code{csv_path}, \code{manifest_path}, and the tidy \code{data}.
}
\description{
Turns a DPMM results object into (1) a tidy long CSV of demand
denominators by calendar year and hierarchy tier, indexed to the base year,
and (2) a JSON provenance manifest. Written to \code{output_directory} as
\verb{dpmm_demand_contract_v<version>.csv} and \code{..._manifest.json}.
}
