% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-forecast_probabilities.R
\name{forecast_probabilities}
\alias{forecast_probabilities}
\title{Probability summary of a Monte Carlo output}
\usage{
forecast_probabilities(
  x,
  prob_level = 0.95,
  exceed = NULL,
  direction = c("above", "below"),
  label = NA_character_,
  na.rm = TRUE
)
}
\arguments{
\item{x}{Numeric draws (e.g. one column of a \code{\link[=run_psa]{run_psa()}} result's \code{draws}).}

\item{prob_level}{Central interval mass (e.g. 0.95 gives the 2.5/97.5
quantiles).}

\item{exceed}{Optional numeric threshold(s) for probability statements; \code{NULL}
skips them.}

\item{direction}{\code{"above"} reports \code{P(x > threshold)} (the default), \code{"below"}
reports \code{P(x < threshold)}.}

\item{label}{Optional label carried into the summary.}

\item{na.rm}{Drop \code{NA} draws (failed PSA evaluations) before summarising;
their count is reported as \code{n_na}.}
}
\value{
A list: \code{summary} (one-row data frame: \code{label}, \code{n}, \code{n_na}, \code{mean},
\code{median}, \code{pi_lo}, \code{pi_hi}, \code{prob_level}) and \code{exceedance} (a data frame of
\code{threshold}, \code{direction}, \code{probability}, \code{statement}, or \code{NULL}).
}
\description{
Turns a vector of draws into the report a decision-maker wants: the median, a
central prediction interval, and -- optionally -- the probability the quantity
exceeds (or falls below) each of a set of thresholds. Works on any Monte Carlo
output: a workforce gap, an access metric, a shortfall count.
}
