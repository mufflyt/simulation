% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-forecast_scorecard.R
\name{forecast_scorecard}
\alias{forecast_scorecard}
\title{Forecast-evaluation scorecard for one model}
\usage{
forecast_scorecard(
  data,
  observed = "observed",
  point = "predicted",
  lower = "lower",
  upper = "upper",
  interval_level = 0.95,
  label = NA_character_
)
}
\arguments{
\item{data}{Data frame of forecasts, one row per (cutoff x target).}

\item{observed, point}{Column names for the observed value and point forecast.}

\item{lower, upper}{Column names for the interval bounds, or \code{NULL} to skip
interval metrics.}

\item{interval_level}{Central coverage of \code{[lower, upper]} (e.g. 0.95, so
\code{alpha = 0.05}). Used by the interval score.}

\item{label}{Optional model label carried into the result.}
}
\value{
A one-row data frame: \code{label}, \code{n}, \code{mape}, \code{rmse}, \code{signed_bias},
\code{signed_pct_bias}, and (when intervals given) \code{coverage}, \code{mean_width},
\code{mean_interval_score}, \code{calibration_slope}.
}
\description{
Computes the full suite of comparison metrics for a set of forecasts, so no
single number (least of all coverage) decides the winner: point error (MAPE,
RMSE), signed bias, and -- when interval columns are present -- empirical
coverage, mean interval width, the mean interval score (width-penalized), and
the calibration slope.
}
