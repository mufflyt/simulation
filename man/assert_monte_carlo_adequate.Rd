% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-monte_carlo.R
\name{assert_monte_carlo_adequate}
\alias{assert_monte_carlo_adequate}
\title{Refuse to report an interval the iteration count cannot support}
\usage{
assert_monte_carlo_adequate(
  n_iterations,
  ci = 0.95,
  mode = resolve_reproducibility_mode(),
  what = "supply"
)
}
\arguments{
\item{n_iterations}{Replicates actually run.}

\item{ci}{Interval width the caller intends to report.}

\item{mode}{Reproducibility mode; strict errors, relaxed warns.}

\item{what}{Label used in the message.}
}
\value{
(Invisibly) TRUE when the count supports the interval.
}
\description{
Fails closed when \code{n_iterations} is below \code{\link[=mc_min_iterations]{mc_min_iterations()}}. The bounds
still exist -- they are the sample extremes -- but they must not be published
as a \code{ci} interval, because they are narrower than the truth and get narrower
still as \code{n} falls.
}
\seealso{
Other monte carlo: 
\code{\link{mc_min_iterations}()},
\code{\link{monte_carlo_diagnostics}()},
\code{\link{monte_carlo_se}()}
}
\concept{monte carlo}
