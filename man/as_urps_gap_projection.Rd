% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting-urps_projection.R
\name{as_urps_gap_projection}
\alias{as_urps_gap_projection}
\title{Build a validated gap projection from supply and demand outputs}
\usage{
as_urps_gap_projection(
  supply,
  fte_gap,
  specialty = "FPMRS",
  geography_type = "national",
  geography_id = "US",
  scenario_col = "scenario",
  headcount_col = "headcount_median",
  fte_col = "effective_fte_median",
  cohort_basis = "undeclared",
  observed_share = NA_real_,
  mode = resolve_reproducibility_mode()
)
}
\arguments{
\item{supply}{Supply panel from \code{run_workforce_microsimulation()$supply}, or
a projection data frame from \code{\link[=as_urps_projection]{as_urps_projection()}}.}

\item{fte_gap}{Gap tibble from \code{\link[=compute_fte_gap]{compute_fte_gap()}}; needs \code{year},
\code{required_fte}, and optionally \code{supplied_fte}.}

\item{specialty, geography_type, geography_id}{Contract identifiers.}

\item{scenario_col}{Column in \code{supply} holding the scenario id.}

\item{headcount_col}{Column in \code{supply} holding median headcount.}

\item{fte_col}{Column in \code{supply} holding median clinical FTE.}

\item{cohort_basis}{What the supply cohort is, from
\code{\link[=cohort_provenance]{cohort_provenance()}}\verb{$source}. Only \code{"roster"} is a measured workforce.
Defaults to \code{"undeclared"}, which \code{\link[=validate_urps_gap_projection]{validate_urps_gap_projection()}} refuses
in strict mode: silence here is what let reconstructed supply export
looking measured.}

\item{observed_share}{Share of the cohort with an observed certification
year, from \code{\link[=cohort_provenance]{cohort_provenance()}}\verb{$observed_share}. Optional.}

\item{mode}{Reproducibility mode; passed to \code{\link[=validate_urps_gap_projection]{validate_urps_gap_projection()}}.}
}
\value{
Data frame conforming to the gap projection contract (REQUIRED_COLS +
optional \code{lower_95}, \code{upper_95}, \code{gap_pct}, \code{scenario_label}).
}
\description{
Joins the mufflyaccess supply contract columns with the demand FTE series
produced by \code{\link[=convert_workload_to_fte]{convert_workload_to_fte()}} / \code{\link[=compute_fte_gap]{compute_fte_gap()}}, computing
the explicit shortage/surplus on both FTE and headcount axes.
}
\details{
The headcount conversion for demand uses the ratio
\code{supply_headcount / supply_clinical_fte} from the supply side; this keeps
the two axes internally consistent without requiring a separate staffing
ratio assumption.
}
\seealso{
Other urps projection:
\code{\link[=gap_projections_all_scenarios]{gap_projections_all_scenarios()}},
\code{\link[=validate_urps_gap_projection]{validate_urps_gap_projection()}}
}
\concept{reporting}
\concept{urps projection}
