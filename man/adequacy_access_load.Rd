% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-access_response_bridge.R
\name{adequacy_access_load}
\alias{adequacy_access_load}
\title{Convert a base-year adequacy into a clear_access() load table}
\usage{
adequacy_access_load(
  adequacy,
  base_supply_fte,
  insurance_fraction = 1,
  catchment = NULL,
  status = "assumed_illustrative"
)
}
\arguments{
\item{adequacy}{Numeric, all \verb{> 0}. Base-year adequacy ratio(s); scalar for a
single national cell or a vector for catchment-level heterogeneity.}

\item{base_supply_fte}{Numeric, all \verb{> 0}. Base-year clinical FTE, used as the
accessible capacity in the same currency as demand.}

\item{insurance_fraction}{Share of capacity that accepts the patient, in 0 to
\enumerate{
\item Default 1.
}}

\item{catchment}{Optional id vector carried through to \code{\link[=clear_access]{clear_access()}}. When
\code{NULL}, ids are \code{"national"} (one cell) or \code{c1..cn}.}

\item{status}{Calibration status recorded as the \code{response_assumption}
attribute. Default "assumed_illustrative".}
}
\value{
A tibble with \code{catchment}, \code{demand_workload}, \code{accessible_capacity}
and \code{insurance_fraction}, ready for \code{\link[=clear_access]{clear_access()}}, carrying a
\code{response_assumption} attribute naming the mapping and its status.
}
\description{
Bridges the FTE-denominated base-year gap to the demand-vs-capacity currency
that \code{\link[=clear_access]{clear_access()}} consumes, so a proposed adequacy can be pushed through
the access queue. Demand is the required FTE from
\code{\link[=required_fte_base_year]{required_fte_base_year()}} (\code{required = base_supply_fte / adequacy}) and
capacity is \code{base_supply_fte}, so utilization is \code{rho = 1 / adequacy}.
}
\details{
Because \code{\link[=clear_access]{clear_access()}} depends on demand and capacity only through their
ratio, working in FTE units is exact and needs no wRVU conversion. The
mapping is the composition of two shipped functions, not a new assumption;
the only thing assumed is that the steady-state queue describes access, which
the returned table leaves labeled \code{assumed_illustrative}.

\code{adequacy < 1} yields \code{rho > 1}, which \code{\link[=clear_access]{clear_access()}} reports as a
saturated (infinite-wait) catchment. This is surfaced by a warning, not
hidden: a finite national wait under this queue requires a catchment-level
adequacy distribution with mass above 1, which the caller must supply (this
function does not invent the dispersion). \code{adequacy}, \code{base_supply_fte} and
\code{insurance_fraction} are recycled to a common length.
}
\seealso{
\code{\link[=required_fte_base_year]{required_fte_base_year()}} for the demand side, \code{\link[=clear_access]{clear_access()}} for
the queue it feeds, \code{\link[=simulate_access_for_adequacy]{simulate_access_for_adequacy()}} for the composed map.

Other access response bridge: 
\code{\link{simulate_access_for_adequacy}()}
}
\concept{access response bridge}
\concept{calibration}
