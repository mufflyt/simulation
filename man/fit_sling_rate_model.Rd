% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-utilization_models.R
\name{fit_sling_rate_model}
\alias{fit_sling_rate_model}
\title{Fit a surgery-rate model (Poisson with population-at-risk offset)}
\usage{
fit_sling_rate_model(
  cells,
  formula = n_slings ~ AgeGrp + Obesity + VaginalParity + Race,
  offset_col = "pop_at_risk",
  weights_col = NULL,
  overdispersion = TRUE
)
}
\arguments{
\item{cells}{Data frame of cells (e.g. age band x risk stratum) with the count,
the offset column, and predictors.}

\item{formula}{Model formula for the COUNT (no \code{offset()} term). Default
\code{n_slings ~ AgeGrp + Obesity + VaginalParity + Race}.}

\item{offset_col}{Name of the population-at-risk column (must be > 0).}

\item{weights_col}{Optional survey/frequency weights column.}

\item{overdispersion}{If TRUE (default) use quasipoisson; FALSE uses poisson.}
}
\value{
A \code{glm} object; \code{attr(., "hdmm_offset_col")} records the offset name.
}
\description{
Replaces the degenerate cases-only logistic. Expects aggregated cells with a
surgery count and the population at risk in each cell; the count is modelled
with a log link and \code{log(pop_at_risk)} as an offset, so coefficients are
log-rate ratios and per-person expected surgeries come from a \code{pop_at_risk} of
\enumerate{
\item The offset is passed via the glm argument (not the formula), so prediction
via \code{\link[=predict_count_with_uncertainty]{predict_count_with_uncertainty()}} never needs the offset column.
}
}
\seealso{
Other utilization models: 
\code{\link{fit_visit_model}()},
\code{\link{predict_count_with_uncertainty}()}
}
\concept{demand}
\concept{utilization models}
