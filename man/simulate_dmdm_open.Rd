% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-dynamic_open.R
\name{simulate_dmdm_open}
\alias{simulate_dmdm_open}
\title{Simulate an OPEN population through the pelvic-floor multistate model}
\usage{
simulate_dmdm_open(
  init,
  entrants = NULL,
  start_year,
  end_year,
  transitions = dmdm_default_transitions(),
  pop_by_age_year = NULL,
  conservation_tolerance = 0.01,
  allow_uncalibrated = FALSE
)
}
\arguments{
\item{init}{Data frame of agent-tracks present at \code{start_year} (all ages), with
\code{age}, \code{cumulative_vaginal_deliveries}, \code{years_since_last_vaginal_birth},
\code{bmi}, \code{hysterectomy}, \code{menopause_status}, \code{comorbidity}, \code{weight}, and the
marginal \code{p_ui}/\code{p_pop}/\code{p_ai}.}

\item{entrants}{Data frame of new entrants with the same columns plus
\code{entry_year} (added at the start of that year); or \code{NULL}.}

\item{start_year, end_year}{Simulation window (inclusive).}

\item{transitions}{Transition parameters; see \code{\link[=dmdm_default_transitions]{dmdm_default_transitions()}}.
Must be \code{"fitted"}/\code{"calibrated"} unless \code{allow_uncalibrated = TRUE}; see
\code{\link[=assert_calibrated_transitions]{assert_calibrated_transitions()}}.}

\item{pop_by_age_year}{Optional tibble of \code{year}, \code{age}, \code{population}. When
supplied, each year's age-specific weights are rescaled to it, so population
counts follow the Census projection while the model supplies the disease
rates. \code{NULL} (default) runs the internal cohort-component demography.}

\item{conservation_tolerance}{Largest share of a year's simulated population
that may sit at ages the projection does not carry, and so escape
reweighting. Exceeding it warns (strict: errors), because beyond that point
\code{population} and the weighted prevalences mix Census-anchored with
free-floating mass. Only consulted when \code{pop_by_age_year} is supplied.}

\item{allow_uncalibrated}{Declare an exploratory run on placeholder
transitions. Defaults to FALSE. Reweighting to a Census projection anchors
the population counts but does nothing for the disease rates, so an
uncalibrated open run is exactly as illustrative as a closed one.}
}
\value{
A data frame, one row per year: \code{year}, \code{population} (sum of weights),
\code{prev_ui}/\code{prev_pop}/\code{prev_ai} (weighted population prevalence) and
\code{inc_ui}/\code{inc_pop}/\code{inc_ai} (expected national new cases in the year). When
reweighting, also \code{share_unanchored}, with the full per-year audit on the
\code{population_audit} attribute (see \code{\link[=dmdm_population_audit]{dmdm_population_audit()}}).
}
\description{
Deterministic cohort-component engine. Each agent-track carries a survival
\code{weight} (national women represented) and the marginal probabilities
\code{p_ui}/\code{p_pop}/\code{p_ai}. Every year: entrants for that year are added, population
prevalence is recorded, then the marginal probabilities take a Markov step,
weights decay by mortality, and everyone ages.
}
\seealso{
Other dynamic open:
\code{\link[=dmdm_open_prevalence_trajectory]{dmdm_open_prevalence_trajectory()}},
\code{\link[=dmdm_population_audit]{dmdm_population_audit()}}
}
\concept{demand}
\concept{dynamic open}
