% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-backtest.R
\name{backtest_entrant_estimate}
\alias{backtest_entrant_estimate}
\title{Entrant rate estimated from pre-cutoff information only}
\usage{
backtest_entrant_estimate(
  through_year = BACKTEST_CUTOFF_YEAR,
  steady_from = 2018L,
  agents
)
}
\arguments{
\item{through_year}{Cutoff year.}

\item{steady_from}{First year of the steady-state window.}

\item{agents}{Cohort supplying the age structure for the departure estimate.}
}
\value{
List with \code{gross_entrants}, \code{departures}, \code{window}, \code{sd_entrants}.
}
\description{
The main model estimates this from 2018-2023, which would leak the entire
validation window. Here the window ends at the cutoff.
}
\details{
The steady-state window still starts at 2018: net growth averaged 86.5/yr
over 2014-2017 while the initial certification backlog cleared, which is not a
rate that could persist. That judgement uses only pre-cutoff information.

NO DEPARTURE ADD-BACK. \code{n_certified} is a flow of new certifications that
nothing is subtracted from, so its mean is already the gross entrant rate;
the previous \code{net + departures} added modelled departures to a series that
had never removed them. Correcting this LOWERS the pre-cutoff estimate from
60.7 to 32.7/yr, because the 2018-2020 window contains the COVID-collapsed
2020 cohort (n = 10). The old double-count happened to mask a genuinely
unrepresentative estimation window; the fix exposes it, which is the point of
a back-test. Neither quantity is re-tuned after seeing the 2023 value.
}
\seealso{
Other backtest: 
\code{\link{.series_through}()},
\code{\link{BACKTEST_PARAMETER_PROVENANCE}},
\code{\link{BACKTEST_TARGET_YEAR}},
\code{\link{assert_backtest_parameters_precede_cutoff}()},
\code{\link{assert_no_leakage}()},
\code{\link{backtest_attrition_requirement}()},
\code{\link{backtest_cohort_at}()},
\code{\link{backtest_cohorts_through}()},
\code{\link{backtest_loo_validation}()},
\code{\link{backtest_multi_window}()},
\code{\link{backtest_oos_interval}()},
\code{\link{backtest_retirement_regime}()},
\code{\link{backtest_rolling_origin}()},
\code{\link{backtest_target_candidates}()},
\code{\link{reset_leakage_audit}()},
\code{\link{validate_backtest_target}()}
}
\concept{backtest}
\concept{validation}
