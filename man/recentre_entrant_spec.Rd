% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-parameter_uncertainty.R
\name{recentre_entrant_spec}
\alias{recentre_entrant_spec}
\title{Re-centre a parameter spec on a new entrant level}
\usage{
recentre_entrant_spec(spec, entrant_mean)
}
\arguments{
\item{spec}{A \code{\link[=supply_parameter_spec]{supply_parameter_spec()}}, or NULL.}

\item{entrant_mean}{New point estimate.}
}
\value{
The spec with \code{entrant_mean} replaced; NULL passes through.
}
\description{
Keeps the observed series (and therefore the SPREAD) while moving the point
estimate the draw is centred on.
}
\details{
WHY THIS EXISTS. \code{run_supply_microsimulation()} lets \code{entrant_mean} take
precedence over \code{entrants_per_year}, so a single spec shared across scenarios
silently overrode every scenario's entrant level. The "Fellowship output
+10\%" and "-10\%" scenarios returned results identical to Baseline to the last
digit -- the entrant-policy lever, which is the most policy-relevant knob in
the model, did nothing at all. Re-centring per scenario keeps the scenario
definition authoritative for the LEVEL and the observed series authoritative
for the UNCERTAINTY.
}
\keyword{internal}
