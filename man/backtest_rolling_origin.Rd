% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-backtest.R
\name{backtest_rolling_origin}
\alias{backtest_rolling_origin}
\title{Rolling-origin validation: train only on outcomes known at the origin}
\usage{
backtest_rolling_origin(
  cutoffs = 2013:2020,
  horizon = 3L,
  predictor = "nrmp",
  min_train = 2L,
  conf = 0.95
)
}
\arguments{
\item{cutoffs}{Candidate windows.}

\item{horizon}{Projection length.}

\item{predictor}{Entrant predictor, as in \code{\link[=backtest_multi_window]{backtest_multi_window()}}.}

\item{min_train}{Minimum training windows; origins with fewer are excluded
rather than scored on a spread that is not estimable.}

\item{conf}{Interval level.}
}
\value{
Tibble, one row per eligible origin.
}
\description{
The distinction from leave-one-out is not pedantry, and it is the reason this
function exists separately.
}
\details{
A naive LOO estimates the error distribution for window \code{c} from every OTHER
window, including later ones. But a window with cutoff \verb{c'} has target
\verb{c' + horizon}, and its error is not OBSERVABLE until that target year. Using
it to bound a forecast made at origin \code{c} requires knowing an outcome that
had not happened yet. That is future leakage even though each individual
prediction respected its own cutoff.

Rolling origin admits a training window only when \code{target_year <= origin}, so
every error in the training set was measurable at the moment of forecast.
}
