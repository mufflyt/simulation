% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-first_entry_rate.R
\name{annual_first_urps_entry_rate}
\alias{annual_first_urps_entry_rate}
\title{Annual first-entry rate into URPS care}
\usage{
annual_first_urps_entry_rate(
  entrants,
  eligible_prevalent,
  conf_level = 0.95,
  numerator_source = NULL,
  denominator_source = NULL
)
}
\arguments{
\item{entrants}{Data frame with \code{condition}, \code{age_band}, \code{year}, \code{n} --
UNIQUE WOMEN with a first qualifying URPS entry, not visits.}

\item{eligible_prevalent}{Data frame with \code{condition}, \code{age_band}, \code{year},
\code{n} -- the current-year eligible prevalent stock.}

\item{conf_level}{Confidence level for the Wilson interval.}

\item{numerator_source, denominator_source}{Provenance strings. Required:
an estimate whose sources are unrecorded cannot be audited later.}
}
\value{
A tibble with one row per condition/age band/year carrying
\code{entrants_n}, \code{eligible_prevalent_n}, \code{rate}, \code{rate_lo}, \code{rate_hi}, and
both provenance columns.
}
\description{
Combines a claims-derived numerator with an externally-derived prevalent
denominator and returns \strong{both, separately}, alongside the rate.
}
\details{
NUMERATOR AND DENOMINATOR ARE RETAINED, NOT COLLAPSED. A rate alone hides
denominator-transport problems: an MA-APCD numerator over a national
prevalence denominator is a category error that a single number makes
invisible. Keeping \code{entrants_n} and \code{eligible_prevalent_n} beside the rate
forces the mismatch to be seen.

THE DENOMINATOR IS A CURRENT-YEAR STOCK. \code{eligible_prevalent_n} counts women
who meet the modelled prevalent-disease and eligibility definition \strong{in that
year, regardless of prior care history}. It is NOT a lifetime
ever-diagnosed cohort and NOT a never-treated cohort. It therefore mixes
never-treated women, previously-treated-but-still-symptomatic women, and
women with recurrent disease. If the external prevalence source removes
successfully treated asymptomatic women, that is correct and expected --
they are no longer in the current eligible prevalent stock.

NO DEFAULTS. Both inputs must be supplied. A default numerator or
denominator would let this function return a number when no data exists,
which is the failure mode the whole estimand contract exists to prevent.
}
\seealso{
Other first entry:
\code{\link[=first_entry_rate_status]{first_entry_rate_status()}}
}
\concept{demand}
\concept{first entry}
