% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-chia_casemix_duckdb.R
\name{chia_casemix_con}
\alias{chia_casemix_con}
\title{Open a read-only connection to the CHIA Case Mix DuckDB}
\usage{
chia_casemix_con(duckdb_path = .chia_duckdb_default())
}
\arguments{
\item{duckdb_path}{Path to the DuckDB. Default resolves via
\code{.chia_duckdb_default()} (env var \code{URPS_CHIA_DUCKDB}, else a glob for the
\code{MufflySamsung*} volume).}
}
\value{
An open \code{DBIConnection}.
}
\description{
Thin, fail-closed connector: verifies the file exists and the
\code{chia_casemix} schema is present before returning. The caller owns the
connection and must \code{DBI::dbDisconnect(con, shutdown = TRUE)} when done --
this is meant to be passed as \code{con} into the chia_casemix consumer
functions (e.g. \code{\link[=build_chia_inpatient_urps_series]{build_chia_inpatient_urps_series()}}), which may issue
more than one query per call.
}
\details{
NOTE: as of this writing the database holds only the RAW per-year tables
(\code{chia_casemix.hdd_discharge_2015}, etc.) and finalize_db.py's
\verb{v_*_all_years} unions. The \code{v_hdd_discharge_canonical} /
\code{v_ood_observation_canonical} views that \code{\link[=build_chia_inpatient_urps_series]{build_chia_inpatient_urps_series()}}
and related functions query are a separate, more involved layer (physician
attribution, era-column normalisation across the 2004-2018 schema drift,
procedure classification) that is not yet built -- see
\verb{scripts/chia/test_chia.py} for the intended shape. Querying those views
before that layer exists will fail with "view does not exist", not
silently return nothing.
}
\concept{chia inpatient surgery}
\concept{demand}
