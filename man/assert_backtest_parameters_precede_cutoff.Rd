% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-parameter_provenance.R
\name{assert_backtest_parameters_precede_cutoff}
\alias{assert_backtest_parameters_precede_cutoff}
\title{Fail if any published parameter in the back-test postdates the cutoff}
\usage{
assert_backtest_parameters_precede_cutoff(
  cutoff_year = BACKTEST_CUTOFF_YEAR,
  provenance = BACKTEST_PARAMETER_PROVENANCE
)
}
\arguments{
\item{cutoff_year}{The declared forecast origin.}

\item{provenance}{Provenance table; defaults to
\link{BACKTEST_PARAMETER_PROVENANCE}.}
}
\value{
Invisibly, the rows that were checked. Errors on any violation.
}
\description{
The parameter-side companion to \code{\link[=assert_no_leakage]{assert_no_leakage()}}. Checks only rows
flagged \code{in_primary_backtest}, because a parameter scored in a supplementary
sensitivity analysis is allowed to postdate the origin: that is what makes it
a sensitivity analysis rather than the primary result.
}
\details{
A SENSITIVITY ANALYSIS DOES NOT CURE LEAKAGE. If a post-cutoff parameter is
in the primary path, the primary result is not an out-of-time forecast, and
showing that the effect is small does not restore the claim. The remedy is to
remove the parameter from the primary analysis and score it separately, which
is why \code{in_primary_backtest} gates this check and the report names the
omitted rows rather than hiding them.
}
\examples{
assert_backtest_parameters_precede_cutoff(2020L)
}
\seealso{
Other backtest: 
\code{\link{.series_through}()},
\code{\link{BACKTEST_PARAMETER_PROVENANCE}},
\code{\link{BACKTEST_TARGET_YEAR}},
\code{\link{assert_no_leakage}()},
\code{\link{backtest_attrition_requirement}()},
\code{\link{backtest_cohort_at}()},
\code{\link{backtest_cohorts_through}()},
\code{\link{backtest_entrant_estimate}()},
\code{\link{backtest_loo_validation}()},
\code{\link{backtest_multi_window}()},
\code{\link{backtest_oos_interval}()},
\code{\link{backtest_retirement_regime}()},
\code{\link{backtest_rolling_origin}()},
\code{\link{backtest_target_candidates}()},
\code{\link{reset_leakage_audit}()},
\code{\link{validate_backtest_target}()}
}
\concept{backtest}
\concept{validation}
