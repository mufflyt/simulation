% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-isochrone_access_response.R
\name{join_lizeth_to_catchments}
\alias{join_lizeth_to_catchments}
\title{Attach fielded Lizeth waits to their provider-catchment by NPI}
\usage{
join_lizeth_to_catchments(
  lizeth_access,
  catchments,
  crosswalk = NULL,
  npi_col = "npi"
)
}
\arguments{
\item{lizeth_access}{Call-level records from \code{\link[=prepare_lizeth_access]{prepare_lizeth_access()}}, with at
least \code{npi} and \code{wait_business_days}.}

\item{catchments}{Provider-catchment table from
\code{\link[=e2sfca_catchments_from_access]{e2sfca_catchments_from_access()}}.}

\item{crosswalk}{Optional data frame with columns \code{npi} and \code{catchment} mapping
Lizeth NPIs to catchment ids. When \code{NULL} (default), \code{npi} is matched
directly against \code{catchment}.}

\item{npi_col}{Name of the NPI column in \code{lizeth_access}. Default \code{"npi"}.}
}
\value{
\code{lizeth_access} with added columns \code{catchment}, \code{accessible_capacity},
\code{demand_workload}, \code{adequacy_relative}, and \code{matched} (logical). The overall
match rate is attached as attribute \code{match_rate}.
}
\description{
Joins call-level Lizeth access records (from \code{\link[=prepare_lizeth_access]{prepare_lizeth_access()}}) to the
provider-catchment loads from \code{\link[=e2sfca_catchments_from_access]{e2sfca_catchments_from_access()}}, so each
observed wait carries the isochrone-derived access of the provider that was
actually called. This is the paired (local access, realized wait) dataset that
identifies the wait response and the decay parameter.
}
\details{
The canonical isochrone cohort is NPI-keyed, so by default the Lizeth \code{npi}
joins directly to the catchment id. When the isochrone layer used a non-NPI
provider id (e.g. \code{coord_id}), pass a \code{crosswalk} data frame mapping \code{npi} to
\code{catchment}. Calls whose NPI is missing or matches no catchment are retained
with \code{matched = FALSE} and \code{NA} access, so the match rate is auditable rather
than silently dropped.
}
\seealso{
Other isochrone access response: 
\code{\link{capacity_status_with_isochrone_response}()},
\code{\link{e2sfca_catchments_from_access}()},
\code{\link{fit_decay_sigma}()},
\code{\link{lizeth_wait_response_loss}()},
\code{\link{wait_response_region_holdout}()}
}
\concept{calibration}
\concept{isochrone access response}
