% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geography-isochrone_import.R
\name{verify_canonical_isochrones}
\alias{verify_canonical_isochrones}
\title{Verify the canonical isochrone set without throwing}
\usage{
verify_canonical_isochrones(
  dir = isochrone_source_dir(),
  verify_checksums = TRUE
)
}
\arguments{
\item{dir}{Source directory; defaults to \code{\link[=isochrone_source_dir]{isochrone_source_dir()}}.}

\item{verify_checksums}{Hash every band file (default TRUE).}
}
\value{
List with \code{ok}, \code{reason}, \code{dir}, \code{run_id}, \code{bands}, and per-file \code{files}.
}
\description{
Checks identity, not merely existence: the registry's \code{active_run_id}, the
band set, and the SHA-256 of every band file. Returns a report so a caller can
decide; \code{\link[=assert_canonical_isochrones]{assert_canonical_isochrones()}} is the fail-closed form.
}
\details{
\code{verify_checksums = FALSE} skips the ~1.6 GB hash, which takes minutes. Use it
only for a fast presence check; it cannot establish identity.
}
\seealso{
Other isochrone import: 
\code{\link{assert_canonical_isochrones}()},
\code{\link{isochrone_source_dir}()}
}
\concept{geography}
\concept{isochrone import}
