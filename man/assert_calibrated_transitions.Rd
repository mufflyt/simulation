% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-dynamic_multistate.R
\name{assert_calibrated_transitions}
\alias{assert_calibrated_transitions}
\title{Refuse to run the DMDM on uncalibrated transitions}
\usage{
assert_calibrated_transitions(
  transitions,
  allow_uncalibrated = FALSE,
  what = "DMDM transitions",
  mode = resolve_reproducibility_mode()
)
}
\arguments{
\item{transitions}{Transition parameters; see \code{\link[=dmdm_default_transitions]{dmdm_default_transitions()}}.}

\item{allow_uncalibrated}{Declare an exploratory run on placeholder inputs.}

\item{what}{Label naming the caller, used in the message.}

\item{mode}{Reproducibility mode; strict errors.}
}
\value{
(Invisibly) TRUE when the transitions are fit to report.
}
\description{
The onset and remission coefficients above are placeholders. A run built on
them produces a well-formed prevalence trajectory that means nothing, and the
output is numerically indistinguishable from a fitted run -- there is no shape
in the result that reveals the inputs were invented. Labelling alone has
therefore proven insufficient: \code{\link[=export_dmdm_demand_contract]{export_dmdm_demand_contract()}} propagates the
status into metadata, but the numbers still leave the function.
}
\details{
So the engine fails closed. Supply transitions whose \code{status} is \code{"fitted"} or
\code{"calibrated"}, or pass \code{allow_uncalibrated = TRUE} to declare an exploratory
run. This mirrors \code{\link[=assert_publishable_workload]{assert_publishable_workload()}} and
\code{\link[=assert_parameter_uncertainty]{assert_parameter_uncertainty()}}: strict mode stops, permissive mode warns
loudly, and the override is always an affirmative argument rather than a
default the caller can inherit without noticing.
}
\seealso{
Other dynamic multistate: 
\code{\link{dmdm_default_transitions}()},
\code{\link{dmdm_prevalence_trajectory}()},
\code{\link{simulate_dmdm}()}
}
\concept{demand}
\concept{dynamic multistate}
