% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_demand_contract.R
\name{export_hdmm_demand_contract}
\alias{export_hdmm_demand_contract}
\title{Export the HDMM life-course demand trajectory as contract tiers 5-6}
\usage{
export_hdmm_demand_contract(
  trajectory,
  output_directory,
  model_version = HDMM_DEMAND_CONTRACT_VERSION,
  base_year = 2025L,
  scenario = "baseline",
  calibration_status = "placeholder_uncalibrated",
  population_scope = "us_adult_women",
  verbose = TRUE
)
}
\arguments{
\item{trajectory}{Data frame with \code{year}, \code{care_seeking_national}, and
\code{service_units_national} (the \code{demand_summary} from
\code{\link[=lifecourse_demand_trajectory]{lifecourse_demand_trajectory()}}).}

\item{output_directory}{Directory to write into (created if missing).}

\item{model_version}{Version string stamped on every row and the manifest.
Defaults to \code{HDMM_DEMAND_CONTRACT_VERSION}.}

\item{base_year}{Calendar year the index is normalized to (= 100). Default 2025.}

\item{scenario}{Scenario label recorded in the manifest.}

\item{calibration_status}{Provenance guard; keep "placeholder_uncalibrated"
until the obstetric/urogynecologic transition equations are fitted.}

\item{population_scope}{Population denominator label. Default "us_adult_women".}

\item{verbose}{Log progress. Default TRUE.}
}
\value{
(invisibly) a list with \code{csv_path}, \code{manifest_path}, and the tidy \code{data}.
}
\description{
Formats a national life-course demand summary into the shared
demand-contract schema (the same columns as \code{\link[=export_dpmm_demand_contract]{export_dpmm_demand_contract()}}),
emitting \code{tier5_care_seeking} and \code{tier6_procedural} indexed to the base year,
plus a JSON provenance manifest.
}
\keyword{internal}
