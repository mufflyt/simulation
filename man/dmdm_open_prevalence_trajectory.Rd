% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-dynamic_open.R
\name{dmdm_open_prevalence_trajectory}
\alias{dmdm_open_prevalence_trajectory}
\title{Open-population prevalence + incidence trajectory}
\usage{
dmdm_open_prevalence_trajectory(
  pop_by_age_year,
  start_year,
  end_year,
  entry_age = 40L,
  n_init = 50000,
  n_entrants = 2000,
  seed = NULL,
  risk_params = lifecourse_risk_params(),
  transitions = dmdm_default_transitions(),
  reweight_to_projection = FALSE,
  allow_uncalibrated = FALSE
)
}
\arguments{
\item{pop_by_age_year}{Tibble with \code{year}, \code{age}, \code{population}.}

\item{start_year, end_year}{Simulation window.}

\item{entry_age}{Age at which new women enter. Default 40.}

\item{n_init, n_entrants}{Agent-track counts for the base year and each entrant
cohort.}

\item{seed, risk_params, transitions}{Passed through.}

\item{reweight_to_projection}{If TRUE, rescale each year's age-specific weights
to \code{pop_by_age_year} so population counts match the Census projection while
the model supplies the disease rates. Default FALSE (internal cohort-component
demography).}

\item{allow_uncalibrated}{Declare an exploratory run; passed through to
\code{\link[=simulate_dmdm_open]{simulate_dmdm_open()}}.}
}
\value{
The per-year data frame from \code{\link[=simulate_dmdm_open]{simulate_dmdm_open()}}.
}
\description{
Builds the base-year population (all ages >= \code{entry_age}) and one entrant
cohort at \code{entry_age} for each subsequent year, then runs \code{\link[=simulate_dmdm_open]{simulate_dmdm_open()}}.
Entrants are seeded from the cross-sectional risk at \code{entry_age} (low, since
most disease develops later) and then evolve dynamically.
}
