% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-practice_economics_sensitivity.R
\name{practice_economics_elasticity}
\alias{practice_economics_elasticity}
\title{Standardized elasticities for practice-economics assumptions}
\usage{
practice_economics_elasticity(
  practice_tbl,
  inputs = practice_economics_defaults(),
  draws = 1000L,
  seed = 20260821L,
  pct_perturbation = 0.1
)
}
\arguments{
\item{practice_tbl}{Baseline practice-tbl, as for
\code{\link[=simulate_practice_economics]{simulate_practice_economics()}}.}

\item{inputs}{Named list from \code{\link[=practice_economics_defaults]{practice_economics_defaults()}}.}

\item{draws}{Monte Carlo draws per scenario.}

\item{seed}{Reproducible random seed (same seed reused per scenario so
differences reflect the perturbation, not draw noise).}

\item{pct_perturbation}{Symmetric perturbation fraction for the \code{pct10}
scheme (default \code{0.10}).}
}
\value{
Tibble: \code{input_name}, \code{perturbation_type} (\code{"pct10"}/\code{"p25_p75"}),
\code{baseline_value}, \code{low_value}, \code{high_value}, \code{elasticity_low},
\code{elasticity_high}, \code{unstable_baseline}.
}
\description{
\code{\link[=practice_economics_sensitivity_decomposition]{practice_economics_sensitivity_decomposition()}} perturbs each assumption
family to a single, differently-sized "favorable" alternative -- useful
for ranking levers, not for comparing their sensitivity on a common
scale, since a bigger counterfactual shift on one input isn't the same
claim as a bigger underlying sensitivity. This function reports the
standard elasticity, \verb{(pct change in physician_compensation_capacity) / (pct change in the input)}, for the model's continuous, distributionally-
declared inputs only.
}
\details{
\code{payer_mix} is excluded: it is compositional (four shares summing to 1,
not a single scalar that moves by "+-10\%"). \code{annual_wrvu}/\code{app_fte} are
excluded too: they are \code{practice_tbl} fields with no uncertainty
distribution declared for them in this model -- see
\code{\link[=practice_economics_sensitivity_decomposition]{practice_economics_sensitivity_decomposition()}}'s \code{wrvu_productivity}/
\code{app_intensity} scenarios for those instead.

Two perturbation schemes, reported side by side:
\describe{
\item{pct10}{Every continuous input's central value (mode/median/mean)
moved +-\code{pct_perturbation}, holding its declared spread/shape fixed
-- symmetric and comparable across inputs.}
\item{p25_p75}{Only for \code{overhead}, \code{malpractice}, \code{app_compensation},
and \code{commercial_ratio} -- inputs with an uncertainty distribution
ALREADY declared in \code{inputs}. Uses the real p25/p75 of that exact
declared distribution (collapsed to a point at that value, the same
technique \code{\link[=practice_economics_sensitivity_decomposition]{practice_economics_sensitivity_decomposition()}} uses),
not an invented range. \code{NA} for the four collection rates, which
have no declared distribution at all.}
}

Elasticity on a near-zero or sign-changing baseline
\code{physician_compensation_capacity} is numerically unstable (a small
denominator inflates the ratio, and a perturbation that crosses zero
makes "percent change" discontinuous) -- \code{unstable_baseline} flags this
rather than reporting a number that looks precise but isn't.

\code{elasticity_low}/\code{elasticity_high} are not forced to be equal, even for
an input (like \code{overhead}) that enters the cost formula linearly: the
same \code{seed} is reused across scenarios so differences reflect the
perturbation rather than fresh draw noise, but pinning one input to a
fixed value changes how many random draws the OTHER stochastic inputs
consume from the same seeded stream, so a few-percent low/high asymmetry
on an otherwise-linear input is expected Monte Carlo artifact, not a
sign of real nonlinearity -- a large asymmetry (multiples, not a few
percent) is the signal worth investigating.
}
\concept{economics}
