% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-repro_provenance.R
\name{write_artifact_with_provenance}
\alias{write_artifact_with_provenance}
\title{Write a derived artifact with a provenance sidecar (atomic, fail-closed)}
\usage{
write_artifact_with_provenance(
  object,
  artifact_path,
  inputs = NULL,
  code_paths = character(0),
  run_id = make_run_id(),
  mode = resolve_reproducibility_mode(),
  source = NA_character_,
  extra = list()
)
}
\arguments{
\item{object}{R object to persist (saved with \code{\link[=saveRDS]{saveRDS()}}).}

\item{artifact_path}{Destination \code{.rds} path.}

\item{inputs}{Object(s) whose fingerprint gates staleness (list or object).}

\item{code_paths}{Source file(s) that produced the artifact.}

\item{run_id}{Run identifier (\code{\link[=make_run_id]{make_run_id()}}).}

\item{mode}{Reproducibility mode recorded in provenance.}

\item{source}{Free-text description of the data source.}

\item{extra}{Named list of additional provenance fields (e.g. tool versions).}
}
\value{
(Invisibly) the provenance list that was written.
}
\description{
Implements the isochrones Cache Provenance Contract (Non-Negotiable #18):
\itemize{
\item tmp-write then atomic rename of the payload,
\item a JSON sidecar written \emph{last} recording the input fingerprint, code
fingerprint, content SHA-256 of the payload, tool versions, a composite
key, and run provenance (run_id, run_mode, created_at, source, count).
}
}
\details{
The sidecar is written after (and separately from) the payload so a crash
mid-write leaves no sidecar -> the artifact is treated as absent on load.
}
