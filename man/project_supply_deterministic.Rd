% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12-provider_microsimulation.R
\name{project_supply_deterministic}
\alias{project_supply_deterministic}
\title{Deterministic age-structured projection (validation backbone)}
\usage{
project_supply_deterministic(
  agents,
  years,
  entrants_per_year,
  hazard_table = NULL,
  conversion_floor = 1,
  retirement_schedule = RETIREMENT_HAZARD_BY_AGE,
  fte_method = "hours",
  hours_model = NULL,
  hours_intercept = HWSM_HOURS_INTERCEPT,
  sex = "female"
)
}
\arguments{
\item{agents}{Starting agent tibble.}

\item{years}{Integer years to project.}

\item{entrants_per_year}{Annual entrants (BEFORE the conversion haircut).}

\item{hazard_table}{Optional age-band hazard table; when NULL the single-year
\code{\link[=departure_hazard]{departure_hazard()}} schedule is used, matching the stochastic engine.}

\item{conversion_floor}{Graduate-to-practice conversion. Previously ABSENT
from this function while the stochastic engine applied it, so the two
disagreed by 37\% at 2050 whenever conversion was not 1.0 -- read as model
disagreement when it was a missing argument.}

\item{retirement_schedule}{Single-year retirement hazard schedule.}

\item{fte_method}{FTE method (see \code{\link[=provider_clinical_fte]{provider_clinical_fte()}}).}

\item{hours_model}{Optional fitted hours model.}

\item{hours_intercept}{Intercept for the reference hours schedule.}

\item{sex}{Sex mix to evaluate the hazard and hours schedules against.}
}
\value{
Per-year tibble with expected headcount and effective FTE.
}
\description{
A faithful port of cliff::wc_project — expected departures (count * hazard)
rather than Bernoulli draws — used to validate the stochastic microsimulation
against its analytic expectation (they should agree to within a few percent;
cliff code/05 reports 4.51\% mean agreement).
}
