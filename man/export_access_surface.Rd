% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting-export_demand_contract.R
\name{export_access_surface}
\alias{export_access_surface}
\title{Export the tract-level drive-time access surface as a downstream contract}
\usage{
export_access_surface(
  access,
  output_directory,
  sigma_fit = NULL,
  capacity = NULL,
  isochrone_run_id = NA_character_,
  model_version = ACCESS_SURFACE_CONTRACT_VERSION,
  calibration_status = NULL,
  allow_unvalidated = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{access}{A \code{\link[=run_geographic_access]{run_geographic_access()}} result (resolved), a
\code{\link[=compute_e2sfca_access]{compute_e2sfca_access()}} result, or a per-tract access data frame with at
least \code{demand_id}, \code{access}, \code{population} (and optionally \code{access_scaled},
\code{n_providers}).}

\item{output_directory}{Directory to write into (created if missing).}

\item{sigma_fit}{Optional \code{\link[=fit_decay_sigma]{fit_decay_sigma()}} result; supplies \code{sigma} and
\code{wait_scale} (and, absent \code{calibration_status}, its status).}

\item{capacity}{Optional \code{\link[=capacity_status_with_isochrone_response]{capacity_status_with_isochrone_response()}} result;
supplies the object \code{calibration_status} (preferred over \code{sigma_fit}'s).}

\item{isochrone_run_id}{Provenance id of the isochrone run the surface was
built on (e.g. \code{ISOCHRONE_CANONICAL_RUN_ID}). Default \code{NA}.}

\item{model_version}{Version string stamped on every row + the manifest.
Default \code{ACCESS_SURFACE_CONTRACT_VERSION}.}

\item{calibration_status}{Override the provenance status. When \code{NULL} (default)
it is taken from \code{capacity}, then \code{sigma_fit}, then \code{"assumed_illustrative"}.}

\item{allow_unvalidated}{Emit a surface whose response is not geographically
validated. Default \code{FALSE}: the export is refused unless \code{calibration_status}
is \code{"fitted_and_geographically_validated"} (or \code{"calibrated"}), so an
un-transportable fit does not silently reach downstream as a contract.}

\item{verbose}{Log progress. Default \code{TRUE}.}
}
\value{
(invisibly) a list with \code{csv_path}, \code{manifest_path}, and the tidy \code{data}.
}
\description{
Ships the E2SFCA drive-time access surface (one row per demand
tract) as a versioned CSV + JSON manifest for downstream consumers (cliff's
Module D v2). The distance-decay \code{sigma} and wait-response \code{wait_scale} fitted
by the isochrone access-response pipeline are stamped on every row alongside
the \code{isochrone_run_id} and the \code{calibration_status}, so the consumer gates on
provenance exactly as it does for the demand contracts.
}
\concept{export access surface}
\concept{reporting}
