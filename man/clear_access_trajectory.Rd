% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geography-access_clearing.R
\name{clear_access_trajectory}
\alias{clear_access_trajectory}
\title{Clear access across a multi-year trajectory (optionally carrying backlog)}
\usage{
clear_access_trajectory(
  panel,
  carry_backlog = FALSE,
  backlog_fraction = 1,
  appointment_window = 30,
  wait_scale = 30,
  wait_ceiling = Inf,
  status = "assumed_illustrative"
)
}
\arguments{
\item{panel}{A data frame with \code{year} plus the per-catchment \code{\link[=clear_access]{clear_access()}}
inputs (\code{demand_workload}, \code{accessible_capacity}, and the optional
population/fte/insurance/travel columns). A \code{catchment} id is required when
\code{carry_backlog = TRUE}.}

\item{carry_backlog}{If \code{TRUE}, carry unmet demand forward (Phase-3 behaviour).
Default \code{FALSE} (each year cleared independently).}

\item{backlog_fraction}{Fraction of a year's unmet demand carried into the
next year. In [0, 1]. Default 1 (all unmet persists).}

\item{appointment_window, wait_scale, wait_ceiling, status}{Passed through to
\code{\link[=clear_access]{clear_access()}}.}
}
\value{
A tibble: every \code{\link[=clear_access]{clear_access()}} output column across all years, plus
\code{backlog_in} (demand carried in from the prior year) and
\code{demand_workload_base} (this year's demand before any backlog).
}
\description{
Applies \code{\link[=clear_access]{clear_access()}} to each year of a catchment x year panel in
ascending year order. With \code{carry_backlog = TRUE}, a fraction of each
catchment's unmet demand is added to that catchment's demand in the following
year (matched by \code{catchment}), so a persistent shortfall compounds instead of
vanishing at the year boundary.
}
