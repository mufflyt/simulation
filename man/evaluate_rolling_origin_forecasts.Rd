% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-rolling_origin_eval.R
\name{evaluate_rolling_origin_forecasts}
\alias{evaluate_rolling_origin_forecasts}
\title{Evaluate probabilistic forecasts with rolling origins}
\usage{
evaluate_rolling_origin_forecasts(
  observed_panel,
  forecast_fun,
  id_cols,
  model_cols = character(),
  year_col = "year",
  value_col = "observed",
  horizons = c(1L, 3L, 5L, 10L),
  first_origin = NULL,
  last_origin = NULL,
  min_train_years = 5L,
  n_draws = 2000L,
  interval_level = 0.95,
  log_score_method = c("kernel", "gaussian"),
  density_floor = 1e-12,
  seed = 20260821L,
  evidence_registry = NULL,
  save_path = NULL
)
}
\arguments{
\item{observed_panel}{Historical observed outcomes.}

\item{forecast_fun}{Function that fits and predicts from one origin.}

\item{id_cols}{Character vector identifying observed outcome series.}

\item{model_cols}{Character vector identifying competing forecast arms. These
columns are required only in predictive draws, not in observed outcomes.}

\item{year_col}{Name of integer year column.}

\item{value_col}{Name of observed outcome column.}

\item{horizons}{Positive forecast horizons in years.}

\item{first_origin}{Earliest training-set endpoint. Default uses the first
year allowing \code{min_train_years} observations.}

\item{last_origin}{Latest training-set endpoint. Default is the latest year
with observations at the longest requested horizon.}

\item{min_train_years}{Minimum number of distinct training years.}

\item{n_draws}{Number of predictive draws requested from \code{forecast_fun}.}

\item{interval_level}{Central predictive interval level.}

\item{log_score_method}{Either kernel-density or Gaussian log score.}

\item{density_floor}{Minimum predictive density used before taking logs.}

\item{seed}{Reproducibility seed.}

\item{evidence_registry}{Optional provenance and estimand registry.}

\item{save_path}{Optional path for an \code{.rds} evaluation bundle.}
}
\value{
A named list containing origin-level scores, horizon summaries,
forecast draws, a calibration test, and a dynamic summary sentence.
}
\description{
Fits a user-supplied forecasting function at successive historical origins
and scores 1-, 3-, 5-, and 10-year-ahead predictive draws. The forecast
function must accept \code{training_panel}, \code{forecast_years}, \code{id_cols},
\code{year_col}, \code{value_col}, \code{n_draws}, and \code{seed}. It must return one row per
predictive draw with the ID columns, forecast year, \code{draw}, and
\code{prediction}.
}
