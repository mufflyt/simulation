% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-prevalence_calibration.R
\name{calibrate_lifecourse_prevalence}
\alias{calibrate_lifecourse_prevalence}
\title{Recalibrate the placeholder intercept and age slope to published prevalence}
\usage{
calibrate_lifecourse_prevalence(
  cohort,
  risk_params = lifecourse_risk_params(),
  conditions = c("ui", "pop", "ai")
)
}
\arguments{
\item{cohort}{Person-level tibble from the life-course generator, carrying
\code{age} and the covariates the linear predictor uses.}

\item{risk_params}{Risk parameters to adjust, e.g. \code{\link[=lifecourse_risk_params]{lifecourse_risk_params()}}.}

\item{conditions}{Which limbs to recalibrate.}
}
\value{
A list with the adjusted \code{risk_params} and a \code{fit} tibble of
achieved-versus-target prevalence by band.
}
\description{
Recalibrate the placeholder intercept and age slope to published prevalence
}
\details{
Solves for \code{b0} and \code{bage} so the cohort's marginal prevalence by age band
matches \code{\link[=lifecourse_prevalence_targets]{lifecourse_prevalence_targets()}}. Two free parameters against five
targets, so the fit is over-determined and cannot absorb an arbitrary
shape -- a poor match means the covariate structure, not the intercept, is
wrong, and the residuals are returned so that is visible rather than hidden.
}
\seealso{
Other prevalence calibration: 
\code{\link{lifecourse_prevalence_targets}()},
\code{\link{lifecourse_risk_params_prevalence_anchored}()}
}
\concept{demand}
\concept{prevalence calibration}
