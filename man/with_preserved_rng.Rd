% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-repro_provenance.R
\name{with_preserved_rng}
\alias{with_preserved_rng}
\title{Evaluate an expression under a local seed, leaving the caller's stream intact}
\usage{
with_preserved_rng(seed, expr)
}
\arguments{
\item{seed}{Integer seed, or NULL to leave the stream alone.}

\item{expr}{Expression evaluated inside the scope (a promise, forced after
the seed is set and before the stream is restored).}
}
\value{
The value of \code{expr}.
}
\description{
\code{set.seed()} mutates GLOBAL state. A function that seeds internally for its
own reproducibility -- fold assignment, a PSA draw, a synthetic fallback --
silently reseeds the whole session as a side effect, so every draw after it
becomes a function of that seed rather than of the run's declared one. That
is the opposite of reproducibility: the run is deterministic, but not from
the seed anybody chose.
}
\details{
\code{\link[=seed_microsimulation]{seed_microsimulation()}} is the deliberate exception -- seeding the session
IS its purpose.
}
\seealso{
Other repro provenance: 
\code{\link{read_artifact_with_provenance}()},
\code{\link{resolve_reproducibility_mode}()},
\code{\link{seed_microsimulation}()},
\code{\link{write_artifact_with_provenance}()}
}
\concept{core}
\concept{repro provenance}
\keyword{internal}
