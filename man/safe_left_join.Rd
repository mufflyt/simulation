% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-canonical_and_joins.R
\name{safe_left_join}
\alias{safe_left_join}
\title{Left join that refuses silent row loss / fan-out}
\usage{
safe_left_join(
  x,
  y,
  by,
  allow_fanout = FALSE,
  min_match_rate = NULL,
  mode = resolve_reproducibility_mode()
)
}
\arguments{
\item{x, y}{Data frames.}

\item{by}{Join specification (as in \code{\link[dplyr:left_join]{dplyr::left_join()}}).}

\item{allow_fanout}{If FALSE (default), a non-unique key in \code{y} is an error.}

\item{min_match_rate}{Minimum share of left rows that must find a match; a
lower coverage warns (strict: errors). NULL disables the check.}

\item{mode}{Reproducibility mode governing warn-vs-stop on low coverage.}
}
\value{
The joined data frame.
}
\description{
A left join should preserve the left table's row count exactly. This wrapper
asserts keys are present, checks the right table has unique keys unless
\code{allow_fanout = TRUE}, performs the join, and fails if the result row count
differs from the left input (which can only happen via fan-out).
}
\details{
Three distinct failures, all of which a plain \code{\link[dplyr:left_join]{dplyr::left_join()}} performs
silently: a missing key column, a fan-out that duplicates left rows because
the right table's key is not unique, and a join that succeeds while matching
almost nothing.

Only the third is a judgement call, which is why \code{min_match_rate} is
explicit. Below it, the behaviour follows \code{mode}: a warning in \code{relaxed}, an
error in \code{strict}. A low match rate is usually a key-grain mismatch -- state
against state-year, ZIP against ZCTA -- rather than genuinely absent data.
}
\examples{
# A plain left_join silently drops rows that do not match. This one states a
# required match rate and refuses to return a quietly-truncated table.
safe_left_join(
  tibble::tibble(geo = c("CO", "NY"), fte = c(60, 140)),
  tibble::tibble(geo = c("CO", "NY"), population = c(2.9e6, 1.0e7)),
  by = "geo"
)
}
\seealso{
Other canonical and joins:
\code{\link[=resolve_canonical]{resolve_canonical()}}
}
\concept{canonical and joins}
\concept{core}
