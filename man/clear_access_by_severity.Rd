% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geography-access_severity.R
\name{clear_access_by_severity}
\alias{clear_access_by_severity}
\title{Clear demand against shared capacity, stratified by severity and priority}
\usage{
clear_access_by_severity(
  catchments,
  severity_windows = URPS_SEVERITY_WINDOWS,
  severity_col = "severity",
  priority = NULL,
  wait_scale = 30,
  wait_ceiling = Inf,
  status = "assumed_illustrative"
)
}
\arguments{
\item{catchments}{A LONG data frame: one row per catchment x severity, with a
\code{catchment} id, a severity column, \code{demand_workload} (that class's demand),
and \code{accessible_capacity} (the catchment's shared capacity, constant across
its severity rows). Optional \code{insurance_fraction} per row. Demand and
capacity must be finite and >= 0.}

\item{severity_windows}{Named positive numeric: appointment window per
severity class (names are the severity levels). Default
\code{URPS_SEVERITY_WINDOWS} (illustrative).}

\item{severity_col}{Name of the severity column. Default \code{"severity"}.}

\item{priority}{Severity levels ordered highest-priority (served first) to
lowest. Default: shortest window first (most urgent first).}

\item{wait_scale, wait_ceiling, status}{Passed through to \code{\link[=clear_access]{clear_access()}}.}
}
\value{
A long tibble: every \code{\link[=clear_access]{clear_access()}} output column, plus the severity
column and \code{appointment_window}, one row per catchment x severity.
}
\description{
For each catchment, capacity is offered to severity classes in \code{priority}
order (most urgent first): a class is cleared with \code{\link[=clear_access]{clear_access()}} against
the capacity remaining after higher-priority classes have taken theirs, using
that class's appointment window. Higher-severity demand therefore cannot be
displaced by lower-severity volume, and each class's \code{p_appointment} is judged
against its own clinically relevant deadline.
}
