% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-retirement_contract.R
\name{derive_provider_career_states}
\alias{derive_provider_career_states}
\title{Roll licence-level states up to a provider-level career state}
\usage{
derive_provider_career_states(license_state_tbl)
}
\arguments{
\item{license_state_tbl}{Licence-level provider-year states, as returned by
\code{\link[=derive_provider_year_states]{derive_provider_year_states()}} per licence, with \code{provider_id},
\code{license_id}, \code{year}, \code{activity_state} and optionally \code{qualifying}.}
}
\value{
One row per provider-year: \code{provider_id}, \code{year}, \code{career_state},
\code{career_reason}, \code{n_qualifying_licenses}, \code{n_active_licenses}.
}
\description{
A LAPSE IS AN EXIT FROM A LICENCE, NOT NECESSARILY FROM THE PROFESSION.
A physician licensed in Colorado and Wyoming whose Wyoming licence expires
has not left the workforce; they have left Wyoming's. Declaring a career
exit from a single licence event overstates attrition, and it does so
selectively -- multi-state physicians are the ones with the most licences to
lapse, so the bias concentrates in exactly the group least likely to have
actually retired.

The rule is therefore: \strong{a provider-level career exit requires that no
qualifying active licence remains.}

Precedence, applied per provider-year, and deliberately fail-closed against
OVERSTATING supply rather than against overstating exit:

\enumerate{
\item Any \code{DECEASED} licence together with any \code{ACTIVE} licence is a
\code{CONFLICT} -- death is a fact about a person, so it cannot coexist with
practice under another licence.
\item Any \code{DECEASED} licence makes the provider \code{DECEASED}; death applies
to the person, not the credential.
\item Any qualifying \code{ACTIVE} licence makes the provider \code{ACTIVE}.
\item Any \code{CONFLICT} makes the provider \code{CONFLICT}.
\item Any \code{UNKNOWN} licence leaves the provider \code{UNKNOWN}. Exit cannot be
asserted while some licence's status is unobserved -- that licence might
be the active one.
\item Only when every qualifying licence is \code{EXITED} is the provider
\code{EXITED}.
}

Non-qualifying licences (\code{qualifying = FALSE}) are excluded before any of
this: a licence outside the study's scope must not keep a provider in the
active workforce.
}
\seealso{
Other retirement contract:
\code{\link[=adjudicate_terminal_events]{adjudicate_terminal_events()}},
\code{\link[=build_retirement_artifact_manifest]{build_retirement_artifact_manifest()}},
\code{\link[=derive_provider_year_states]{derive_provider_year_states()}},
\code{\link[=persist_retirement_consensus_artifact]{persist_retirement_consensus_artifact()}},
\code{\link[=propagate_retirement_uncertainty_to_access]{propagate_retirement_uncertainty_to_access()}},
\code{\link[=retirement_confidence_dimensions]{retirement_confidence_dimensions()}},
\code{\link[=retirement_exit_taxonomy]{retirement_exit_taxonomy()}},
\code{\link[=retirement_strong_linkage_classes]{retirement_strong_linkage_classes()}},
\code{\link[=validate_retirement_evidence_v2]{validate_retirement_evidence_v2()}},
\code{\link[=validate_retirement_oos]{validate_retirement_oos()}}
}
\concept{retirement contract}
\concept{supply}
