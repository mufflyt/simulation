% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-swan_dmdm_panel.R
\name{build_swan_dmdm_panel}
\alias{build_swan_dmdm_panel}
\title{Build the SWAN person-year panel that the DMDM hazard fitter consumes}
\usage{
build_swan_dmdm_panel(
  swan_wide,
  visits = SWAN_UI_DEFAULT_VISITS,
  conditions = "ui",
  participant_id_column = "SWANID",
  verbose = TRUE
)
}
\arguments{
\item{swan_wide}{A wide SWAN data frame, one row per participant, with
visit-suffixed columns. \code{swan_all_visits.rds} is the intended input.}

\item{visits}{Integer visit numbers to include. Defaults to
\code{SWAN_UI_DEFAULT_VISITS} (0-6 and 10) -- every visit whose UI gate is
\code{INVOLEA}. Visits 7-9 use \code{LEKINVO}, which is not the same question and
flips the sign of the fitted age coefficient (see the module header);
requesting them warns. Pass \code{0:10} to reproduce the earlier, spurious fit.}

\item{conditions}{Conditions to emit state columns for. Defaults to \code{"ui"}
alone. \code{"pop"} is available but warns: fitting it on this data produces a
degenerate model (see the module header), so it is opt-in. \code{"ai"} is an
error because SWAN does not follow it.}

\item{participant_id_column}{Column naming the participant key.}

\item{verbose}{Narrate the reshape and the resulting panel shape.}
}
\value{
A tibble with \code{person_id}, \code{year}, the seven DMDM covariates, and
\code{has_ui} (plus \code{has_pop} when requested). Carries a \code{swan_dmdm_provenance}
attribute recording which covariates are measured, proxied or unmeasured.
}
\description{
Reshapes the wide SWAN frame into one row per participant-visit carrying the
exact columns \code{\link[=dmdm_transition_data]{dmdm_transition_data()}} requires, so a fitted transition set
can replace the placeholder coefficients in \code{\link[=dmdm_default_transitions]{dmdm_default_transitions()}}.
}
\details{
\code{year} is the SWAN visit number, not a calendar year. SWAN visits are nominally
annual and \code{\link[=dmdm_transition_data]{dmdm_transition_data()}} keeps only pairs where
\code{next_year == year + 1}, so using the visit index makes consecutive visits
exactly one step apart and lets the fitter drop real gaps (the 6-to-10 jump in
any visit set that omits 7-9) on its own rather than silently treating a
four-year interval as a one-year transition.
}
\seealso{
\code{\link[=swan_panel_fit_caveats]{swan_panel_fit_caveats()}}, \code{\link[=dmdm_transition_data]{dmdm_transition_data()}}

Other swan dmdm panel: 
\code{\link{swan_panel_fit_caveats}()}
}
\concept{data}
\concept{swan dmdm panel}
