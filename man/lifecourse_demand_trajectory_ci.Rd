% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-lifecourse_uncertainty.R
\name{lifecourse_demand_trajectory_ci}
\alias{lifecourse_demand_trajectory_ci}
\title{Life-course demand trajectory with parameter-uncertainty intervals}
\usage{
lifecourse_demand_trajectory_ci(
  pop_by_age_year,
  years = sort(unique(pop_by_age_year$year)),
  scenario = "baseline",
  n = 1e+05,
  n_draws = 200L,
  probs = c(0.025, 0.5, 0.975),
  seed = NULL,
  param_uncertainty = lifecourse_param_uncertainty(),
  risk_params = lifecourse_risk_params(),
  pathway_params = lifecourse_pathway_params(),
  pathway = NULL,
  ...
)
}
\arguments{
\item{pop_by_age_year}{Tibble with \code{year}, \code{age}, \code{population}.}

\item{years}{Years to run; default all years present.}

\item{scenario, n, ...}{Passed to \code{\link[=lifecourse_demand_trajectory]{lifecourse_demand_trajectory()}}.}

\item{n_draws}{Number of parameter draws.}

\item{probs}{Length-3 vector \code{c(lo, mid, hi)} of quantiles. Default 2.5/50/97.5.}

\item{seed}{Optional RNG seed (controls the whole draw sequence).}

\item{param_uncertainty}{Uncertainty spec from \code{\link[=lifecourse_param_uncertainty]{lifecourse_param_uncertainty()}}.}

\item{risk_params, pathway_params}{Point-estimate parameter tables to perturb.}

\item{pathway}{Optional condition-service pathway table. An EXPLICIT
formal rather than left to \code{...}: R partial-matches named arguments
against formals preceding \code{...}, so \verb{pathway =} was silently bound to
\code{pathway_params} -- a different object -- and the service pathway was
unreachable through this function by any spelling.}
}
\value{
Tibble \code{year}, and for care-seeking and service units the median plus
\verb{_lo}/\verb{_hi} columns.
}
\description{
Runs \code{\link[=lifecourse_demand_trajectory]{lifecourse_demand_trajectory()}} \code{n_draws} times, each with the risk
coefficients and care-pathway probabilities drawn from \code{param_uncertainty},
and returns the national demand summary with median and lower/upper prediction
bounds. The bounds combine parameter uncertainty with cohort sampling noise.
}
\seealso{
Other lifecourse uncertainty: 
\code{\link{assert_interval_nondegenerate}()},
\code{\link{lifecourse_param_uncertainty}()}
}
\concept{demand}
\concept{lifecourse uncertainty}
