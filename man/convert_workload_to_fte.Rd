% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-workload_to_fte.R
\name{convert_workload_to_fte}
\alias{convert_workload_to_fte}
\title{Convert a service-volume schedule to required provider FTE}
\usage{
convert_workload_to_fte(
  volumes,
  wrvu_per_fte = NULL,
  workload = urps_service_workload(),
  delegation = URPS_DELEGATION_MATRIX,
  provider_type = "urps",
  indirect_share = INDIRECT_TIME_SHARE,
  method = c("wrvu", "staffing", "time"),
  staffing_ratios = NULL,
  by_setting = FALSE
)
}
\arguments{
\item{volumes}{Tibble with \code{service}, \code{volume}, optionally \code{year}.}

\item{wrvu_per_fte}{Annual work RVUs per clinical FTE
(\code{\link[=calibrate_wrvu_per_fte]{calibrate_wrvu_per_fte()}}).}

\item{workload}{Service basket.}

\item{delegation}{Delegation matrix (NULL = attribute all volume to URPS).}

\item{provider_type}{Provider type to convert.}

\item{indirect_share}{Share of professional time that is indirect; required
FTE is grossed up by 1/(1 - indirect_share) so administration, documentation
and teaching are not silently free.}

\item{method}{One of "wrvu", "staffing", "time".}

\item{staffing_ratios}{For method = "staffing": tibble with \code{service} and
\code{volume_per_fte} (national volume divided by providers in that setting).}

\item{by_setting}{Logical. When \code{TRUE}, the returned tibble includes a
\code{setting} column and reports required FTE separately for each care delivery
setting (ambulatory, operative, indirect). Requires the \code{setting} column to
be present in \code{volumes} — attach it first with \code{\link[=volumes_with_setting]{volumes_with_setting()}}.
When \code{FALSE} (default), behaviour is unchanged: a single aggregate FTE is
returned.}
}
\value{
Tibble with \code{year} (if present), \code{required_fte}, and the method used.
When \code{by_setting = TRUE}, also includes a \code{setting} column.
}
\description{
Convert a service-volume schedule to required provider FTE
}
\details{
Service volumes become required FTE through a work-RVU basket, so supply and
demand end up in the same units and the difference between them is
meaningful. A service name absent from \code{workload} does not silently vanish:
the join reports the shortfall in match rate, warning in \code{relaxed} mode and
failing in \code{strict}. A dropped service understates demand invisibly, so a
publication run should not be made in \code{relaxed}.

WHAT THIS DOES NOT CHANGE. Because productivity is solved against a base-year
anchor -- \code{required_fte(t) = anchor * wRVU(t) / wRVU(base)} -- the workload
LEVELS and the delegation matrix cancel out of the projection. Changing them
moves the base year and the projection together. A 2.1x change in demand
calibration moved 2050 required FTE by 0.25\%. What does not cancel is the
SHAPE of demand growth, and the anchor itself.
}
\examples{
# Service volumes become REQUIRED FTE through a work-RVU basket, so supply
# and demand end up in the same units. Service names must exist in
# urps_service_workload(); an unmatched name is reported as a low match rate
# (a warning in relaxed mode, an error in strict), never dropped in silence.
volumes <- tibble::tibble(
  service = c("new_consultation", "return_visit", "cystoscopy", "prolapse_procedure"),
  volume  = c(120000, 240000, 45000, 32000)
)
convert_workload_to_fte(volumes, wrvu_per_fte = 7500)
}
\seealso{
Other workload to fte:
\code{\link{URPS_DELEGATION_MATRIX}},
\code{\link{URPS_SURGICAL_SPECIALTY_BENCHMARKS}},
\code{\link{WRVU_PER_FTE_BENCHMARK}},
\code{\link[=allocate_fte_by_setting]{allocate_fte_by_setting()}},
\code{\link[=apportion_service_volume]{apportion_service_volume()}},
\code{\link[=assert_publishable_workload]{assert_publishable_workload()}},
\code{\link[=build_provider_year_productivity_panel]{build_provider_year_productivity_panel()}},
\code{\link[=calibrate_wrvu_per_fte]{calibrate_wrvu_per_fte()}},
\code{\link[=calibration_status_report]{calibration_status_report()}},
\code{\link[=check_productivity_plausible]{check_productivity_plausible()}},
\code{\link[=compute_fte_gap]{compute_fte_gap()}},
\code{\link[=fit_provider_productivity_model]{fit_provider_productivity_model()}},
\code{\link[=implied_urps_share]{implied_urps_share()}},
\code{\link[=predict_provider_capacity]{predict_provider_capacity()}},
\code{\link[=predict_urps_clinical_hours]{predict_urps_clinical_hours()}},
\code{\link[=rescale_delegation_to_capacity]{rescale_delegation_to_capacity()}},
\code{\link[=save_provider_productivity_artifacts]{save_provider_productivity_artifacts()}},
\code{\link[=service_volume_to_wrvu]{service_volume_to_wrvu()}},
\code{\link[=urps_service_workload]{urps_service_workload()}},
\code{\link[=urps_service_workload_status]{urps_service_workload_status()}},
\code{\link[=validate_delegation_matrix]{validate_delegation_matrix()}}
}
\concept{supply}
\concept{workload to fte}
