% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-repro_provenance.R
\name{seed_microsimulation}
\alias{seed_microsimulation}
\title{Seed the RNG for a reproducible microsimulation run}
\usage{
seed_microsimulation(seed = NULL, mode = resolve_reproducibility_mode())
}
\arguments{
\item{seed}{Integer seed. If NULL, uses \code{MICROSIM_SEED} env var or 20260801.}

\item{mode}{Reproducibility mode (see \code{\link[=resolve_reproducibility_mode]{resolve_reproducibility_mode()}}).}
}
\value{
(Invisibly) the integer seed actually applied, or NA if none.
}
\description{
In \code{strict} mode we always seed (default 20260801, the study reference date)
so Monte-Carlo trajectories are bit-identical across machines. In \code{relaxed}
mode we honour an explicit seed if given but otherwise leave the RNG alone.
}
\details{
In \code{strict} mode the RNG is always seeded, so Monte Carlo trajectories are
bit-identical across machines and a reported figure can be reproduced exactly.
In \code{relaxed} mode an explicit seed is honoured but none is imposed, which
keeps repeated exploratory runs from looking more stable than they are.

Returns the seed actually applied, so a run can record it rather than leaving
reproducibility to a claim in a method section.
}
\seealso{
Other repro provenance: 
\code{\link{read_artifact_with_provenance}()},
\code{\link{resolve_reproducibility_mode}()},
\code{\link{with_preserved_rng}()},
\code{\link{write_artifact_with_provenance}()}
}
\concept{core}
\concept{repro provenance}
