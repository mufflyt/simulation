% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geography-telemedicine_reach.R
\name{telemedicine_reach}
\alias{telemedicine_reach}
\title{Apply the telemedicine geographic-reach uplift to nonmetro catchments}
\usage{
telemedicine_reach(
  catchments,
  nonmetro_uplift = 0.15,
  metro_col = "metro",
  nonmetro_value = "NonMetro"
)
}
\arguments{
\item{catchments}{A catchment table with `accessible_capacity` and a
metro/nonmetro column.}

\item{nonmetro_uplift}{Fractional increase applied to a nonmetro catchment's
\code{accessible_capacity} (e.g. 0.15 = +15\%). Non-negative scalar. Default 0.15.}

\item{metro_col}{Name of the metro/nonmetro column. Default \code{"metro"}.}

\item{nonmetro_value}{The value in \code{metro_col} marking a nonmetro catchment.
Default \code{"NonMetro"}.}
}
\value{
The catchment table (tibble) with \code{accessible_capacity} raised on
nonmetro rows and a logical \code{telemedicine_reach_applied} column. With
\code{nonmetro_uplift = 0} or no \code{metro_col}, capacity is returned unchanged.
}
\description{
Raises \code{accessible_capacity} for catchments flagged nonmetro by a labeled
factor, modelling telehealth extending specialist reach into rural areas. The
result is an ordinary catchment table for \code{\link[=clear_access]{clear_access()}} (and the
spatial-overflow clearing when present); under the uplift a nonmetro catchment
clears more of its demand (shorter wait, less unmet). Metro catchments are
untouched.
}
\details{
The uplift is ASSUMED/ILLUSTRATIVE -- a scenario lever, not a measured
telehealth-reach effect. Supply a cited rural tele-reach estimate before any
published number rests on it.
}
