% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geography-spatial_access_e2sfca.R
\name{run_geographic_access}
\alias{run_geographic_access}
\title{Run the geographic access layer end-to-end, or report why it cannot}
\usage{
run_geographic_access(
  membership = "data-raw/spatial/provider_isochrone_membership.rds",
  provider_supply = NULL,
  tract_demand = "data-raw/spatial/tract_fem65_centroids.csv",
  method = c("E2SFCA", "M2SFCA"),
  mode = resolve_reproducibility_mode()
)
}
\arguments{
\item{membership}{A long \code{(demand_id, provider_id, band)} table, or a path to
the \code{.rds} the import script writes.}

\item{provider_supply}{Tibble \code{(provider_id, supply)} keyed to match
\code{membership$provider_id}. \code{NULL} is unresolved: the national aggregate has
no per-provider identity to place.}

\item{tract_demand}{Tibble \code{(demand_id, population)}, or a path to the tract
centroid CSV (\code{GEOID} + \code{fem65}).}

\item{method}{"E2SFCA" (default) or "M2SFCA".}

\item{mode}{Reproducibility mode (reserved; the layer itself is deterministic).}
}
\value{
A list. Unresolved: \code{resolved = FALSE}, \code{reason}, and \code{NULL} surface.
Resolved: \code{resolved = TRUE}, \code{access} (per-tract), \code{national} (roll-up),
\code{method}, \code{n_providers}, \code{n_tracts}, \code{audit}, \code{membership_provenance}.
}
\description{
Run the geographic access layer end-to-end, or report why it cannot
}
\details{
The single entry point that turns the imported isochrone \code{membership} table,
per-provider supply, and per-tract demand into an E2SFCA access surface and
its national roll-up. This is the wiring \code{geographic_access_status()} names as
"called from the orchestrator, not merely loaded": \code{\link[=run_workforce_microsimulation]{run_workforce_microsimulation()}}
calls it and attaches the result.

It FAILS CLOSED. If the membership artifact, the per-provider supply, or the
tract demand is missing, it returns \code{resolved = FALSE} with the reason and
computes nothing. It never falls back to state-level geometry -- the ordering
trap that produces a plausible-but-meaningless access ratio, which
\code{geographic_access_status()} and \code{\link[=validation_report]{validation_report()}}'s geographic gate exist
to prevent. A run without the imported isochrones therefore \emph{reports the gap};
it does not fabricate a number. The membership table is produced by
\code{scripts/data_acquisition/12_build_provider_isochrone_membership.R}.
}
\concept{geography}
