% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_prevalence_to_incidence.R
\name{calibrate_onset_psa}
\alias{calibrate_onset_psa}
\title{Joint (remission, onset) calibration draws for PSA (DisMod/Bayesian style).}
\usage{
calibrate_onset_psa(
  target_by_band,
  bands,
  remission_prior,
  n_draws = 200L,
  ages = 18:100,
  knots = c(50, 70),
  p0 = 0,
  seed = 1L
)
}
\arguments{
\item{target_by_band, bands, ages, knots, p0}{as in \code{\link[=calibrate_onset_given_remission]{calibrate_onset_given_remission()}}.}

\item{remission_prior}{function(n) returning n remission draws (the external prior).}

\item{n_draws}{number of PSA draws.}
}
\value{
list: \code{draws} (data frame: remission, beta cols, worst, plausible),
\code{onset_draws} (matrix ages x n), \code{identifiability} (cor of remission vs onset@65).
}
\description{
Samples \code{remission} from its EXTERNAL prior and calibrates the onset conditional
on each draw, so the returned draws are the correlated (r, onset) pairs that PSA
must sample together. Also returns an identifiability read: how much the fitted
onset level moves with r (prevalence identifies the pair, not each alone).
}
\seealso{
Other prevalence-calibration: 
\code{\link{calibrate_onset_given_remission}()}
}
\concept{prevalence-calibration}
