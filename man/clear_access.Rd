% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geography-access_clearing.R
\name{clear_access}
\alias{clear_access}
\title{Clear demand against accessible capacity into patient-experienced outcomes}
\usage{
clear_access(
  catchments,
  appointment_window = 30,
  wait_scale = 30,
  wait_ceiling = Inf,
  status = "assumed_illustrative"
)
}
\arguments{
\item{catchments}{A data frame with numeric \code{demand_workload} and
\code{accessible_capacity} in the SAME currency (e.g. annual wRVU-equivalent),
and optional \code{accessible_population}, \code{accessible_fte},
\code{median_travel_time}, and \code{insurance_fraction} (in \code{[0, 1]}, default 1). Any
id columns (e.g. \code{catchment}, \code{year}) are carried through untouched. \code{NA}
demand or capacity marks an empty/unknown catchment and yields \code{NA}
outcomes (not an error).}

\item{appointment_window}{Window \code{W} within which an appointment "counts",
in the same time unit as the wait (e.g. days). Positive scalar. Default 30.}

\item{wait_scale}{Proportionality constant \code{k} in \code{wait = k * rho/(1-rho)},
same time unit as \code{appointment_window}. A calibration knob fit to observed
waits (see \code{\link[=access_validation_targets]{access_validation_targets()}}). Positive scalar. Default 30
(labeled illustrative).}

\item{wait_ceiling}{Value reported for \code{wait_time} when \code{rho >= 1}. Default
\code{Inf}; set finite to saturate. Rows at the ceiling are flagged by
\code{wait_censored}.}

\item{status}{Calibration status stamped on every row. Default
"assumed_illustrative" -- fit \code{wait_scale} before publishing.}
}
\value{
A tibble: the input columns, plus \code{utilization}, \code{served},
\code{unmet_demand}, \code{wait_time}, \code{wait_censored}, \code{p_appointment},
\code{panel_size}, \code{median_travel_time}, \code{calibration_status}.
}
\description{
For each catchment, utilization is \code{rho = demand / capacity}. Unmet demand is
\code{max(0, demand - capacity)}. Wait time follows a labeled, monotone
heavy-traffic mapping \code{wait = wait_scale * rho / (1 - rho)} for \code{rho < 1}, and
is reported as censored (at \code{wait_ceiling}) once the queue is unbounded
(\code{rho >= 1}) -- never \code{NaN}, never negative. The probability of obtaining an
appointment within \code{appointment_window} uses an exponential-delay
approximation \code{P(wait <= W) = 1 - exp(-W / wait)}, multiplied by the share of
capacity that will accept the patient (\code{insurance_fraction}). Panel size is
\code{accessible_population / accessible_fte}.
}
