% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-urps_population.R
\name{brfss_pfd_prevalence_for_demand_bands}
\alias{brfss_pfd_prevalence_for_demand_bands}
\title{Aggregate BRFSS cell UI prevalence to DEMAND_AGE_BANDS}
\usage{
brfss_pfd_prevalence_for_demand_bands(
  cells,
  condition = c("ui", "pop", "fi", "any_pfd")
)
}
\arguments{
\item{cells}{Population cell table from \code{\link[=build_urps_population_cells]{build_urps_population_cells()}}.}

\item{condition}{One of \code{"ui"} (default), \code{"pop"}, \code{"fi"}, or \code{"any_pfd"}
(unweighted sum of the three rates, capped at 1).}
}
\value{
Named numeric vector over \code{DEMAND_AGE_BANDS} (same structure as
\code{pfd_prevalence_by_band()}).
}
\description{
Computes a survey-weight-averaged UI prevalence for each of the five
\code{DEMAND_AGE_BANDS} used by \code{\link[=compute_demand_denominators]{compute_demand_denominators()}}, using the
approximate crosswalk from \code{URPS_POP_AGE_BANDS}. The result slots directly
into the \code{pfd_prevalence} argument of \code{compute_demand_denominators()} so
BRFSS-derived prevalence can replace the Nygaard 2008 constants without
restructuring the demand pipeline.
}
