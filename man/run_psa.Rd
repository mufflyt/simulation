% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-psa.R
\name{run_psa}
\alias{run_psa}
\title{Run a probabilistic sensitivity analysis}
\usage{
run_psa(inputs, evaluate, n = 500, seed = 20260801L, verbose = TRUE)
}
\arguments{
\item{inputs}{List of \code{psa_input()} specs.}

\item{evaluate}{Function taking a named list of input values (discrete inputs
passed as their mapped value) and returning a numeric scalar, or a named
numeric vector of several outputs.}

\item{n}{Number of Monte-Carlo draws.}

\item{seed}{RNG seed.}

\item{verbose}{Logical progress.}
}
\value{
List: \code{draws} (tibble of inputs + outputs), \code{inputs}, \code{n}, \code{seed},
\code{n_failed} (evaluations that errored -> NA).
}
\description{
Samples the inputs jointly (LHS) and evaluates \code{evaluate} once per draw.
}
\details{
Probabilistic sensitivity analysis over declared input distributions. Each of
\code{n} iterations draws every input and calls \code{evaluate}, which must return a
named numeric vector; the names become \code{output_names}.

This answers a different question from the Monte Carlo interval on a
projection. The projection's band says how uncertain the answer is; PSA, via
\code{\link[=psa_prcc]{psa_prcc()}}, says WHICH input is responsible, which is what tells you where
another measurement would actually help.
}
\examples{
# Probabilistic sensitivity analysis over declared input distributions.
psa <- run_psa(
  inputs   = list(psa_normal("a", 10, 2), psa_uniform("b", 1, 3)),
  evaluate = function(a, b) c(y = a * b),
  n        = 60,
  verbose  = FALSE
)
psa$output_names
}
\seealso{
Other psa:
\code{\link[=plot_psa_tornado]{plot_psa_tornado()}},
\code{\link[=psa_discrete]{psa_discrete()}},
\code{\link[=psa_normal]{psa_normal()}},
\code{\link[=psa_prcc]{psa_prcc()}},
\code{\link[=psa_sample]{psa_sample()}},
\code{\link[=psa_srrc]{psa_srrc()}},
\code{\link[=psa_tornado]{psa_tornado()}},
\code{\link[=psa_triangular]{psa_triangular()}},
\code{\link[=psa_uniform]{psa_uniform()}}
}
\concept{calibration}
\concept{psa}
