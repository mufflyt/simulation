% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/25b-demand_transition_registry.R
\name{assert_publishable_demand_coefficients}
\alias{assert_publishable_demand_coefficients}
\title{Assert the demand-transition coefficients are publishable}
\usage{
assert_publishable_demand_coefficients(
  variant = c("default", "cited"),
  allow_analogy = FALSE,
  mode = resolve_reproducibility_mode()
)
}
\arguments{
\item{variant}{Which coefficient set to check: "default" or "cited".}

\item{allow_analogy}{Permit \code{derived_by_analogy} coefficients.}

\item{mode}{Reproducibility mode; strict errors, relaxed warns.}
}
\value{
(Invisibly) \code{TRUE} when publishable, \code{FALSE} otherwise.
}
\description{
The coefficient analogue of \code{\link[=assert_publishable_workload]{assert_publishable_workload()}}, applied to the
demand-transition registry. It reduces the registry (for the chosen \code{variant})
to its LEAST-calibrated tier and runs the same accept / opt-in / refuse gate:
\code{calibrated}/\code{solved} pass, \code{derived_by_analogy} needs \code{allow_analogy = TRUE},
and \code{uncalibrated_illustrative} placeholders are refused (strict errors,
relaxed warns). Because the default variant is all placeholders -- and even the
cited variant leaves the care-pathway probabilities as placeholders -- this
surfaces the tier-mixing the bespoke \code{status} string hid.
}
\details{
This is a standalone gate; it is intentionally NOT auto-wired into
\code{\link[=simulate_lifecourse_demand]{simulate_lifecourse_demand()}}.
}
