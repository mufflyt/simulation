% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-prevalence_to_incidence.R
\name{prevalence_observation}
\alias{prevalence_observation}
\title{Build a prevalence observation with a usable standard error (DisMod style).}
\usage{
prevalence_observation(
  band,
  age_start,
  age_end,
  prevalence,
  ci_lower = NA_real_,
  ci_upper = NA_real_,
  se = NA_real_,
  n = NA_real_,
  conf = 0.95
)
}
\arguments{
\item{band}{Label for the age band.}

\item{age_start, age_end}{Inclusive integer ages of the interval.}

\item{prevalence}{observed prevalence in (0,1).}

\item{ci_lower, ci_upper}{reported CI bounds (optional).}

\item{se}{reported standard error (optional, takes precedence).}

\item{n}{sample size for a binomial SE (optional, last resort).}

\item{conf}{CI confidence level (default 0.95).}
}
\value{
one-row data frame: band, age_start, age_end, prevalence, se.
}
\description{
Standardizes a reported age-band prevalence to (age_start, age_end, prevalence,
se). If only a confidence interval is given, the SE is derived from it
(se = (ci_upper - ci_lower) / (2 * z)); if only a sample size is given, the
binomial SE is used. Failing loudly when neither uncertainty source is present
keeps an "exact" observation from silently dominating the likelihood.
}
\seealso{
Other prevalence-calibration: 
\code{\link{fit_prevalence_consistent_psa}()},
\code{\link{fit_prevalence_consistent_transitions}()},
\code{\link{prevalence_from_incidence}()}
}
\concept{prevalence-calibration}
