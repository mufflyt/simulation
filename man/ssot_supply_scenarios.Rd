% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-ssot.R
\name{ssot_supply_scenarios}
\alias{ssot_supply_scenarios}
\title{Supply scenarios from the mufflyaccess registry}
\usage{
ssot_supply_scenarios(baseline_entrants = 55, ids = NULL)
}
\arguments{
\item{baseline_entrants}{Annual entrants under the baseline scenario.}

\item{ids}{Scenario ids to include; defaults to the whole registry.}
}
\value{
Named list of scenario definitions.
}
\description{
Adopts \code{mufflyaccess::urps_scenarios()} as the single source of truth and
translates it into the shape this package's engine consumes. The registry
carries \code{entrant_multiplier}, \code{retirement_shift_years},
\code{late_career_fte_factor}, \code{late_career_fte_onset_age} and (from registry
v1.3.0) \code{career_change_multiplier} (the burnout / early-exit lever).
}
\details{
Two mapping notes:
\itemize{
\item the registry expresses entrants as a MULTIPLIER on the modelled rate, so a
baseline entrant count must be supplied to turn it into a count;
\item \code{late_career_fte_factor} is applied only from \code{late_career_fte_onset_age},
which is more specific than a uniform hours multiplier and is honoured as
such by \code{\link[=simulate_provider_career_once]{simulate_provider_career_once()}}.
}
}
\keyword{internal}
