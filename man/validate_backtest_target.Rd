% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-backtest.R
\name{validate_backtest_target}
\alias{validate_backtest_target}
\title{Validate the back-test target against the simulated cohort's contract}
\usage{
validate_backtest_target(
  target_year = BACKTEST_TARGET_YEAR,
  geography = "national",
  board_pathway = "ABOG_PLUS_ABU",
  measure = "board_certified_active",
  acknowledge_no_attrition = FALSE,
  expected_value = NULL
)
}
\arguments{
\item{target_year}{Year being scored.}

\item{geography, board_pathway, measure}{Dimensions the cohort was built on.}

\item{acknowledge_no_attrition}{Proceed despite the observed series applying
no attrition. Required, because the model does apply it.}

\item{expected_value}{The count the caller believes it is scoring against.
Supplying it turns a silently-different-but-internally-consistent target
into an error: asking for the ABOG-only pathway returns 1,027, which is a
perfectly valid count and the wrong one for an ABOG+ABU cohort.}
}
\value{
List with the validated \code{value} and the reconciliation record.
}
\description{
Fails closed on every dimension that could make the comparison invalid. The
attrition mismatch is separated out because it does not invalidate the target
choice -- it changes what the comparison means.
}
\details{
Establishes what the back-test is scoring against before any arm runs:
geography, board pathway, measure, and whether the observed series has
attrition applied.

\code{acknowledge_no_attrition} must be passed explicitly when the target series
is a gross flow. The certification series never nets departures out, so an
arm that also subtracts departures is scored against a target that does not,
and the mismatch shows up as apparent model error rather than as the
definition difference it actually is.
}
\seealso{
Other backtest: 
\code{\link{.series_through}()},
\code{\link{BACKTEST_PARAMETER_PROVENANCE}},
\code{\link{BACKTEST_TARGET_YEAR}},
\code{\link{assert_backtest_parameters_precede_cutoff}()},
\code{\link{assert_no_leakage}()},
\code{\link{backtest_attrition_requirement}()},
\code{\link{backtest_cohort_at}()},
\code{\link{backtest_cohorts_through}()},
\code{\link{backtest_entrant_estimate}()},
\code{\link{backtest_loo_validation}()},
\code{\link{backtest_multi_window}()},
\code{\link{backtest_oos_interval}()},
\code{\link{backtest_retirement_regime}()},
\code{\link{backtest_rolling_origin}()},
\code{\link{backtest_target_candidates}()},
\code{\link{reset_leakage_audit}()}
}
\concept{backtest}
\concept{validation}
