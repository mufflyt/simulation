% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-prevalence_to_incidence.R
\name{fit_prevalence_consistent_psa}
\alias{fit_prevalence_consistent_psa}
\title{Joint (remission, onset) posterior draws for PSA (DisMod / Bayesian style).}
\usage{
fit_prevalence_consistent_psa(
  observations,
  remission_prior,
  n_draws = 200L,
  ages = 18:100,
  knots = c(60, 70),
  p0 = 0,
  age_weights = NULL,
  smooth_sd = 1,
  report_age = 65,
  seed = 1L
)
}
\arguments{
\item{observations, ages, knots, p0, age_weights, smooth_sd}{as in
\code{\link[=fit_prevalence_consistent_transitions]{fit_prevalence_consistent_transitions()}}.}

\item{remission_prior}{function(n) returning n external remission draws.}

\item{n_draws}{number of PSA draws.}

\item{report_age}{age at which to read the onset level for the identifiability
correlation (default 65).}

\item{seed}{RNG seed for the draws, so a reported identifiability correlation
is reproducible.}
}
\value{
list: \code{draws} (data frame: remission, onset_at_report_age, worst_z,
compatible, plausible), \code{incidence_draws} (matrix ages x n_draws),
\code{identifiability} (cor of remission vs onset@report_age).
}
\description{
Samples remission from its EXTERNAL prior and MAP-fits the onset conditional on
each draw, returning the correlated (r, onset) pairs PSA must sample together.
Prevalence identifies the PAIR, not each rate alone: to hold the same prevalence
a higher remission must be met by a higher onset, so the fitted onset level is
strongly positively correlated with r across the prior (the identifiability read).
}
\seealso{
Other prevalence-calibration:
\code{\link[=fit_prevalence_consistent_transitions]{fit_prevalence_consistent_transitions()}},
\code{\link[=prevalence_from_incidence]{prevalence_from_incidence()}},
\code{\link[=prevalence_observation]{prevalence_observation()}}
}
\concept{prevalence-calibration}
