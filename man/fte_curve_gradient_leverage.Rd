% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-practice_survey.R
\name{fte_curve_gradient_leverage}
\alias{fte_curve_gradient_leverage}
\title{Leverage of the borrowed hours curve's age gradient}
\usage{
fte_curve_gradient_leverage(
  agents,
  horizon_years = 25L,
  gradient_scale = c(0, 0.5, 1, 1.5),
  fte_hours = URPS_FTE_CLINICAL_HOURS_PER_WEEK
)
}
\arguments{
\item{agents}{Cohort with \code{age} and \code{sex}.}

\item{horizon_years}{Years to age the cohort by.}

\item{gradient_scale}{Multipliers on the published age gradient.}

\item{fte_hours}{Clinical hours defining 1.0 FTE.}
}
\value{
Tibble of \code{gradient_scale}, \code{fte_per_head_base},
\code{fte_per_head_horizon}, \code{drift_pct}, \code{fte_delta_per_1000_head}.
}
\description{
Scales the published HWSM age gradient by \code{gradient_scale} and reports mean
clinical FTE per head after ageing the cohort \code{horizon_years}.
\code{gradient_scale = 0} is a flat curve (hours independent of age); \code{1} is the
published gradient; \verb{>1} steepens it.
}
\details{
The intercept is re-solved for every scale, so each row is normalised to
1.000 FTE per head in the base year and the rows differ ONLY in shape. That
is the point: the level is not what is borrowed in a way that matters.
}
\seealso{
Other practice survey: 
\code{\link{fte_curve_status}()},
\code{\link{geographic_access_status}()},
\code{\link{unresolved_calibration_items}()},
\code{\link{urps_practice_survey_requirements}()}
}
\concept{data}
\concept{practice survey}
