% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-legacy_loader.R
\name{load_legacy}
\alias{load_legacy}
\title{Source the legacy scripts in a declared order, reporting every collision}
\usage{
load_legacy(
  dir = legacy_dir(),
  envir = parent.frame(),
  files = LEGACY_LOAD_ORDER,
  functions_only = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{dir}{Directory holding the scripts.}

\item{envir}{Environment to source into.}

\item{files}{Subset of \link{LEGACY_LOAD_ORDER} to load.}

\item{functions_only}{Evaluate only definitions (default). Set FALSE to run
the scripts as scripts, which requires the external data to be present.}

\item{quiet}{Suppress the collision report.}
}
\value{
(Invisibly) a tibble of shadowed names and the file that won.
}
\description{
By default only DEFINITIONS are evaluated. The legacy scripts interleave
function definitions with analysis code that reads data files at top level --
\code{workforce.R} alone has ~196 top-level statements, several of them
\code{load()}/\code{read_rds()} calls. Sourcing them wholesale would try to read a
filesystem that may not exist and would run analyses nobody asked for, so the
loader evaluates the function definitions and skips the rest.
}
\seealso{
Other legacy loader:
\code{\link{LEGACY_CANONICAL}},
\code{\link[=check_legacy_canonical]{check_legacy_canonical()}},
\code{\link[=legacy_collisions]{legacy_collisions()}}
}
\concept{core}
\concept{legacy loader}
