% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-sources.R
\name{nrmp_track_split}
\alias{nrmp_track_split}
\title{NRMP matched positions split into the OB/GYN and urology tracks}
\usage{
nrmp_track_split(available_by = NULL, track = NULL, exact_only = FALSE)
}
\arguments{
\item{available_by}{Keep only reports published by this year. Each report
appears in its own appointment year.}

\item{track}{Optional filter, \code{"obgyn"} or \code{"urology"}.}

\item{exact_only}{Keep only years that reproduce Table 1 exactly.}
}
\value{
Tibble of \code{appointment_year}, \code{track}, \code{n_programs},
\code{positions_offered}, \code{positions_filled}, \code{residual_offered},
\code{residual_filled}, \code{reconciles_exactly}, \code{urology_share}.
}
\description{
Reconstructed from the per-program listings, which carry the NRMP specialty
code that the aggregate table collapses. Carries the years whose
reconstruction lands within 2 of Table 1, each stamped with its own residual;
\code{reconciles_exactly} marks the four that reproduce it precisely. A residual is
an unrecovered program of unknown track, so treat the split as accurate to
about 3 percentage points where it is non-zero.
}
\examples{
nrmp_track_split(track = "urology")[, c("appointment_year", "positions_filled")]
}
\seealso{
Other sources:
\code{\link[=nrmp_entrant_series]{nrmp_entrant_series()}},
\code{\link[=nrmp_entrants]{nrmp_entrants()}},
\code{\link[=urps_age_productivity_curve]{urps_age_productivity_curve()}},
\code{\link[=urps_empirical_hazard_by_ageband]{urps_empirical_hazard_by_ageband()}},
\code{\link[=urps_empirical_retirement_schedule]{urps_empirical_retirement_schedule()}},
\code{\link[=urps_hazard_exposure]{urps_hazard_exposure()}}
}
\concept{calibration}
\concept{sources}
