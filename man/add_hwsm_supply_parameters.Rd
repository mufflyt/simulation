% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-hwsm_parameters.R
\name{add_hwsm_supply_parameters}
\alias{add_hwsm_supply_parameters}
\title{Add HRSA HWSM work-effort and retirement parameters to a provider roster}
\usage{
add_hwsm_supply_parameters(
  provider_tbl,
  age_col = "age",
  sex_col = "sex",
  save_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{provider_tbl}{A data frame with one row per physician.}

\item{age_col}{Name of the integer age column. Default "age".}

\item{sex_col}{Name of the sex column (Female/F, Male/M). Default "sex".}

\item{save_dir}{Optional directory in which to write the augmented roster as a
timestamped CSV. \code{NULL} (default) writes nothing.}

\item{verbose}{Logical; emit progress and summary messages. Default TRUE.}
}
\value{
\code{provider_tbl} with HWSM columns added, carrying a \code{provenance}
attribute from \code{\link[=hwsm_provenance]{hwsm_provenance()}}.
}
\description{
Adds age- and sex-specific weekly professional hours, work-effort FTE
(\code{hwsm_fte}), probability of remaining active (\code{p_active}), and one-year
retirement probability (\code{p_retire_next_year}) from the HRSA HWSM Surgery
physician parameters. Surgery is the proxy for practising urogynecologists.

HRSA defines one physician FTE as 40 professional hours per week, so
\code{hwsm_fte} can exceed 1.0 for high-hours cohorts.
}
\details{
\code{hwsm_fte} is work effort among physicians who are STILL ACTIVE. Retirement is
simulated separately (see \code{\link[=hwsm_retirement_hazard_table]{hwsm_retirement_hazard_table()}}), so do NOT multiply
\code{hwsm_fte} by \code{p_active} -- that would charge retirement twice. \code{hwsm_fte} is on
HRSA's 40 professional-hours basis; it is not the engine's 37.2 clinical-hour
FTE (see \code{\link[=apply_hrsa_surgical_fte]{apply_hrsa_surgical_fte()}} for the peak-relative clinical variant).
}
\examples{
\dontrun{
roster <- data.frame(age = c(40L, 62L, 71L), sex = c("Female", "Male", "Female"))
add_hwsm_supply_parameters(roster)
}
}
\seealso{
Other hwsm supply:
\code{\link{hwsm_provenance}()},
\code{\link{hwsm_retirement_hazard_table}()}
}
\concept{hwsm supply}
\concept{supply}
