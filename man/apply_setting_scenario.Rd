% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-urps_settings.R
\name{apply_setting_scenario}
\alias{apply_setting_scenario}
\title{Apply a named setting-shift scenario to a service-volume tibble}
\usage{
apply_setting_scenario(
  volumes,
  scenario_id = "baseline",
  mix = URPS_DEFAULT_SETTING_MIX,
  productivity = URPS_SETTING_PRODUCTIVITY,
  aggregate = TRUE,
  registry = URPS_SETTING_SCENARIOS
)
}
\arguments{
\item{volumes}{Tibble with \code{service}, \code{volume} (and optionally \code{year}).}

\item{scenario_id}{Character key in \link{URPS_SETTING_SCENARIOS}.}

\item{mix}{Setting-mix table for the split step.}

\item{productivity}{Per-setting productivity multipliers.}

\item{aggregate}{Logical. When \code{TRUE} (default), re-aggregate to
(\code{year}, \code{service}) level for downstream wRVU conversion. When \code{FALSE},
return the long setting-level tibble.}

\item{registry}{Setting scenario registry; defaults to \link{URPS_SETTING_SCENARIOS}.}
}
\value{
A tibble compatible with \code{\link[=convert_workload_to_fte]{convert_workload_to_fte()}}.
}
\description{
Convenience function that:
\enumerate{
\item Splits undifferentiated volumes across settings via \code{\link[=split_volumes_by_setting]{split_volumes_by_setting()}}.
\item Applies each shift specification from \code{URPS_SETTING_SCENARIOS[[scenario_id]]}.
\item Applies per-setting productivity adjustments via \code{\link[=apply_setting_productivity]{apply_setting_productivity()}}.
\item Re-aggregates to the (\code{year}, \code{service}) level so the output is
compatible with \code{\link[=convert_workload_to_fte]{convert_workload_to_fte()}} — or returns the long
setting-level tibble when \code{aggregate = FALSE}.
}
}
\examples{
demand <- compute_demand_denominators(example_female_population_by_band())
vols   <- example_service_volumes(demand)
adj    <- apply_setting_scenario(vols, "telehealth_10pct")
# adj is in the same shape as vols but with effective volumes adjusted
}
\seealso{
\code{\link[=split_volumes_by_setting]{split_volumes_by_setting()}}, \code{\link[=apply_setting_productivity]{apply_setting_productivity()}},
\link{URPS_SETTING_SCENARIOS}
}
