% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting-export_demand_contract.R
\name{export_dmdm_demand_contract}
\alias{export_dmdm_demand_contract}
\title{Export a DMDM open-population trajectory as demand-contract tiers}
\usage{
export_dmdm_demand_contract(
  trajectory,
  output_directory,
  model_version = DMDM_DEMAND_CONTRACT_VERSION,
  base_year = 2025L,
  calibration_status = "placeholder_uncalibrated",
  transitions = NULL,
  population_scope = "us_adult_women",
  allow_uncalibrated = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{trajectory}{Data frame with \code{year}, \code{population}, \code{prev_ui}, \code{prev_pop},
\code{prev_ai}.}

\item{output_directory}{Directory to write into (created if missing).}

\item{model_version}{Version string. Default \code{DMDM_DEMAND_CONTRACT_VERSION}.}

\item{base_year}{Calendar year the index is normalized to (= 100). Default 2025.}

\item{calibration_status}{Provenance guard. Keep "placeholder_uncalibrated"
until the onset/remission hazards are fitted (see \code{\link[=fit_dmdm_transitions]{fit_dmdm_transitions()}}).
Ignored (and derived from \code{transitions}) when \code{transitions} carries its own
\code{calibration_status}.}

\item{transitions}{Optional transition object actually used to produce
\code{trajectory} (e.g. \code{\link[=dmdm_transitions_with_pop_literature]{dmdm_transitions_with_pop_literature()}}). When supplied,
its \code{calibration_status} becomes the object-level status and its per-condition
\code{provenance} is stamped per tier in a \code{tier_calibration_status} column, so a
downstream consumer can gate on the provenance of the specific tier it reads
(e.g. \code{dmdm_pop} = "derived_by_analogy" while \code{dmdm_ui}/\code{dmdm_ai} stay
placeholder). The any-PFD \code{tier3} is stamped with the \emph{weakest} provenance
across the conditions that compose it.}

\item{population_scope}{Population denominator label. Default "us_adult_women".}

\item{allow_uncalibrated}{Declare an exploratory export. Defaults to FALSE, so
the contract is refused unless every tier it would write is \code{"fitted"} or
\code{"calibrated"}; see \code{\link[=assert_calibrated_transitions]{assert_calibrated_transitions()}}. This mirrors the gate
on the engines that produce \code{trajectory} -- without it, a hand-assembled
trajectory could still reach downstream consumers as a demand contract.}

\item{verbose}{Log progress. Default TRUE.}
}
\value{
(invisibly) a list with \code{csv_path}, \code{manifest_path}, and the tidy \code{data}.
}
\description{
Formats a dynamic-prevalence trajectory (from
\code{\link[=dmdm_open_prevalence_trajectory]{dmdm_open_prevalence_trajectory()}} / \code{\link[=simulate_dmdm_open]{simulate_dmdm_open()}}) into the shared
demand-contract schema, emitting \code{tier3_prevalent_pfd} (any-PFD) plus
per-condition \code{dmdm_ui}/\code{dmdm_pop}/\code{dmdm_ai}, indexed to the base year, with a
JSON provenance manifest.
}
\seealso{
Other export demand contract:
\code{\link[=export_hdmm_demand_contract]{export_hdmm_demand_contract()}}
}
\concept{export demand contract}
\concept{reporting}
\keyword{internal}
