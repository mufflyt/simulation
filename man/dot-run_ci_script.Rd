% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting-scorecard_audits.R
\name{.run_ci_script}
\alias{.run_ci_script}
\title{Run a CI script as a subprocess from the repository root}
\usage{
.run_ci_script(rel_path)
}
\arguments{
\item{rel_path}{Script path relative to the repo root.}
}
\value{
A list with \code{available} and, when available, \code{status} and \code{text}.
}
\description{
The scripts this backs (\code{scripts/ci/check_suite.R},
\verb{.github/scripts/adversarial/*.R}) assume \code{getwd()} is the repository
root, per their own headers. \code{system2()} inherits the CALLER's working
directory, which may be \code{tests/testthat} (when called from a test) or
anywhere else (when called interactively) -- so the working directory is
switched to the resolved repo root for the duration of the call, exactly
as CI itself runs them.
}
\keyword{internal}
