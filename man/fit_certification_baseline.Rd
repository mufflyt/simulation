% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/41-interval_coverage.R
\name{fit_certification_baseline}
\alias{fit_certification_baseline}
\title{Fit an expected-count baseline to the certification series}
\usage{
fit_certification_baseline(
  certification_series,
  fit_through_year,
  trend_family = "loglinear",
  verbose = TRUE
)
}
\arguments{
\item{certification_series}{A data frame with integer \code{year} and integer
non-negative \code{certification_count}, one row per year, no gaps.}

\item{fit_through_year}{Integer scalar. Only years at or before this are
used. Every later year is held out and never read, so this argument is
the leakage boundary.}

\item{trend_family}{Character scalar, either \code{"loglinear"} for a
log-linear trend in year or \code{"intercept"} for a flat mean. Use
\code{"intercept"} when fewer than six training years are available.}

\item{verbose}{Logical flag controlling console logging via
\code{\link[base:message]{base::message()}}. Defaults to \code{TRUE}.}
}
\value{
A list with elements \code{fitted_model}, \code{dispersion},
\code{training_years}, \code{trend_family}, and \code{fit_through_year}.
}
\description{
Fits a smooth count trend to certifications observed strictly before
\code{fit_through_year}, in the observed-versus-expected spirit of the excess
mortality literature: the baseline states what the series would have done
absent disruption, and the disrupted window is scored against it rather
than absorbed into it.
}
\details{
Dispersion is estimated from Pearson residuals rather than assumed to be
one. The certification series is strongly overdispersed relative to
Poisson, and treating it as Poisson is one of the two reasons the shipped
intervals are too narrow.
}
\examples{
illustrative_certification_series <- data.frame(
  year = 2012:2023,
  certification_count = c(
    33L, 36L, 41L, 39L, 45L, 44L, 40L, 48L, 10L, 81L, 54L, 72L
  )
)
baseline <- fit_certification_baseline(
  illustrative_certification_series, 2020L, "loglinear", verbose = FALSE
)
round(baseline$dispersion, 1)
}
