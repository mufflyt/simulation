% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/25-demand_lifecourse.R
\name{lifecourse_required_fte}
\alias{lifecourse_required_fte}
\title{Required provider FTE from the life-course demand pathway}
\usage{
lifecourse_required_fte(
  pop_by_age_year,
  wrvu_per_fte,
  scenario = "baseline",
  n = 100000,
  seed = NULL,
  ...
)
}
\arguments{
\item{pop_by_age_year}{Tibble with \code{year}, \code{age}, \code{population}.}

\item{wrvu_per_fte}{Annual work RVUs per clinical FTE. No default on purpose:
derive it from a base-year anchor with \code{\link[=calibrate_wrvu_per_fte]{calibrate_wrvu_per_fte()}} rather than
assuming a productivity level. For an illustrative run pass
\code{WRVU_PER_FTE_BENCHMARK[["median"]]}.}

\item{scenario, n, seed, ...}{Passed to \code{\link[=lifecourse_demand_trajectory]{lifecourse_demand_trajectory()}}.}
}
\value{
A list with \code{service_volumes}, \code{demand_summary}, and \code{required_fte}
(the tibble returned by \code{\link[=convert_workload_to_fte]{convert_workload_to_fte()}}).
}
\description{
Runs \code{\link[=lifecourse_demand_trajectory]{lifecourse_demand_trajectory()}} and converts its annual service volumes
to required FTE with \code{\link[=convert_workload_to_fte]{convert_workload_to_fte()}} (R/17). The provider-type
delegation matrix and the work-RVU conversion are therefore REUSED, not
reimplemented here.
}
