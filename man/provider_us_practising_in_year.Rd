% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-provider_microsimulation.R
\name{provider_us_practising_in_year}
\alias{provider_us_practising_in_year}
\title{STRICT "practising in the US in year Y" predicate}
\usage{
provider_us_practising_in_year(agents, year)
}
\arguments{
\item{agents}{Agent tibble; \code{left_country} is optional and absent means nobody
has emigrated, which is the state of every run that does not apply the
migration matrix.}

\item{year}{Integer year.}
}
\value{
Logical vector, TRUE where the agent is active AND in the country.
}
\description{
\code{\link[=provider_active_in_year]{provider_active_in_year()}} answers the CERTIFICATION question: has this
provider entered practice and not retired. That is the quantity the observed
contract series measures -- \code{n_active} equals \code{n_ever_certified} in every row,
so the series counts people who hold certification whether or not they
practise here. A provider who emigrates is still certified, and removing them
from that count would break the definition match the back-test depends on.
}
\details{
This answers the SUPPLY question instead: can this provider see a patient in
the United States. An emigrant cannot, so they leave this count -- and they
have already left every state total, because
\code{\link[=apply_provider_migration_matrix]{apply_provider_migration_matrix()}} sets their \code{state} to NA.

The two differ by exactly the emigrant stock, which is why both exist. The
supply/demand gap must use THIS one: comparing a certification count against
US demand inflates supply by every provider who left.
}
\seealso{
Other provider microsimulation: 
\code{\link{build_hazard_table}()},
\code{\link{classify_workforce_outlook}()},
\code{\link{initialize_provider_agents}()},
\code{\link{microsim_age_band_of}()},
\code{\link{project_supply_deterministic}()},
\code{\link{provider_active_in_year}()},
\code{\link{provider_clinical_fte}()},
\code{\link{retirement_hazard}()},
\code{\link{run_supply_microsimulation}()},
\code{\link{simulate_provider_career_once}()}
}
\concept{provider microsimulation}
\concept{supply}
