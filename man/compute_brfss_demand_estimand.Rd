% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-urps.R
\name{compute_brfss_demand_estimand}
\alias{compute_brfss_demand_estimand}
\title{Compute D4 demand estimand from BRFSS population cells}
\usage{
compute_brfss_demand_estimand(
  pop_by_band,
  brfss_cells,
  condition = "ui",
  care_seeking_rate = 0.25,
  referral_rate = 0.5
)
}
\arguments{
\item{pop_by_band}{Tibble from \code{\link[=npp_female_by_band]{npp_female_by_band()}} with columns
\code{year}, \code{age_band}, \code{female_pop}.}

\item{brfss_cells}{Population cell table from
\code{\link[=build_urps_population_cells]{build_urps_population_cells()}}.}

\item{condition}{PFD condition to use; passed to
\code{\link[=brfss_pfd_prevalence_for_demand_bands]{brfss_pfd_prevalence_for_demand_bands()}}.  Default \code{"ui"}.}

\item{care_seeking_rate}{Fraction of prevalent women seeking care annually.
Default 0.25 (Nygaard 2008 supplemental).}

\item{referral_rate}{Fraction of care-seeking women reaching a
urogynecologist.  Default 0.50 (placeholder; calibrate to Kirby 2013).}
}
\value{
Long tibble: \code{year}, \code{estimand = "D4"}, \code{label}, \code{demand_cases}.
}
\description{
Produces a fourth demand series (D4) using BRFSS survey-weighted UI
prevalence crossed with care-seeking and referral rates, scaled to the
Census-NPP female population projection.  D4 is returned in the same long
format as \code{\link[=compute_demand_denominators]{compute_demand_denominators()}} so it can be appended to the
D1/D2/D3 tibble and flow through \code{\link[=assert_estimands_independent]{assert_estimands_independent()}} and
\code{\link[=assess_demand_concordance]{assess_demand_concordance()}} without modification.
}
\details{
D4 differs from D1 in three ways: (1) prevalence is BRFSS survey-weighted
rather than a Nygaard 2008 scalar, (2) the rate includes an explicit
care-seeking × referral step so the unit is urogynecology visits, not
prevalent cases, and (3) coverage of 18-39 year-olds is included via the
BRFSS "18-34" band rather than starting at 20.
}
