% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-fellowship_conversion.R
\name{fit_fellowship_conversion}
\alias{fit_fellowship_conversion}
\alias{print.fellowship_conversion}
\title{Fit the conversion fraction and lag distribution jointly}
\usage{
fit_fellowship_conversion(
  filled,
  outcome,
  lags = FELLOWSHIP_DEFAULT_LAGS,
  coverage = 1,
  exclude_years = NULL,
  n_starts = 24L,
  seed = 20260811L
)

\method{print}{fellowship_conversion}(x, ...)
}
\arguments{
\item{filled}{Named numeric vector of NRMP positions filled, keyed by
appointment year.}

\item{outcome}{Named numeric vector of outcomes, keyed by outcome year.}

\item{lags}{Integer lags to search.}

\item{coverage}{Fraction of the national outcome population observed.}

\item{exclude_years}{Outcome years to drop, for administrative artefacts that
are not cohort flow -- the 2014 founding backlog of a subspecialty whose
certification began in 2013, and the 2020 cancelled examination. Dropping
them is a judgement about what the outcome series MEASURES, so it is an
explicit argument, is recorded on the returned object, and is printed. It is
never applied by default: a caller who does not ask keeps every year.}

\item{n_starts}{Random restarts for the optimiser.}

\item{seed}{RNG seed; the RNG state is restored on exit.}

\item{x}{A \code{fellowship_conversion} object.}

\item{...}{Unused.}
}
\value{
Object of class \code{fellowship_conversion}.
}
\description{
Constrained least squares on \verb{outcome_t = theta * sum_k w_k * filled_\{t-k\}}.
\code{w} is carried on a softmax so it stays a probability vector without a
constrained optimiser, and \code{theta} on a log scale so it stays positive
WITHOUT being capped at 1 (see the file header).
}
\details{
IDENTIFIABILITY IS REPORTED, NOT ASSUMED. With ten or so outcome years and a
seven-point lag support, \code{w} is weakly identified: many shapes fit almost
equally well. The returned object carries \code{n_obs}, \code{n_params} and the
steady-state comparison so a reader can see how much the shape is being
asked to do.
}
\seealso{
Other fellowship conversion:
\code{\link{FELLOWSHIP_DEFAULT_LAGS}},
\code{\link{FELLOWSHIP_ROSTER_ABOG_COVERAGE}},
\code{\link[=fellowship_certification_series]{fellowship_certification_series()}},
\code{\link[=fellowship_conversion_steady_state]{fellowship_conversion_steady_state()}},
\code{\link[=fellowship_first_billing_series]{fellowship_first_billing_series()}}
}
\concept{fellowship conversion}
\concept{supply}
