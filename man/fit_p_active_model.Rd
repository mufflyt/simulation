% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-urps_flows.R
\name{fit_p_active_model}
\alias{fit_p_active_model}
\title{Fit a labor force participation model from observed activity data}
\usage{
fit_p_active_model(data, weights = NULL)
}
\arguments{
\item{data}{A data frame with at least:
\describe{
\item{active}{Integer or logical: 1/TRUE = active, 0/FALSE = inactive.}
\item{age}{Numeric age in completed years.}
\item{sex}{Character \code{"male"} / \code{"female"} (case-insensitive).}
\item{years_certified}{Numeric years since first board certification.}
}}

\item{weights}{Optional numeric weights (e.g. survey sample weights).}
}
\value{
A named list with the same structure as \link{URPS_P_ACTIVE_COEF} plus
metadata elements \verb{$model} (the fitted \code{glm} object), \verb{$n}, \verb{$source}.
}
\description{
When ABOG recertification or AMA Masterfile records are available, this
function estimates the logistic regression coefficients empirically rather
than using the pre-calibrated \link{URPS_P_ACTIVE_COEF} defaults.
}
\details{
The fitted coefficient list is compatible with the \code{coef} argument of
\code{\link[=urps_p_active]{urps_p_active()}}.
}
\examples{
\dontrun{
abog_df <- read.csv("data-raw/abog_recert_longitudinal.csv")
fitted  <- fit_p_active_model(abog_df)
urps_p_active(65, "female", 32, coef = fitted)
}
}
