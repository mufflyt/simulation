% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-condition_service_pathway.R
\name{pathway_service_volumes}
\alias{pathway_service_volumes}
\title{Service volumes from the condition-specific pathway}
\usage{
pathway_service_volumes(
  treated,
  year,
  pathway = condition_service_pathway(),
  by_stage = FALSE
)
}
\arguments{
\item{treated}{Named numeric vector of treated patients per condition.}

\item{year}{Calendar year stamped on the result.}

\item{pathway}{A table from \code{\link[=condition_service_pathway]{condition_service_pathway()}}.}

\item{by_stage}{Return \code{condition} and \code{stage} alongside \code{service} instead of
collapsing to service totals. Useful for auditing where workload arises.}
}
\value{
A tibble of \code{year}, \code{service}, \code{volume} (plus \code{condition} and \code{stage}
when \code{by_stage = TRUE}).
}
\description{
Drop-in replacement for the flat \verb{condition x service x per_treated} join:
takes the same treated-patient counts and returns the same
\code{year}/\code{service}/\code{volume} shape \code{\link[=convert_workload_to_fte]{convert_workload_to_fte()}} expects.
}
