% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting-baseline_gap.R
\name{external_anchor_gap}
\alias{external_anchor_gap}
\title{Base-year shortfall from an external published anchor}
\usage{
external_anchor_gap(
  base_supply_fte,
  adequacy = NULL,
  required_fte = NULL,
  anchor,
  citation,
  calibration_status = c("calibrated", "derived_by_analogy"),
  evidence = character(0),
  access_components_counted = character(0)
)
}
\arguments{
\item{base_supply_fte}{Base-year supplied FTE.}

\item{adequacy}{Base-year adequacy ratio (supply / demand) implied by the
anchor. Give this or \code{required_fte}, not both.}

\item{required_fte}{Base-year required FTE implied by the anchor.}

\item{anchor}{Short label for what the anchor is (e.g. "ACOG/AUGS
urogynaecologists per 100,000 women benchmark").}

\item{citation}{Attribution for the anchor. Required and non-empty.}

\item{calibration_status}{\code{"calibrated"} when the anchor was measured on
urogynaecologists, \code{"derived_by_analogy"} when it comes from another
specialty.}

\item{evidence}{Additional supporting indicators.}

\item{access_components_counted}{Access deficits already inside this anchor,
passed through to \code{\link[=assert_access_not_double_counted]{assert_access_not_double_counted()}}.}
}
\value{
An object of class \code{urps_baseline_gap}.
}
\description{
The fourth route, and in practice the reachable one for a surgical
subspecialty. HRSA designates shortage areas for primary medical care, dental
and mental health -- not subspecialties -- so \code{\link[=hpsa_removal_shortfall]{hpsa_removal_shortfall()}} has
no URPS input to read, which leaves a fielded capacity survey or an anchor
borrowed from outside this model.
}
\details{
An anchor is any externally published estimate that pins the base-year level:
a provider-to-population benchmark, a payer network-adequacy standard, an
access-based required-supply estimate, or another study's URPS shortfall.
Supply exactly one of \code{adequacy} or \code{required_fte}; the other is derived.

Unlike \code{\link[=assumed_baseline_gap]{assumed_baseline_gap()}}, this route demands a \code{citation}. An anchor is
external evidence, so it is attributable by construction; an unattributable
one is an assumption and belongs in \code{\link[=assumed_baseline_gap]{assumed_baseline_gap()}}.

Returns a finished \code{\link[=baseline_gap]{baseline_gap()}} object rather than a bare list, because
this is the only route that already carries everything the object needs --
the tier and the source travel with the anchor itself.
}
\seealso{
Other baseline gap: 
\code{\link{assert_access_not_double_counted}()},
\code{\link{assert_baseline_gap_estimated}()},
\code{\link{assumed_baseline_gap}()},
\code{\link{baseline_gap}()},
\code{\link{capacity_category_adequacy}()},
\code{\link{capacity_survey_adequacy}()},
\code{\link{capacity_survey_shortfall_bottom_up}()},
\code{\link{hpsa_removal_shortfall}()},
\code{\link{published_baseline_gaps}()},
\code{\link{required_fte_base_year}()}
}
\concept{baseline gap}
\concept{reporting}
