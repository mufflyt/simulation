% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-lifecourse.R
\name{lifecourse_demand_estimand}
\alias{lifecourse_demand_estimand}
\title{Life-course demand as a concordance estimand}
\usage{
lifecourse_demand_estimand(
  demand_summary,
  measure = c("service_units", "care_seeking"),
  estimand = "D5_lifecourse_service",
  label = "Life-course service demand (care-pathway, vaginal-delivery driven)"
)
}
\arguments{
\item{demand_summary}{The \code{demand_summary} from \code{\link[=lifecourse_demand_trajectory]{lifecourse_demand_trajectory()}}.}

\item{measure}{Which series to treat as demand: "service_units" (procedural
load) or "care_seeking".}

\item{estimand, label}{Estimand id and label.}
}
\value{
Tibble with \code{year}, \code{estimand}, \code{label}, \code{demand_cases}.
}
\description{
Reshapes a life-course demand summary into the long demand-estimand format used
by \code{\link[=compute_demand_denominators]{compute_demand_denominators()}}, \code{\link[=compute_growth_adequacy]{compute_growth_adequacy()}} and
\code{\link[=assess_demand_concordance]{assess_demand_concordance()}}, so the childbirth-driven pathway can be checked
for concordance against the other estimands. Its generator differs, so it is
not a proportional rescaling — exactly the real-concordance check the framework
wants.
}
\details{
Estimand id note: this is \code{D5}, the life-course \emph{service} demand (downstream of
the care pathway). It is distinct from \code{D4} in
\code{\link[=compute_demand_denominators_lifecourse]{compute_demand_denominators_lifecourse()}} (R/demand-obstetric_exposure), which is an obstetric-
exposure-weighted prevalent-case \emph{denominator}. D1–D3 are the published
denominators (R/demand-urps); D4 is the obstetric-weighted denominator; D5 is the
dynamic service-demand series.
}
\seealso{
Other lifecourse:
\code{\link[=lifecourse_demand_trajectory]{lifecourse_demand_trajectory()}},
\code{\link[=lifecourse_required_fte]{lifecourse_required_fte()}},
\code{\link[=lifecourse_risk_params_cited]{lifecourse_risk_params_cited()}},
\code{\link[=lifecourse_risk_params_placeholder]{lifecourse_risk_params_placeholder()}},
\code{\link[=simulate_lifecourse_demand]{simulate_lifecourse_demand()}}
}
\concept{demand}
\concept{lifecourse}
