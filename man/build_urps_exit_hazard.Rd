% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-retirement_hazard.R
\name{build_urps_exit_hazard}
\alias{build_urps_exit_hazard}
\title{Build URPS Age-Specific Retirement Hazard}
\usage{
build_urps_exit_hazard(
  cliff_duckdb_path = NULL,
  cliff_ageband_csv = system.file("extdata", "provider_year",
    "retirement_hazard_by_ageband.csv", package = "urpssim"),
  require_calibrated = FALSE,
  min_confidence = 0.6,
  smooth = TRUE,
  scale_shift = 0,
  verbose = TRUE
)
}
\arguments{
\item{cliff_duckdb_path}{Character path to the cliff DuckDB, or NULL. Used
only when \code{cliff_ageband_csv} is unavailable.}

\item{cliff_ageband_csv}{Character path to the cliff exposure-based age-band
hazard CSV (columns \code{age_band}, \code{person_years}, \code{events}, \code{annual_hazard}).
Defaults to the vendored copy under \code{inst/extdata/provider_year}; set to
\code{NULL} to force the DuckDB/Weibull path.}

\item{require_calibrated}{Logical; if TRUE, error instead of silently
returning the \code{derived_by_analogy} Weibull fallback. Every fallback path
otherwise yields a well-formed 102-row curve with \code{n_events = 0}, which is
indistinguishable from success unless the caller inspects \code{source}. Set
this whenever a downstream result depends on the hazard actually being
fitted to observed data. It also catches the partial case, where a per-sex
fallback leaves analogy rows inside a result still labelled \code{calibrated}.
Default FALSE, so existing callers are unaffected.}

\item{min_confidence}{Minimum cliff confidence score to include. Default 0.60.}

\item{smooth}{Logical; loess-smooth the per-age predictions. Default TRUE.}

\item{scale_shift}{Numeric years to shift the hazard curve along age
(\code{+2} = later/delayed retirement). Applied to the cliff age-band and
Weibull paths alike.}

\item{verbose}{Logical.}
}
\value{
Named list: \code{exit_probs} (data frame), \code{source}, \code{n_events},
\code{hazard_cv}, and \code{weibull_params} (present when a Weibull path is used).
}
\description{
Returns a per-age exit probability table. Its consumer,
\code{advance_urps_agents()}, is archived in inst/archive/supply.R.
By default the calibrated source is the cliff pipeline's exposure-based
age-band hazard (person-years at risk and departure events per age band,
vendored under \code{inst/extdata/provider_year}), expanded to a per-age curve at
the \code{calibrated} tier with Poisson exposure-based uncertainty. This is a real
age-conditional hazard (a risk set), preferred over both the events-only
DuckDB Gompertz fit and the Weibull analogy. Sparse zero-event bands (the
70+ band has 0 events in 16 person-years) fall back to the Weibull analogy
for those ages rather than asserting no retirement. When the age-band file is
absent and a cliff DuckDB is supplied, a Gompertz model is fitted to observed
departure events; otherwise the Weibull survival curves from
\code{\link[=urps_weibull_exit_probs]{urps_weibull_exit_probs()}} (derived-by-analogy from HWSM Exhibits 17–18) are
used.
}
\concept{retirement hazard}
\concept{supply}
