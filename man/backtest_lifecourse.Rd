% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-demand_lifecourse.R
\name{backtest_lifecourse}
\alias{backtest_lifecourse}
\title{Back-test the life-course demand model against a held-out year}
\usage{
backtest_lifecourse(
  service_volumes,
  observed_fit,
  observed_target,
  fit_through_year = 2017L,
  target_year = 2023L,
  map = LIFECOURSE_ANCHOR_MAP,
  compare_categories = NULL
)
}
\arguments{
\item{service_volumes}{Tibble \code{year}, \code{service}, \code{volume} spanning both years.}

\item{observed_fit}{Tibble \code{category}, \code{observed} at \code{fit_through_year}.}

\item{observed_target}{Tibble \code{category}, \code{observed} at \code{target_year}.}

\item{fit_through_year, target_year}{Back-test window.}

\item{map}{Service-to-anchor-category map.}

\item{compare_categories}{Optional character vector naming the categories that
define the PRIMARY back-test score. When \code{NULL} (default) every comparable
category is primary (unchanged behaviour). When supplied, the summary MAPE
is computed only over these categories; the others are retained in
\code{by_category} as secondary diagnostics (\code{primary_backtest = FALSE}). Errors
if none of the requested categories are comparable, so an empty configured
set fails loudly rather than silently scoring against everything.}
}
\value{
List: \code{by_category} (\code{category}, \code{predicted}, \code{observed}, \code{ratio},
\code{abs_pct_error}, \code{primary_backtest}) and \code{summary} (\code{mape}, \code{n},
\code{target_year}, \code{primary_categories}). The MAPE is over the primary set only.
}
\description{
Fits calibration scalars on \code{fit_through_year}, carries them forward, and
compares the model's projected anchor totals at \code{target_year} with observed
totals — the credibility check the Dall-family models stop short of. Defaults
read \code{config/calibration_targets.yml$backtest}.
}
\seealso{
Other demand lifecourse:
\code{\link[=calibrate_lifecourse_demand]{calibrate_lifecourse_demand()}},
\code{\link[=lifecourse_anchor_predictions]{lifecourse_anchor_predictions()}}
}
\concept{calibration}
\concept{demand lifecourse}
