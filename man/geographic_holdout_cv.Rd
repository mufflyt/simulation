% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-geographic_holdout.R
\name{geographic_holdout_cv}
\alias{geographic_holdout_cv}
\title{Geographic held-out (spatial) cross-validation of predicted vs observed stock}
\usage{
geographic_holdout_cv(
  data,
  observed,
  predictors,
  geo = NULL,
  region = NULL,
  scheme = c("loo", "region", "kfold"),
  k = 10L,
  family = c("poisson", "gaussian"),
  offset_col = NULL,
  fit_predict = NULL,
  seed = NULL
)
}
\arguments{
\item{data}{Data frame, one row per geography, with the observed column, the
predictor column(s), and optionally a \code{region} grouping and an offset column.}

\item{observed}{Name of the observed count/stock column (the validation target).}

\item{predictors}{Character vector of predictor column names.}

\item{geo}{Optional id column for labelling rows. Default row index.}

\item{region}{Optional grouping column for \code{scheme = "region"}.}

\item{scheme}{\code{"loo"} (default), \code{"region"}, or \code{"kfold"}.}

\item{k}{Number of folds for \code{"kfold"}. Default 10 (capped at \code{nrow(data)}).}

\item{family}{\code{"poisson"} (default; log-link count model) or \code{"gaussian"}.}

\item{offset_col}{Optional column for a Poisson log-offset (turns the count
model into a rate model, e.g. providers per capita). Default \code{NULL}.}

\item{fit_predict}{Optional \verb{function(train, test)} returning predictions for
\code{test}, overriding the default model. Must not look at \code{test}'s observed.}

\item{seed}{Optional RNG seed for \code{"kfold"} reproducibility.}
}
\value{
A list: \code{predictions} (one row per geography: \code{geo}, \code{region}, \code{fold},
\code{observed}, \code{predicted}), \code{metrics} (\code{n}, \code{mape}, \code{rmse}, \code{r2_oos},
\code{calibration_slope}, \code{spearman}), and the \code{scheme}/\code{family} used.
}
\description{
Refits the model on the training geographies and predicts the held-out ones,
so the score is genuinely out-of-sample along a dimension that played no part
in model selection. Supports leave-one-geography-out (\code{"loo"}),
leave-one-region-out (\code{"region"}, the classic geographic hold-out), and random
\code{"kfold"}. Leakage-free by construction: each fold's fit sees only its training
rows.
}
\concept{geographic holdout}
\concept{validation}
