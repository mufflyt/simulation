% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-urps.R
\name{compute_demand_denominators}
\alias{compute_demand_denominators}
\title{Compute D1/D2/D3 demand estimands from an AGE-BANDED female population}
\usage{
compute_demand_denominators(
  pop_by_band,
  pfd_prevalence = pfd_prevalence_by_band(),
  consult_rate = CONSULT_RATE_BY_AGE,
  surgery_rate_per_1000 = WU2011_SURGERY_RATE_PER_1000
)
}
\arguments{
\item{pop_by_band}{Tibble with \code{year}, \code{age_band} (one of \code{DEMAND_AGE_BANDS})
and \code{female_pop}.}

\item{pfd_prevalence}{Named age-band prevalence for D1.}

\item{consult_rate}{Named age-band consultation rate for D2.}

\item{surgery_rate_per_1000}{Named age-band surgery rate per 1,000 for D3.}
}
\value{
Long tibble: \code{year}, \code{estimand} (D1/D2/D3), \code{label}, \code{demand_cases}.
}
\description{
Each estimand applies its own age-specific rate schedule, so the three series
respond differently to a shifting age structure. This is the sanctioned entry
point; \code{\link[=compute_demand_denominators_crude]{compute_demand_denominators_crude()}} reproduces the old single-rate
behaviour and is retained only for backward comparison.
}
\details{
Three estimands with genuinely different age profiles: surgery peaks at 60-79
and falls by half at 80+, while prevalence keeps rising. That difference is
what makes them informative.

The failure this replaced computed every estimand as
\code{population_65plus * constant}, which makes D1, D2 and D3 the same series up
to scale -- three numbers that look like corroboration and are one number
printed three times. \code{\link[=detect_proportional_estimands]{detect_proportional_estimands()}} tests for exactly that
and \code{\link[=assert_estimands_independent]{assert_estimands_independent()}} refuses it in strict mode.
}
\examples{
# Three estimands with genuinely different age profiles -- not one series
# rescaled three ways, which is what detect_proportional_estimands() catches.
head(compute_demand_denominators(example_female_population_by_band(2025:2027)))
}
\seealso{
Other urps:
\code{\link[=anchor_index]{anchor_index()}},
\code{\link[=apply_age_specific_surgery_demand]{apply_age_specific_surgery_demand()}},
\code{\link[=assess_demand_concordance]{assess_demand_concordance()}},
\code{\link[=compute_brfss_demand_estimand]{compute_brfss_demand_estimand()}},
\code{\link[=compute_demand_coverage]{compute_demand_coverage()}},
\code{\link[=compute_demand_denominators_crude]{compute_demand_denominators_crude()}},
\code{\link[=compute_growth_adequacy]{compute_growth_adequacy()}},
\code{\link[=npp_female_by_band]{npp_female_by_band()}},
\code{\link[=npp_women_65plus]{npp_women_65plus()}},
\code{\link[=pfd_prevalence_by_band]{pfd_prevalence_by_band()}},
\code{\link[=pfd_prevalence_ownership]{pfd_prevalence_ownership()}},
\code{\link[=rebase_to_year]{rebase_to_year()}},
\code{\link[=resolve_demand_population]{resolve_demand_population()}}
}
\concept{demand}
\concept{urps}
