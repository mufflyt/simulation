% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/61-supply_capacity_hierarchy.R
\name{supply_capacity_hierarchy}
\alias{supply_capacity_hierarchy}
\title{Tiered supply-capacity hierarchy (headcount -> clinical FTE -> wRVU -> accessible)}
\usage{
supply_capacity_hierarchy(
  headcount,
  clinical_fte,
  wrvu_per_fte = WRVU_PER_FTE_BENCHMARK[["median"]],
  accessible_fraction = 1,
  insurance_fraction = 1,
  group = NA_character_
)
}
\arguments{
\item{headcount}{Active provider headcount (tier 1). Length 1, >= 0.}

\item{clinical_fte}{Clinical FTE (tier 2), already reflecting participation and
age/sex hours. Length 1, >= 0.}

\item{wrvu_per_fte}{Annual work RVUs per clinical FTE (tier 2->3 productivity).
Default the median of \code{WRVU_PER_FTE_BENCHMARK}; prefer a value from
\code{\link[=calibrate_wrvu_per_fte]{calibrate_wrvu_per_fte()}} solved on a base-year anchor.}

\item{accessible_fraction}{Share of capacity within geographic reach of the
population (tier 3->4), e.g. from \code{\link[=spatial_access_ratio]{spatial_access_ratio()}}. In 0 to 1.}

\item{insurance_fraction}{Share of capacity accepting the relevant insurance
(tier 3->4). In 0 to 1. Default 1 (no restriction modelled).}

\item{group}{Optional scalar label stamped on every row for easy binding.}
}
\value{
A 4-row tibble: \code{group}, \code{tier} (1-4), \code{label}, \code{value}, \code{unit},
\code{provider_equivalent}, \code{retained_vs_headcount}.
}
\description{
Assembles the four supply tiers for one group (e.g. a pathway, a year, or the
national total) into a tidy table with the between-tier conversion made
explicit. Map it over pathways/years and \code{dplyr::bind_rows()} the results; pass
\code{group} to label each call. Report ABU and ABOG separately (a scope assumption
that should not be buried in one pooled stock) by calling once per pathway.
}
