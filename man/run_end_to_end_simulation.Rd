% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-run_end_to_end_simulation.R
\name{run_end_to_end_simulation}
\alias{run_end_to_end_simulation}
\title{Run the coupled URPS workforce and demand simulation}
\usage{
run_end_to_end_simulation(
  start_year = 2025L,
  end_year = 2035L,
  n_agents = 1000L,
  initial_provider_count = 1200L,
  fellowship_entrants = 55L,
  app_delegation_rate = 0.15,
  medicaid_fee_ratio = 0.75,
  geography_engine = c("hrr_balance", "county_endogenous"),
  entrant_engine = c("parametric", "joint_donor_sampling"),
  productivity_engine = c("benchmark", "lmer_fitted"),
  county_market_tbl = NULL,
  entrant_donor_pool = NULL,
  productivity_panel = NULL,
  productivity_model = NULL,
  geography_solver = solve_endogenous_geography,
  entrant_simulator = simulate_joint_entrant_characteristics,
  productivity_fitter = fit_provider_productivity_model,
  productivity_predictor = NULL,
  geography_control = base::list(),
  evidence_db = NULL,
  empirical_parameters = NULL,
  policy_migration_scenario = "baseline",
  policy_evidence_db = NULL,
  run_practice_economics = FALSE,
  practice_payer_mix = NULL,
  practice_economics_draws = 100L,
  seed = 20260821L,
  save_outputs = TRUE,
  output_dir = "artifacts/end_to_end",
  service_share_engine = c("legacy_matrix", "calibrated"),
  service_share_bundle = NULL,
  service_share_draw = NULL
)
}
\arguments{
\item{start_year}{First simulation year.}

\item{end_year}{Last simulation year.}

\item{n_agents}{Number of patient agents.}

\item{initial_provider_count}{Baseline number of providers.}

\item{fellowship_entrants}{Annual number of fellowship graduates.}

\item{app_delegation_rate}{APP delegation fraction.}

\item{medicaid_fee_ratio}{Medicaid-to-Medicare fee ratio.}

\item{geography_engine}{Geography engine name.}

\item{entrant_engine}{Entrant engine name.}

\item{productivity_engine}{Productivity engine name.}

\item{county_market_tbl}{County-year market inputs for endogenous geography.}

\item{entrant_donor_pool}{Historical entrant profiles used as donors.}

\item{productivity_panel}{Provider-year productivity training panel.}

\item{productivity_model}{Optional already-fitted productivity model.}

\item{geography_solver}{Function used for endogenous geography.}

\item{entrant_simulator}{Function used for joint entrant sampling.}

\item{productivity_fitter}{Function used to fit productivity.}

\item{productivity_predictor}{Optional custom prediction function.}

\item{geography_control}{Named list passed to the geography solver.}

\item{evidence_db}{Optional national evidence DuckDB path.}

\item{empirical_parameters}{Optional named numeric vector of empirical parameters.}

\item{policy_migration_scenario}{One of \code{"baseline"} (default, an identity
transform -- zero behavior change), \code{"observed_migration"},
\code{"migration_stress"}, \code{"legislative_climate"}, or \code{"combined_stress"}.
See \code{\link[=simulate_policy_migration_scenarios]{simulate_policy_migration_scenarios()}}. Ignored unless
\code{policy_evidence_db} is also supplied.}

\item{policy_evidence_db}{Optional path to a policy migration DuckDB (see
\code{\link[=open_policy_migration_duckdb]{open_policy_migration_duckdb()}}). \code{NULL} (default) skips the
state-migration/legislative-policy step entirely. State-year allocation
uses real ACS population shares from
\code{\link[=national_older_female_population_by_state]{national_older_female_population_by_state()}}; when the database has no
ingested evidence, the module degrades gracefully to its declared prior
(see \code{\link[=calibrate_legislative_relocation]{calibrate_legislative_relocation()}}) rather than a no-op.}

\item{run_practice_economics}{Whether to run \code{\link[=simulate_practice_economics]{simulate_practice_economics()}}
each year against a practice-tbl bridged from \code{provider_cohort}. \code{FALSE}
(default) skips the step entirely -- zero behavior change. Each active
provider becomes one practice-year: \code{annual_wrvu} allocates that year's
national \code{wrvu_total} by each provider's share of \code{supplied_fte},
\code{practice_setting} derives from \code{academic_setting}/\code{hospital_outpatient}
(\code{academic}/\code{hospital_employed}/\code{independent}), \code{app_fte} is
\code{fte * app_delegation_rate} -- documented proxies, not real per-provider
data (this runner has no real provider-level wRVU or APP headcount).}

\item{practice_payer_mix}{Optional payer-mix tibble (the shape
\code{\link[=practice_payer_mix_defaults]{practice_payer_mix_defaults()}} returns), applied uniformly to every
provider-practice. Computed via \code{\link[=practice_payer_mix_defaults]{practice_payer_mix_defaults()}} when
\code{NULL} and \code{run_practice_economics = TRUE}.}

\item{practice_economics_draws}{Monte Carlo draws per practice-year passed
to \code{\link[=simulate_practice_economics]{simulate_practice_economics()}}; kept below its 500-draw default to
bound runtime at full provider-cohort scale.}

\item{seed}{Master random seed.}

\item{save_outputs}{Whether to save timestamped CSV files.}

\item{output_dir}{Directory for saved CSV files.}

\item{service_share_engine}{Which service-share pathway to route demand
through: \code{"legacy_matrix"} (the default fixed matrix) or \code{"calibrated"},
which draws from \code{service_share_bundle}.}

\item{service_share_bundle}{Calibrated service-share bundle, as validated by
\code{\link[=validate_service_share_bundle]{validate_service_share_bundle()}}. Required when \code{service_share_engine} is
\code{"calibrated"}.}

\item{service_share_draw}{Which \code{draw_id} of \code{service_share_bundle} to use.
\code{NULL} selects one deterministically from \code{seed}, so a run stays
reproducible; an unavailable id is an error rather than a silent fallback.}
}
\value{
A named list containing the audit ledger, spatial balances,
provider cohort, engine diagnostics, fitted productivity model, policy
migration diagnostics/summary, practice economics diagnostics, and
simulation configuration.
}
\description{
Runs the eight-step annual microsimulation and optionally couples the
endogenous county geography, joint entrant donor, and fitted productivity
engines. Supports empirical parameter overrides via National Evidence DuckDB.
}
\concept{core}
\concept{simulation pipeline}
