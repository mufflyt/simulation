% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-practice_payer_mix.R
\name{namcs_urps_payer_mix}
\alias{namcs_urps_payer_mix}
\title{NAMCS-derived URPS payer mix (Medicare/Medicaid/commercial/self-pay)}
\usage{
namcs_urps_payer_mix(namcs = NULL, min_records = NAMCS_MIN_RECORDS)
}
\arguments{
\item{namcs}{Tibble from \code{\link[=load_namcs_pooled]{load_namcs_pooled()}}; loaded when \code{NULL}.}

\item{min_records}{NCHS reliability floor (see \code{\link[=namcs_urps_visit_anchor]{namcs_urps_visit_anchor()}});
any tier below this unweighted-record count is flagged \code{reliable = FALSE}.}
}
\value{
Tibble with \code{payer_tier} (\code{Medicare}/\code{Medicaid}/\code{Private}/
\code{Uninsured}), \code{share} (fraction of URPS visits, \code{Other} excluded and the
rest renormalized to sum to 1), \code{n_unweighted}, \code{reliable}. Carries a
\code{provenance} attribute.
}
\description{
Revives \code{.namcs_insurance_tier()} (defined in
R/demand-namcs_visit_equations.R, written but never called before this
function) and applies it to the same URPS-filtered, PATWT-weighted visit
pool \code{namcs_urps_visit_anchor()} uses: \code{is_urps} diagnosis flag, NAMCS
\code{SEX == 1} (female), \code{AGE >= 18}, non-missing \code{PATWT}. The five-tier NAMCS
classification (\code{Private}/\code{Medicare}/\code{Medicaid}/\code{Uninsured}/\code{Other}) is
reduced to the four shares \code{\link[=simulate_practice_economics]{simulate_practice_economics()}} needs by
dropping \code{Other} (worker's comp, other government payers, no-charge/
charity -- none of which map cleanly onto the four-way split) and
renormalizing the rest to sum to 1.
}
\seealso{
Other practice economics:
\code{\link[=ahrq_3prd_medicare_medicaid_ratio]{ahrq_3prd_medicare_medicaid_ratio()}},
\code{\link[=chia_medicare_medicaid_ratio]{chia_medicare_medicaid_ratio()}},
\code{\link[=practice_payer_mix_defaults]{practice_payer_mix_defaults()}}
}
\concept{practice economics}
\concept{supply}
