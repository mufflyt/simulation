% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-backtest.R
\name{assert_no_leakage}
\alias{assert_no_leakage}
\title{Assert that no contract read exceeded the cutoff}
\usage{
assert_no_leakage(through_year = BACKTEST_CUTOFF_YEAR)
}
\arguments{
\item{through_year}{The cutoff the run declared.}
}
\value{
(Invisibly) the audit log.
}
\description{
Assert that no contract read exceeded the cutoff
}
\details{
The audit is STATEFUL. \code{.series_through()} records the maximum year of every
contract read; this checks that maximum against the declared cutoff. Call
\code{\link[=reset_leakage_audit]{reset_leakage_audit()}} at the start of a run, or the audit carries reads
from the previous one.

IT FAILS WHEN NOTHING WAS AUDITED, and that is deliberate. No recorded read
means either the back-test reached the contract outside \code{.series_through()}
-- so the guard saw nothing and proves nothing -- or it never read the
contract at all. Both are failures, and treating an empty audit as "clean"
would make the guard silently vacuous, which is worse than absent.
}
\seealso{
Other backtest:
\code{\link[=.series_through]{.series_through()}},
\code{\link{BACKTEST_PARAMETER_PROVENANCE}},
\code{\link{BACKTEST_TARGET_YEAR}},
\code{\link[=assert_backtest_parameters_precede_cutoff]{assert_backtest_parameters_precede_cutoff()}},
\code{\link[=backtest_attrition_requirement]{backtest_attrition_requirement()}},
\code{\link[=backtest_cohort_at]{backtest_cohort_at()}},
\code{\link[=backtest_cohorts_through]{backtest_cohorts_through()}},
\code{\link[=backtest_entrant_estimate]{backtest_entrant_estimate()}},
\code{\link[=backtest_loo_validation]{backtest_loo_validation()}},
\code{\link[=backtest_multi_window]{backtest_multi_window()}},
\code{\link[=backtest_oos_interval]{backtest_oos_interval()}},
\code{\link[=backtest_retirement_regime]{backtest_retirement_regime()}},
\code{\link[=backtest_rolling_origin]{backtest_rolling_origin()}},
\code{\link[=backtest_target_candidates]{backtest_target_candidates()}},
\code{\link[=reset_leakage_audit]{reset_leakage_audit()}},
\code{\link[=validate_backtest_target]{validate_backtest_target()}}
}
\concept{backtest}
\concept{validation}
