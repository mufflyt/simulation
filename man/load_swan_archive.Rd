% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/50-swan_archive.R
\name{load_swan_archive}
\alias{load_swan_archive}
\title{Load a SWAN archive file with verified provenance}
\usage{
load_swan_archive(
  file = "swan_all_visits.rds",
  path = NULL,
  verify = TRUE,
  expected_sha256 = NULL,
  mode = resolve_reproducibility_mode(),
  verbose = TRUE
)
}
\arguments{
\item{file}{Bare file name beneath the SWAN directory. Defaults to
\code{"swan_all_visits.rds"}, the only file carrying every covariate the DMDM
panel needs.}

\item{path}{Full path to an approved local copy, bypassing \code{\link[=swan_path]{swan_path()}}.
Recorded in the provenance so a run off a non-canonical copy is visible.}

\item{verify}{Hash the file and compare against the reference. \code{TRUE} by
default; set \code{FALSE} only for the multi-gigabyte files where the cost is not
worth it, which records \code{verified = FALSE}.}

\item{expected_sha256}{Override the shipped reference, for a file this package
does not ship a checksum for.}

\item{mode}{Reproducibility mode; strict errors on a mismatch.}

\item{verbose}{Narrate resolution, size, hash and verification.}
}
\value{
The object read from the \code{.rds}, carrying a \code{swan_archive_provenance}
attribute (\code{path}, \code{file}, \code{bytes}, \code{modified}, \code{sha256}, \code{verified},
\code{reference_source}).
}
\description{
Resolves the file beneath the external data root (or an explicitly approved
local copy), hashes it, checks the hash against \link{SWAN_ARCHIVE_SHA256}, reads
it, and attaches a \code{swan_archive_provenance} attribute.
}
\details{
A checksum mismatch means the bytes are not the ones the shipped reference was
taken from. That is treated the way every other provenance failure in this
package is treated: strict mode stops, relaxed mode warns loudly and proceeds,
so an exploratory session is not blocked but cannot mistake the file for the
validated one.
}
\seealso{
\code{\link[=swan_archive_provenance]{swan_archive_provenance()}}, \code{\link[=build_swan_dmdm_panel]{build_swan_dmdm_panel()}},
\code{\link[=swan_dmdm_panel_from_archive]{swan_dmdm_panel_from_archive()}}
}
