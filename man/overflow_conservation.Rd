% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geography-access_clearing.R
\name{overflow_conservation}
\alias{overflow_conservation}
\title{Check the demand-conservation invariants of an overflow_access() result}
\usage{
overflow_conservation(x, tol = 1e-08)
}
\arguments{
\item{x}{A tibble returned by \code{\link[=overflow_access]{overflow_access()}}.}

\item{tol}{Absolute tolerance for the "conserved" flags. Default 1e-8.}
}
\value{
A list: \code{per_catchment_resid} (numeric, one per row), \code{transfer_resid}
and \code{system_resid} (scalars), and \code{conserved} (all within \code{tol}).
}
\description{
Verifies the Phase-2 accounting from the returned columns alone: the
per-catchment identity \code{demand = served_local + spilled_out + unmet}, the
transfer balance \code{sum(spilled_out) = sum(spilled_in)}, and the system
identity \code{sum(served) + sum(unmet) = sum(demand)}. Returns the residuals so a
caller (or a test) can assert they are ~0 rather than trusting a boolean.
}
\seealso{
Other access clearing: 
\code{\link{catchment_neighbors_from_coords}()},
\code{\link{clear_access}()},
\code{\link{clear_access_trajectory}()},
\code{\link{overflow_access}()}
}
\concept{access clearing}
\concept{geography}
