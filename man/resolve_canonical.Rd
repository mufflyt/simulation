% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-canonical_and_joins.R
\name{resolve_canonical}
\alias{resolve_canonical}
\title{Resolve a canonical input path by logical name (fail-closed)}
\usage{
resolve_canonical(
  name,
  config_path = .canonical_config_path(),
  mode = resolve_reproducibility_mode()
)
}
\arguments{
\item{name}{Logical name of the source (a key under \verb{sources:} in the YAML).}

\item{config_path}{Path to the canonical-sources YAML.}

\item{mode}{Reproducibility mode (\code{\link[=resolve_reproducibility_mode]{resolve_reproducibility_mode()}}).}
}
\value{
Absolute path to the verified canonical file.
}
\description{
Looks \code{name} up in \code{config/canonical_sources.yml}. The entry may be a bare
path string or a mapping with \code{path} and optional \code{sha256}. Unknown names and
missing files always \code{stop()}. A recorded SHA-256 that does not match the
file's actual digest \code{stop()}s in strict mode and warns in relaxed mode.
}
\seealso{
Other canonical and joins: 
\code{\link{safe_left_join}()}
}
\concept{canonical and joins}
\concept{core}
