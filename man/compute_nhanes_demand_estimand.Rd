% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/46-nhanes_pfd_prevalence.R
\name{compute_nhanes_demand_estimand}
\alias{compute_nhanes_demand_estimand}
\title{Compute the D6 NHANES-calibrated prevalence-based demand estimand}
\usage{
compute_nhanes_demand_estimand(
  pop_projection,
  prevalence_table,
  care_seeking_rate = 0.25,
  visits_per_fte = 2800,
  fte_fraction = 0.35
)
}
\arguments{
\item{pop_projection}{Tibble with \code{year}, \code{age_band}, \code{race_eth},
\code{population} (projected women 20+).  Columns \code{hysterectomy} is optional;
if absent, prevalence is marginalized over hysterectomy status.}

\item{prevalence_table}{Output of \code{\link[=nhanes_ui_prevalence_by_stratum]{nhanes_ui_prevalence_by_stratum()}}.}

\item{care_seeking_rate}{Fraction of women with UI who seek specialist URPS
care annually.  Defaults to 0.25 (per Sung et al. 2010, OB-GYN).}

\item{visits_per_fte}{Annual URPS visits one FTE provider handles.
Defaults to 2800.}

\item{fte_fraction}{Fraction of specialist visits attributable to FPMRS.
Defaults to 0.35.}
}
\value{
Tibble with \code{year}, \code{estimand = "D6"}, \code{demand_visits},
\code{demand_clinical_fte}.
}
\description{
Combines NHANES UI prevalence with population projections and a care-seeking
rate to produce annual URPS visit demand and required FTE.
}
\details{
Formula:
demand_visits\link{stratum, year} =
prevalence\link{stratum} × population\link{stratum, year} × care_seeking_rate

demand_fte\link{year} = sum(demand_visits\link{year}) × fte_fraction / visits_per_fte
}
