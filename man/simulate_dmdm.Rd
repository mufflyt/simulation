% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-dynamic_multistate.R
\name{simulate_dmdm}
\alias{simulate_dmdm}
\title{Simulate a closed cohort forward through the pelvic-floor multistate model}
\usage{
simulate_dmdm(
  cohort0,
  start_year,
  end_year,
  transitions = dmdm_default_transitions(),
  seed = NULL,
  allow_uncalibrated = FALSE
)
}
\arguments{
\item{cohort0}{Data frame of women at \code{start_year} with columns \code{age},
\code{cumulative_vaginal_deliveries}, \code{years_since_last_vaginal_birth}, \code{bmi},
\code{hysterectomy}, \code{menopause_status}, \code{comorbidity}, and optionally initial
states \code{has_ui}/\code{has_pop}/\code{has_ai} (default absent).}

\item{start_year, end_year}{Simulation window (inclusive).}

\item{transitions}{Transition parameters; see \code{\link[=dmdm_default_transitions]{dmdm_default_transitions()}}.
Must be \code{"fitted"}/\code{"calibrated"} unless \code{allow_uncalibrated = TRUE}; see
\code{\link[=assert_calibrated_transitions]{assert_calibrated_transitions()}}.}

\item{seed}{Optional RNG seed (the engine is deterministic given it).}

\item{allow_uncalibrated}{Declare an exploratory run on placeholder
transitions. Defaults to FALSE, so strict mode stops rather than returning
numbers built on invented coefficients.}
}
\value{
A data frame, one row per year: \code{year}, \code{living}, \code{deaths},
\code{prev_ui}/\code{prev_pop}/\code{prev_ai} (prevalence among the living at the start of
the year) and \code{inc_ui}/\code{inc_pop}/\code{inc_ai} (new onsets during the year).
}
\description{
Simulate a closed cohort forward through the pelvic-floor multistate model
}
\seealso{
Other dynamic multistate:
\code{\link[=assert_calibrated_transitions]{assert_calibrated_transitions()}},
\code{\link[=dmdm_default_transitions]{dmdm_default_transitions()}},
\code{\link[=dmdm_prevalence_trajectory]{dmdm_prevalence_trajectory()}},
\code{\link[=dmdm_swan_fitted_transitions]{dmdm_swan_fitted_transitions()}}
}
\concept{demand}
\concept{dynamic multistate}
