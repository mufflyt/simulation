% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14-spatial_access_e2sfca.R
\name{compute_e2sfca_access}
\alias{compute_e2sfca_access}
\title{Compute E2SFCA accessibility (base R, no spatial dependencies)}
\usage{
compute_e2sfca_access(
  membership,
  supply,
  demand,
  weights = E2SFCA_DEFAULT_WEIGHTS,
  step2_power = 1,
  per_capita_scale = 1e+05
)
}
\arguments{
\item{membership}{Long tibble: \code{demand_id}, \code{provider_id}, \code{band} (minutes),
one row per (demand unit reachable from provider within that band).}

\item{supply}{Tibble: \code{provider_id}, \code{supply} (S_j, e.g. provider count/FTE).}

\item{demand}{Tibble: \code{demand_id}, \code{population} (P_i, the demand denominator).}

\item{weights}{Cumulative band weights.}

\item{step2_power}{1 = E2SFCA, 2 = M2SFCA.}

\item{per_capita_scale}{Scale for reporting (e.g. 1e5 => access per 100k).}
}
\value{
List: \code{access} (per demand unit), \code{provider_ratios}, \code{audit}, \code{meta}.
}
\description{
Two-step floating catchment over drive-time bands. Port of
twostep::dj7_tract_access.
}
\details{
Step 1 (provider ratio R_j): \verb{R_j = S_j / sum_b w'_b * Pop_b(j)}
Step 2 (accessibility A_i):  \verb{A_i = sum over j reaching i of W_b(i,j) * R_j}

Zero-demand semantics are preserved exactly: a provider whose weighted demand
is 0 yields ratio = NA (undefined, not 0) and contributes no access; its
supply is booked into the audit block (never silently treated as zero
capacity).
}
