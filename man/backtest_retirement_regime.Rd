% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-backtest.R
\name{backtest_retirement_regime}
\alias{backtest_retirement_regime}
\title{Which retirement regime the contract series is in}
\usage{
backtest_retirement_regime(series = NULL)
}
\arguments{
\item{series}{Contract series; read from the contract when NULL.}
}
\value{
One of "not_ascertained", "zero", "ascertained", with a \code{source}
attribute recording whether it came from the accessor or from inference.
}
\description{
VERSION-TOLERANT BY DESIGN. Two mufflyaccess generations describe the same
fact differently: 0.10.0 serves \code{n_retired} as integer zeros with no
\code{urps_retirement_status()}, while later contracts serve NA and expose that
accessor. Both mean "departures were never ascertained", and a guard keyed on
either representation alone breaks on the other -- which is exactly what
happened: tests asserting the NA form failed against 0.10.0.
}
\details{
The contract's own accessor is authoritative when present; otherwise the
regime is inferred from the data. Callers branch on the RETURNED VALUE rather
than on message text, so neither generation needs a different test.
}
\seealso{
Other backtest: 
\code{\link{.series_through}()},
\code{\link{BACKTEST_TARGET_YEAR}},
\code{\link{assert_no_leakage}()},
\code{\link{backtest_attrition_requirement}()},
\code{\link{backtest_cohort_at}()},
\code{\link{backtest_cohorts_through}()},
\code{\link{backtest_entrant_estimate}()},
\code{\link{backtest_loo_validation}()},
\code{\link{backtest_multi_window}()},
\code{\link{backtest_oos_interval}()},
\code{\link{backtest_rolling_origin}()},
\code{\link{backtest_target_candidates}()},
\code{\link{reset_leakage_audit}()},
\code{\link{validate_backtest_target}()}
}
\concept{backtest}
\concept{validation}
