% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-isochrone_access_response.R
\name{wait_response_region_holdout}
\alias{wait_response_region_holdout}
\title{Leave-one-region-out holdout of the fitted wait response}
\usage{
wait_response_region_holdout(
  response_table,
  wait_col = "wait_business_days",
  region_col = "region",
  geo_col = NULL,
  min_regions = 4L,
  seed = NULL
)
}
\arguments{
\item{response_table}{Matched call-level rows with \code{wait_col},
\code{demand_workload}, \code{accessible_capacity} and \code{region_col} (join the Lizeth
\code{state}/region onto the output of \code{\link[=join_lizeth_to_catchments]{join_lizeth_to_catchments()}}).}

\item{wait_col}{Observed wait column. Default \code{"wait_business_days"}.}

\item{region_col}{Region grouping column for the leave-one-region-out folds.
Default \code{"region"}.}

\item{geo_col}{Optional per-call id column for labelling. Default \code{NULL}.}

\item{min_regions}{Minimum distinct regions required to run the holdout.
Default \code{4}.}

\item{seed}{Passed through to \code{\link[=geographic_holdout_cv]{geographic_holdout_cv()}} (unused by the region
scheme, which is deterministic). Default \code{NULL}.}
}
\value{
The \code{\link[=geographic_holdout_cv]{geographic_holdout_cv()}} result (\code{predictions}, \code{metrics},
\code{scheme}, \code{family}) with added \code{n_regions} and \code{n_calls}.
}
\description{
Cross-validates the fitted access -> wait response across geography using
\code{\link[=geographic_holdout_cv]{geographic_holdout_cv()}} with \code{scheme = "region"}: \code{wait_scale} is refit on
the training regions and used to predict the held-out region's waits, so the
score is genuinely out-of-sample along a dimension that played no part in the
fit. This is the guard against a response overfit to the spatial sample before
it is allowed to resolve base-year adequacy.
}
\details{
Each matched call contributes its access term \code{x = rho / (1 - rho)}
(\code{rho = demand_workload / accessible_capacity}); saturated calls (\code{rho >= 1})
are excluded. The per-fold model is the closed-form origin least-squares
\code{wait_scale = sum(x * wait) / sum(x^2)} fit on training calls and applied to
the held-out region's \code{x}. \code{sigma} is held at the value used to build
\code{response_table}, so this tests transportability of the response given the
fitted decay, not a per-fold refit of the decay itself.
}
\seealso{
Other isochrone access response:
\code{\link{capacity_status_with_isochrone_response}()},
\code{\link{e2sfca_catchments_from_access}()},
\code{\link{fit_decay_sigma}()},
\code{\link{join_lizeth_to_catchments}()},
\code{\link{lizeth_wait_response_loss}()}
}
\concept{isochrone access response}
\concept{validation}
