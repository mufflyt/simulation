% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geography-access_clearing.R
\name{overflow_access}
\alias{overflow_access}
\title{Reallocate unmet demand to neighbouring catchments with spare capacity}
\usage{
overflow_access(
  catchments,
  neighbors,
  max_travel_penalty = Inf,
  appointment_window = 30,
  wait_scale = 30,
  wait_ceiling = Inf,
  status = "assumed_illustrative"
)
}
\arguments{
\item{catchments}{A data frame with a \code{catchment} id (unique), numeric
\code{demand_workload} and \code{accessible_capacity} (same currency), and the
optional \code{\link[=clear_access]{clear_access()}} columns. \code{NA} demand/capacity marks a
non-participating catchment (it neither spills nor absorbs).}

\item{neighbors}{A data frame of directed overflow edges: \code{from} and \code{to}
catchment ids and a non-negative \code{travel_penalty} (extra travel to reach
the neighbour, in the wait/appointment time unit). Self-edges are rejected;
every id must be present in \code{catchments}.}

\item{max_travel_penalty}{Edges with \code{travel_penalty} above this are dropped
(demand that can reach no neighbour within budget is censored as unmet).
Positive scalar. Default \code{Inf}.}

\item{appointment_window, wait_scale, wait_ceiling, status}{Passed through to
\code{\link[=clear_access]{clear_access()}} for the post-overflow recompute.}
}
\value{
A tibble: the \code{\link[=clear_access]{clear_access()}} outputs computed on the post-overflow
effective demand, plus \code{demand_workload_pre_overflow}, \code{served_local}
(each catchment's own demand served at home), \code{spilled_out}, \code{spilled_in},
\code{unmet_before_overflow} (the Phase-1 residual), and \code{overflow_travel_time}
(mean total travel borne by demand spilled out of the catchment; \code{NA} where
none spilled). System-level overflow totals are attached as
\code{attr(x, "overflow")}.
}
\description{
The Phase-2 spatial-overflow step. Each catchment is first cleared locally
(as in \code{\link[=clear_access]{clear_access()}}); a shortage catchment's residual demand is then
offered to neighbouring catchments with spare capacity, cheapest edge first,
until it is placed or no reachable neighbour has room. Demand is conserved:
\code{demand = served_local + spilled_out + unmet} per catchment, and
\code{sum(spilled_out) == sum(spilled_in)} across the system. Post-overflow waits,
appointment probabilities and utilization are recomputed by \code{\link[=clear_access]{clear_access()}}
on each catchment's effective demand (\code{demand - spilled_out + spilled_in}).
}
\details{
The permissive bound on the pair whose other half is \code{\link[=clear_access]{clear_access()}}. Unmet
demand in one catchment is absorbed by neighbours listed in \code{neighbors}
(columns \code{from}, \code{to}, \code{travel_penalty}), which assumes patients will travel
and that the receiving capacity is genuinely available to them.

Neither function is the right answer on its own. Real spill is somewhere
between none and free, and the two runs together say how much of the
conclusion depends on which assumption you make.
}
\examples{
# Unmet demand in one catchment can be absorbed by a neighbour, at a travel
# penalty. Compare with clear_access(), which strands it.
catchments <- tibble::tibble(
  catchment           = c("A", "B", "C"),
  demand_workload     = c(1200, 800, 400),
  accessible_capacity = c(900, 900, 500)
)
neighbors <- tibble::tibble(from = c("A", "A", "B"), to = c("B", "C", "C"),
                            travel_penalty = c(10, 25, 15))
overflow_access(catchments, neighbors)[, c("catchment", "spilled_out", "spilled_in")]
}
\seealso{
Other access clearing: 
\code{\link{catchment_neighbors_from_coords}()},
\code{\link{clear_access}()},
\code{\link{clear_access_trajectory}()},
\code{\link{overflow_conservation}()}
}
\concept{access clearing}
\concept{geography}
