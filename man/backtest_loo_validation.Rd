% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-backtest.R
\name{backtest_loo_validation}
\alias{backtest_loo_validation}
\title{Leave-one-out validation across windows (leaky by construction)}
\usage{
backtest_loo_validation(
  cutoffs = 2013:2020,
  horizon = 3L,
  predictor = "nrmp",
  min_train = 2L,
  conf = 0.95
)
}
\arguments{
\item{cutoffs}{Candidate windows.}

\item{horizon}{Projection length.}

\item{predictor}{Entrant predictor, as in \code{\link[=backtest_multi_window]{backtest_multi_window()}}.}

\item{min_train}{Minimum training windows; origins with fewer are excluded
rather than scored on a spread that is not estimable.}

\item{conf}{Interval level.}
}
\value{
Tibble, one row per window.
}
\description{
Retained ONLY as the comparator that shows what rolling origin corrects.
Training uses every other window regardless of whether its outcome had
occurred by the origin, so the interval for an early window can be informed
by outcomes from years after it. Do not report this as predictive
calibration.
}
\details{
KEPT ONLY AS THE COMPARATOR that shows what rolling origin corrects. Never
report it as predictive calibration.

The leak is concrete: leave-one-out trains on every other window regardless
of when its outcome occurred, so the interval for an early origin can be
informed by outcomes from years AFTER it -- information the forecaster could
not have had. \code{n_train_future} counts, per row, how many training windows
were in that position, which makes the size of the advantage visible instead
of leaving it as an argument.

\code{\link[=backtest_rolling_origin]{backtest_rolling_origin()}} admits a training window only when its outcome
was observable at the origin (\code{target_year <= origin}). Comparing the two is
the point: any coverage LOO enjoys over rolling origin is borrowed from the
future.
}
\seealso{
Other backtest:
\code{\link[=.series_through]{.series_through()}},
\code{\link{BACKTEST_PARAMETER_PROVENANCE}},
\code{\link{BACKTEST_TARGET_YEAR}},
\code{\link[=assert_backtest_parameters_precede_cutoff]{assert_backtest_parameters_precede_cutoff()}},
\code{\link[=assert_no_leakage]{assert_no_leakage()}},
\code{\link[=backtest_attrition_requirement]{backtest_attrition_requirement()}},
\code{\link[=backtest_cohort_at]{backtest_cohort_at()}},
\code{\link[=backtest_cohorts_through]{backtest_cohorts_through()}},
\code{\link[=backtest_entrant_estimate]{backtest_entrant_estimate()}},
\code{\link[=backtest_multi_window]{backtest_multi_window()}},
\code{\link[=backtest_oos_interval]{backtest_oos_interval()}},
\code{\link[=backtest_retirement_regime]{backtest_retirement_regime()}},
\code{\link[=backtest_rolling_origin]{backtest_rolling_origin()}},
\code{\link[=backtest_target_candidates]{backtest_target_candidates()}},
\code{\link[=reset_leakage_audit]{reset_leakage_audit()}},
\code{\link[=validate_backtest_target]{validate_backtest_target()}}
}
\concept{backtest}
\concept{validation}
