% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-isochrone_access_response.R
\name{lizeth_wait_response_loss}
\alias{lizeth_wait_response_loss}
\title{Closed-form wait-response fit and loss for NPI-joined Lizeth calls}
\usage{
lizeth_wait_response_loss(
  response_table,
  wait_col = "wait_business_days",
  weight_col = NULL,
  wait_ceiling = 365
)
}
\arguments{
\item{response_table}{Matched call-level rows with \code{wait_col},
\code{demand_workload} and \code{accessible_capacity} (from
\code{\link[=join_lizeth_to_catchments]{join_lizeth_to_catchments()}}, filtered to \code{matched}).}

\item{wait_col}{Observed wait column. Default \code{"wait_business_days"}.}

\item{weight_col}{Optional per-call weight column (e.g. an ascertainment
weight). \code{NULL} (default) weights every call equally.}

\item{wait_ceiling}{Retained for interface symmetry with \code{\link[=clear_access]{clear_access()}}; the
loss excludes saturated calls rather than censoring them. Default \code{365}.}
}
\value{
A list: \code{wait_scale} (fitted constant), \code{sse}, \code{rmse}, \code{n_used}
(unsaturated calls fitted), and \code{n_censored} (saturated calls excluded).
}
\description{
Given calls already joined to their provider-catchment
(\code{\link[=join_lizeth_to_catchments]{join_lizeth_to_catchments()}}), fits the single wait-response constant
\code{wait_scale} by least squares through the origin and returns the residual sum
of squares. This is the per-provider objective that identifies the decay
parameter in \code{\link[=fit_decay_sigma]{fit_decay_sigma()}}: the queue map \code{wait = wait_scale * rho/(1-rho)}
is linear in \code{wait_scale}, so for a fixed catchment structure the best
\code{wait_scale} and its error are closed-form.
}
\details{
For each matched call, \code{rho = demand_workload / accessible_capacity} and
\code{x = rho / (1 - rho)}. Only unsaturated calls (\verb{0 <= rho < 1}, finite wait)
enter the fit; saturated calls (\code{rho >= 1}, infinite modelled wait) are counted
but excluded, mirroring \code{\link[=fit_wait_scale]{fit_wait_scale()}}. The weighted least-squares estimate
through the origin is \code{wait_scale = sum(w * W * x) / sum(w * x^2)}, and the loss
is \code{sum(w * (W - wait_scale * x)^2)}. Raw (not log) waits keep the estimate
closed-form; the reference-adequacy inverse fit, which matches log-wait
quantiles, is a separate step.
}
\seealso{
Other isochrone access response:
\code{\link[=capacity_status_with_isochrone_response]{capacity_status_with_isochrone_response()}},
\code{\link[=e2sfca_catchments_from_access]{e2sfca_catchments_from_access()}},
\code{\link[=fit_decay_sigma]{fit_decay_sigma()}},
\code{\link[=join_lizeth_to_catchments]{join_lizeth_to_catchments()}},
\code{\link[=wait_response_region_holdout]{wait_response_region_holdout()}}
}
\concept{calibration}
\concept{isochrone access response}
