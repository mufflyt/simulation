% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-medicare_realized_care.R,
%   R/supply-medicare_capacity.R
\name{aggregate_medicare_realized_care}
\alias{aggregate_medicare_realized_care}
\title{Aggregate Medicare claims into billed urogynecologic services}
\usage{
aggregate_medicare_realized_care(
  claims,
  crosswalk = urps_medicare_service_crosswalk(),
  year = "year",
  hcpcs = "HCPCS_Cd",
  services = "Tot_Srvcs",
  state = "Rndrng_Prvdr_State_Abrvtn",
  provider_type = "Rndrng_Prvdr_Type",
  place_of_service = "Place_Of_Srvc",
  npi = "Rndrng_NPI"
)

aggregate_medicare_realized_care(
  claims,
  crosswalk = urps_medicare_service_crosswalk(),
  year = "year",
  hcpcs = "HCPCS_Cd",
  services = "Tot_Srvcs",
  state = "Rndrng_Prvdr_State_Abrvtn",
  provider_type = "Rndrng_Prvdr_Type",
  place_of_service = "Place_Of_Srvc",
  npi = "Rndrng_NPI"
)
}
\arguments{
\item{claims}{Claim lines, such as the CMS Provider-and-Service PUF.}

\item{crosswalk}{HCPCS-to-service crosswalk; defaults to the procedure-only
URPS basket. Do not add generic E/M codes unless claims are linked to a
validated URPS roster or diagnosis definition.}

\item{year, hcpcs, services}{Column names in \code{claims}.}

\item{state, provider_type, place_of_service, npi}{Optional claims columns to
retain as dimensions when present. Set any to NULL to omit it.}

\item{include_non_specific}{Passed to the crosswalk.}
}
\value{
Tibble of \code{year}, \code{service}, \code{state}, \code{provider_type},
\code{place_of_service}, \code{billed_services}, \code{billing_npis}, \code{estimand}, with an
\code{unmapped_service_fraction} attribute.

Tibble of annual service totals with explicit \code{estimand} and
\code{payer_scope} labels. The \code{unmapped_service_fraction} attribute records
claims excluded because their HCPCS code is outside the study basket.
}
\description{
Maps HCPCS codes to services through \code{\link[=urps_medicare_service_crosswalk]{urps_medicare_service_crosswalk()}} and
sums billed service counts. Codes outside the crosswalk are DROPPED, and the
share of services they represent is returned as the
\code{unmapped_service_fraction} attribute rather than discarded silently -- a high
fraction means the crosswalk is missing the procedures this population
actually receives, which is a finding, not a rounding error.

This is deliberately an \emph{observed-use} estimand. It measures billed Medicare
fee-for-service services after access, referral, payer mix, and capacity have
already shaped care. It must not be substituted for latent all-payer demand.
}
