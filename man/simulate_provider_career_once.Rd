% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12-provider_microsimulation.R
\name{simulate_provider_career_once}
\alias{simulate_provider_career_once}
\title{Simulate one stochastic provider-career trajectory}
\usage{
simulate_provider_career_once(
  agents,
  years,
  entrants_per_year,
  hazard_table = NULL,
  conversion_floor = 1,
  subspecialty = "FPMRS",
  retirement_schedule = RETIREMENT_HAZARD_BY_AGE,
  career_change_hazard = CAREER_CHANGE_HAZARD_UNDER_50,
  fte_method = "hours",
  hours_model = NULL,
  hours_multiplier = 1,
  hours_intercept = HWSM_HOURS_INTERCEPT,
  late_career_fte_factor = 1,
  late_career_fte_onset_age = NA_real_,
  entrant_female_share = 0.82,
  placement_shares = NULL
)
}
\arguments{
\item{agents}{Starting agent tibble (\code{\link[=initialize_provider_agents]{initialize_provider_agents()}}).}

\item{years}{Integer vector of calendar years to simulate (ascending).}

\item{entrants_per_year}{Numeric annual entrant count (may be fractional; the
fractional part is realised stochastically).}

\item{hazard_table}{Age-band hazard table (\code{\link[=build_hazard_table]{build_hazard_table()}}).}

\item{conversion_floor}{Fraction of entrants that actually enter practice
(cliff WORKFORCE_CONVERSION_FLOOR = 0.70). Applied as a haircut on entrants.}

\item{subspecialty}{Subspecialty label for injected entrants.}

\item{retirement_schedule}{Single-year-of-age retirement hazard schedule.
Scenarios pass a SHIFTED schedule (retire n years earlier/later).}

\item{career_change_hazard}{Annual departure hazard below the retirement age.
Set to 0, with a zero retirement schedule, for a no-attrition run.}

\item{fte_method}{FTE method: "hours", "participation", or "legacy_weight".}

\item{hours_model}{Optional fitted hours model from
\code{\link[=fit_clinical_hours_model]{fit_clinical_hours_model()}}; when NULL the reference schedule is used.}

\item{hours_multiplier}{Scenario knob scaling hours worked.}

\item{hours_intercept}{Intercept for the reference hours schedule; use
\code{\link[=calibrate_hours_intercept]{calibrate_hours_intercept()}} so hours and the FTE threshold agree.}

\item{late_career_fte_factor}{Multiplier on clinical FTE applied only from
\code{late_career_fte_onset_age} (a mufflyaccess scenario field).}

\item{late_career_fte_onset_age}{Age from which the late-career factor
applies; NA disables it.}

\item{entrant_female_share}{Share of new entrants drawn as female.}

\item{placement_shares}{Optional tibble of \code{geo} and \code{share} enabling entrant
placement and mid-career migration.}
}
\value{
List with \code{panel} (per-year summary tibble) and \code{agents} (final agent
table incl. drawn retirement years) so the temporal cohort is reconstructible.
}
\description{
Each year: draw Bernoulli retirement per active agent using the age-band
hazard, age everyone by one year, then inject \code{entrants_per_year} new agents
at the entry age. Records active headcount, effective FTE, and mean age.
}
