% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/25-demand_lifecourse.R
\name{simulate_lifecourse_demand}
\alias{simulate_lifecourse_demand}
\title{Simulate one year of life-course pelvic-floor demand as service volumes}
\usage{
simulate_lifecourse_demand(
  pop_by_age,
  year,
  scenario = "baseline",
  n = 100000,
  seed = NULL,
  cesarean_rate = NULL,
  access_gain = 1,
  barrier_prevalence = 0.35,
  prevention_target = c("pop", "ui", "ai", "bmi"),
  prevention_effect = 0.2,
  risk_params = lifecourse_risk_params(),
  pathway_params = lifecourse_pathway_params(),
  service_map = lifecourse_service_map()
)
}
\arguments{
\item{pop_by_age}{Tibble with \code{age} and \code{population} (female population for the
target year).}

\item{year}{Calendar year (stamped on the output).}

\item{scenario}{One of "baseline", "delivery_mode", "reduced_barriers",
"prevention".}

\item{n}{Number of synthetic persons.}

\item{seed}{Optional RNG seed.}

\item{cesarean_rate}{Share of live births delivered by cesarean (delivery-mode
lever). Default \code{NULL} uses the CITED cohort-varying cesarean baseline (via
R/13b \code{cohort_vaginal_exposure()}); pass a scalar to override it.}

\item{access_gain}{Multiplier on care-seeking for high-barrier women
(reduced-barriers lever). Default 1; the "reduced_barriers" scenario sets 1.6
when left at 1.}

\item{barrier_prevalence}{Fraction of synthetic persons assigned \code{high_barrier = 1}.
Default 0.35; derive from actual BRFSS data with
\code{brfss_barrier_prevalence(cells)} (R/44-urps_population.R) to anchor to
observed uninsured/low-income rates rather than a fixed assumption.}

\item{prevention_target}{One of "pop", "ui", "ai", "bmi" (prevention lever).}

\item{prevention_effect}{Fractional reduction applied by the prevention lever.}

\item{risk_params, pathway_params, service_map}{Parameter tables; defaults are
placeholders — override to calibrate.}
}
\value{
A list with \code{person_years}, \code{service_volumes} (tibble \code{year},
\code{service}, \code{volume}), \code{care_seeking_national}, \code{treated_national}, and \code{meta}.
}
\description{
Runs the reproductive life-course pipeline (childbirth exposure -> pelvic-floor
conditions -> care pathway -> service use) for a single calendar year and
scenario, and returns national, population-scaled annual SERVICE VOLUMES ready
for \code{\link[=convert_workload_to_fte]{convert_workload_to_fte()}} / \code{\link[=apportion_service_volume]{apportion_service_volume()}} in
R/17-workload_to_fte.R. Demand is not converted to FTE here and provider
substitution is handled by that module's delegation matrix.
}
\details{
Scenarios: \code{"baseline"}; \code{"delivery_mode"} (shifts the primary exposure via
\code{cesarean_rate}); \code{"reduced_barriers"} (raises care-seeking for high-barrier
women via \code{access_gain}); \code{"prevention"} (cuts one transition — BMI reduction
or a condition-specific risk reduction — the ONLY place BMI interventions
live). Provider-mix / treatment-substitution scenarios belong to
R/17-workload_to_fte.R, not here.
}
