% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/49-entrant_regime.R
\name{run_entrant_regime_backtest}
\alias{run_entrant_regime_backtest}
\title{Score the regime estimator against the same cutoff and target as the back-test}
\usage{
run_entrant_regime_backtest(
  cutoff_year = BACKTEST_CUTOFF_YEAR,
  target_year = BACKTEST_TARGET_YEAR,
  n_iterations = 1000L,
  seed = 20260802L,
  trend_family = "loglinear",
  verbose = TRUE
)
}
\arguments{
\item{cutoff_year, target_year}{Cutoff and scoring year.}

\item{n_iterations}{Monte Carlo replicates per arm.}

\item{seed}{RNG seed.}

\item{trend_family}{Passed to \code{\link[=fit_entrant_regime_model]{fit_entrant_regime_model()}}.}

\item{verbose}{Emit progress via \code{\link[base:message]{base::message()}}.}
}
\value{
List with \code{summary}, \code{model}, and \code{entrant_path}.
}
\description{
Deliberately separate from \code{\link[=run_backtest]{run_backtest()}}. That function runs the four
PRESPECIFIED arms whose result is frozen in \code{BACKTEST_RECORD_2020_2023}, and
folding a later estimator into it would rewrite a record whose whole value is
that nothing was re-tuned after the error was seen.
}
\details{
The result of this function is NOT an out-of-sample test at the 2020 cutoff:
the estimator was built after that miss was examined. It is a refit, reported
as one. \code{\link[=entrant_regime_rolling_validation]{entrant_regime_rolling_validation()}} carries the out-of-sample claim.
}
\keyword{internal}
