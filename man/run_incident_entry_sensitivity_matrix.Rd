% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand-incident_entry.R
\name{run_incident_entry_sensitivity_matrix}
\alias{run_incident_entry_sensitivity_matrix}
\title{Run the Pre-Registered Incident-Entry Sensitivity Matrix}
\usage{
run_incident_entry_sensitivity_matrix(
  claims_variants,
  enrollment_tbl,
  member_year_tbl,
  roster_variants,
  stock_probability_tbl,
  washout_variants = c(12L, 24L, 36L),
  gap_variants = c(0L, 1L),
  year_variants = list(primary = c(2023L, 2024L), sensitivity_2022_24 = c(2022L, 2023L,
    2024L)),
  ...
)
}
\arguments{
\item{claims_variants}{Named list of \code{claims_tbl} variants, one per
index-event / diagnosis-window / provider-definition / case-rule
combination to test. Must include an element named \code{"primary"}.}

\item{enrollment_tbl, member_year_tbl, stock_probability_tbl}{Passed through
unchanged to every run -- these are not part of the pre-registered
sensitivity matrix.}

\item{roster_variants}{Named list of \code{roster_tbl} variants, parallel to
\code{claims_variants} by name. A single unnamed/length-1 list is recycled
across every claims variant (the common case: only the claims
classification changes, the roster does not).}

\item{washout_variants}{Washout months to sweep one at a time against the
primary spec. Must include 24 (the primary value); default matches the
pre-registered \code{c(12, 24, 36)}.}

\item{gap_variants}{Allowed enrollment-gap months to sweep one at a time.
Must include 0 (the primary value); default matches the pre-registered
\code{c(0, 1)}.}

\item{year_variants}{Named list of \code{analysis_years} vectors to sweep one at
a time. Must include an element named \code{"primary"}; default matches the
pre-registered primary (2023-2024) against the 2022-24 sensitivity.}

\item{...}{Passed to every call of \code{\link[=estimate_incident_entry_hazard]{estimate_incident_entry_hazard()}}
(e.g. \code{min_cell_n}, \code{conf_level}).}
}
\value{
A tibble: \code{estimate_incident_entry_hazard()$public} row-bound
across every run, with two added columns identifying which single axis
moved from the primary spec for that row -- \code{sensitivity_dimension}
(\code{"primary"}, \code{"washout_months"}, \code{"allowed_gap_months"},
\code{"analysis_years"}, or \code{"claims_variant"}) and \code{sensitivity_value} (the
value or variant name that produced it).
}
\description{
Runs \code{\link[=estimate_incident_entry_hazard]{estimate_incident_entry_hazard()}} once per dimension of the
pre-registered sensitivity matrix (docs/INCIDENT_ENTRY_ESTIMAND.md #8),
holding every other dimension at its primary value. Existence of a swing
across reasonable definitions is itself a finding -- see the estimand's own
framing: "If \code{q} swings from 0.08 to 0.40 across reasonable case
definitions, that uncertainty belongs in the simulation." This function
produces the comparison table that swing is read from; it does not
interpret it.
}
\seealso{
Other incident entry:
\code{\link[=estimate_incident_entry_hazard]{estimate_incident_entry_hazard()}},
\code{\link[=incident_entry_wilson]{incident_entry_wilson()}},
\code{\link[=save_incident_entry_hazard]{save_incident_entry_hazard()}}
}
\concept{demand}
\concept{incident entry}
