% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supply-practice_payer_mix.R
\name{practice_payer_mix_defaults}
\alias{practice_payer_mix_defaults}
\title{Default practice-economics payer mix}
\usage{
practice_payer_mix_defaults(namcs_mix = NULL, include_crosscheck = TRUE)
}
\arguments{
\item{namcs_mix}{Tibble from \code{\link[=namcs_urps_payer_mix]{namcs_urps_payer_mix()}}; computed when \code{NULL}.}

\item{include_crosscheck}{Whether to attach the 3P-RD and CHIA cross-check
ratios (3P-RD requires the vendored \code{data-raw/ahrq_3prd} file; CHIA
requires the live CHIA DuckDB to be reachable -- either or both may be
silently omitted from the attribute when unavailable).}
}
\value{
One-row tibble with \code{medicare_share}, \code{medicaid_share},
\code{commercial_share}, \code{self_pay_share} (sums to 1), \code{self_pay_reliable}
(\code{FALSE} when the underlying NAMCS Uninsured cell is below the NCHS
reliability floor). Carries a \code{crosschecks} attribute (a named list,
\code{ahrq_3prd}/\code{chia}) when requested and reachable.
}
\description{
The \code{medicare_share}/\code{medicaid_share}/\code{commercial_share}/\code{self_pay_share}
input \code{\link[=simulate_practice_economics]{simulate_practice_economics()}} needs, built entirely from
\code{\link[=namcs_urps_payer_mix]{namcs_urps_payer_mix()}} (\code{Private} -> \code{commercial_share}, \code{Uninsured} ->
\code{self_pay_share}). \code{\link[=ahrq_3prd_medicare_medicaid_ratio]{ahrq_3prd_medicare_medicaid_ratio()}} and
\code{\link[=chia_medicare_medicaid_ratio]{chia_medicare_medicaid_ratio()}} are attached as a \code{crosschecks} attribute
for comparison only -- see those functions' docstrings for why neither is
blended in.
}
\concept{supply}
