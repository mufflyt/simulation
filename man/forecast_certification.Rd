% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/41-interval_coverage.R
\name{forecast_certification}
\alias{forecast_certification}
\title{Forecast certification counts with an overdispersed predictive interval}
\usage{
forecast_certification(
  baseline_fit,
  forecast_years,
  interval_inflation = 1,
  nominal_coverage = 0.95,
  verbose = TRUE
)
}
\arguments{
\item{baseline_fit}{A list returned by \code{\link[=fit_certification_baseline]{fit_certification_baseline()}}.}

\item{forecast_years}{Integer vector of years to forecast. Must all be
strictly later than \code{baseline_fit$fit_through_year}.}

\item{interval_inflation}{Numeric scalar >= 1 multiplying the half-width.}

\item{nominal_coverage}{Numeric scalar in \verb{(0, 1)}. Defaults to 0.95.}

\item{verbose}{Logical. Defaults to \code{TRUE}.}
}
\value{
A tibble with columns \code{year}, \code{expected_count}, \code{lower_bound},
and \code{upper_bound}.
}
\description{
Produces expected counts and a predictive interval for held-out years. The
interval is negative-binomial when the fitted dispersion exceeds one and
Poisson otherwise, then widened by \code{interval_inflation}. Pass an inflation
factor from \code{\link[=solve_interval_inflation]{solve_interval_inflation()}} rather than a hand-picked one.
}
