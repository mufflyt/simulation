% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-baseline_gap.R
\name{baseline_gap}
\alias{baseline_gap}
\title{Build a labelled base-year gap object}
\usage{
baseline_gap(
  base_supply_fte,
  adequacy,
  method = c("capacity_survey", "hpsa_removal", "assumed", "external_anchor"),
  evidence = character(0),
  access_components_counted = character(0),
  calibration_status = NULL,
  source = NA_character_,
  population = NA_character_,
  year = NA_integer_,
  source_specialty = NA_character_,
  externally_measured = NA,
  adequacy_ci = NULL
)
}
\arguments{
\item{base_supply_fte}{Base-year supplied FTE.}

\item{adequacy}{Base-year adequacy ratio.}

\item{method}{One of \code{BASELINE_GAP_METHODS}. Names the arithmetic only.}

\item{evidence}{Character vector of supporting indicators.}

\item{access_components_counted}{Character vector naming access deficits
already embedded in this gap (e.g. "nonmetro", "uninsured", "racial_equity").}

\item{calibration_status}{One of \code{BASELINE_GAP_TIERS}: whether this number was
measured on urogynaecologists, borrowed from another specialty, or assumed.
Inferred only for \code{method = "assumed"} (from whether an evidence ledger is
present); for every other method it must be declared, because the method
cannot distinguish a fielded URPS survey from a borrowed distribution.}

\item{source}{Attribution for the number (the study, survey or anchor).}

\item{population}{Population the estimate refers to (e.g. "adult women").
\code{NA} is honest and reported as "undeclared"; it is never silently filled in.}

\item{year}{Base year the estimate refers to.}

\item{source_specialty}{Specialty the number was actually measured on (URPS
itself, or the analogue specialty a borrowed distribution came from).}

\item{externally_measured}{Logical flag: whether the number was externally
measured (\code{TRUE}) rather than assumed. \code{NA} when undeclared.}

\item{adequacy_ci}{Optional two-element confidence interval for \code{adequacy}.
The required-FTE and shortfall intervals are derived from it, with the
bound swap (the low adequacy bound gives the high required-FTE bound).}
}
\value{
An object of class \code{urps_baseline_gap}.
}
\description{
Central constructor. Carries the method, the adequacy, the FTE anchor, and an
\code{access_components_counted} field used downstream to prevent an access-equity
demand scenario from double-counting a deficit already inside the base-year
gap.
}
