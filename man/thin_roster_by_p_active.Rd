% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_flows.R
\name{thin_roster_by_p_active}
\alias{thin_roster_by_p_active}
\title{Thin a base-year provider roster by labor-force participation probability}
\usage{
thin_roster_by_p_active(
  agents,
  coef = URPS_P_ACTIVE_COEF,
  scenario_id = NULL,
  stochastic = TRUE
)
}
\arguments{
\item{agents}{A data frame / tibble of agents with at least columns \code{age}
and optionally \code{sex} (defaults to \code{"female"}) and \code{years_certified}
(defaults to \code{pmax(age - MICROSIM_AGE_AT_CERT, 0)} when absent).}

\item{coef}{Named list of logistic coefficients; see \link{URPS_P_ACTIVE_COEF}.}

\item{scenario_id}{Passed to \code{\link[=urps_p_active]{urps_p_active()}}.}

\item{stochastic}{Logical. When \code{TRUE} (default) each row is retained by a
Bernoulli draw against P(active). When \code{FALSE} the function returns a
weighted version of \code{agents} with a new column \code{p_active} and does NOT
drop rows — useful for expected-value calculations without randomness.}
}
\value{
When \code{stochastic = TRUE}: a subset of \code{agents} (rows retained).
When \code{stochastic = FALSE}: \code{agents} with an added \code{p_active} column.
}
\description{
Converts a "treat every unconfirmed provider as active" starting roster into
one whose size reflects the estimated fraction of providers who are actually
practising. Each provider is retained with probability
P(active | age, sex, years_certified) and dropped otherwise — a Bernoulli
thinning whose expectation equals the point estimate from \code{\link[=urps_p_active]{urps_p_active()}}.
}
\details{
Use this BEFORE passing the roster to \code{\link[=run_supply_microsimulation]{run_supply_microsimulation()}} via
\code{thin_by_p_active = TRUE}, or call it directly when you want a deterministic
(expected-value) adjustment rather than a stochastic draw.
}
\keyword{internal}
