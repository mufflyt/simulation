% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geography-urps_migration.R
\name{urps_migration_matrix}
\alias{urps_migration_matrix}
\title{Build a state-level migration matrix with rural-urban asymmetry}
\usage{
urps_migration_matrix(
  states,
  urbanicity = CONUS_STATE_URBANICITY,
  destination_weights = NULL,
  rural_to_urban = URPS_RURAL_URBAN_DRIFT$rural_to_urban,
  urban_to_rural = URPS_RURAL_URBAN_DRIFT$urban_to_rural,
  same_state_prior = 0.25,
  out_of_country_rate = 0.003
)
}
\arguments{
\item{states}{Character vector of state abbreviations to include.}

\item{urbanicity}{Named character vector mapping state → \code{"metro"} or
\code{"nonmetro"}. Defaults to \link{CONUS_STATE_URBANICITY}.}

\item{destination_weights}{Named numeric vector of destination weights
(e.g. current workforce size per state). Uniform when \code{NULL}.}

\item{rural_to_urban}{Fraction of nonmetro-origin movers redirected to metro
destinations. Default from \link{URPS_RURAL_URBAN_DRIFT}.}

\item{urban_to_rural}{Fraction of metro-origin movers redirected to nonmetro
destinations. Default from \link{URPS_RURAL_URBAN_DRIFT}.}

\item{same_state_prior}{Prior weight on staying in the same state (0 = no
diagonal boost, 1 = never move). Default 0.25 is the Fraher et al. (2024)
pediatric subspecialty median intra-division stay rate after re-entry.}

\item{out_of_country_rate}{Annual probability that a mover goes out of the
country entirely (appended as an extra destination \code{"out_of_country"}).
Set to 0 to omit.}
}
\value{
Long-format tibble with columns \code{origin}, \code{destination},
\code{probability}. Passes \code{\link[=validate_migration_matrix]{validate_migration_matrix()}}.
}
\description{
Constructs a long-format origin-by-destination migration probability table
by combining:
\itemize{
\item A uniform baseline: each state-to-state pair starts at its national
population share (proportional to \code{destination_weights}).
\item A rural-urban asymmetry correction: moves from nonmetro origin states
are redirected toward metro destinations at rate \code{rural_to_urban};
moves from metro origin states are redirected toward nonmetro at rate
\code{urban_to_rural}.
\item A dominant diagonal: providers who move are more likely to stay in their
own state than the national share alone predicts (calibrated by
\code{same_state_prior}).
\item Row normalisation to ensure each origin sums to 1.
}
}
\details{
The resulting matrix is compatible with \code{\link[=apply_provider_migration_matrix]{apply_provider_migration_matrix()}}
and passes \code{\link[=validate_migration_matrix]{validate_migration_matrix()}}.
}
\examples{
states <- c("NY", "CA", "WV", "MT", "TX")
mat    <- urps_migration_matrix(states)
validate_migration_matrix(mat)
}
\seealso{
\code{\link[=apply_provider_migration_matrix]{apply_provider_migration_matrix()}}, \code{\link[=apply_urps_migration]{apply_urps_migration()}},
\link{CONUS_STATE_URBANICITY}, \link{URPS_RURAL_URBAN_DRIFT}

Other urps migration: 
\code{\link{CONUS_STATE_URBANICITY}},
\code{\link{URPS_MIGRATION_HAZARD}},
\code{\link{URPS_RURAL_URBAN_DRIFT}},
\code{\link{agent_urbanicity_summary}()},
\code{\link{apply_urps_migration}()},
\code{\link{classify_state_urbanicity}()},
\code{\link{migration_drift_scenario}()},
\code{\link{rural_urban_concentration}()}
}
\concept{geography}
\concept{urps migration}
