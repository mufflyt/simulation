% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geography-spatial_access_data.R
\name{build_access_membership}
\alias{build_access_membership}
\title{Build the E2SFCA membership table from isochrone polygons + tract points}
\usage{
build_access_membership(
  iso_sf,
  tracts,
  provider_col = "coord_id",
  band_col = "drive_time",
  crs = 4326
)
}
\arguments{
\item{iso_sf}{Provider isochrones (\code{sf}) with a provider-id column and a
band column (drive-time minutes).}

\item{tracts}{Demand tibble with \code{demand_id}, \code{lon}, \code{lat}
(\code{\link[=load_tract_demand]{load_tract_demand()}}), or an \code{sf} of tract points.}

\item{provider_col}{Name of the provider-id column in \code{iso_sf}.}

\item{band_col}{Name of the band (minutes) column in \code{iso_sf}.}

\item{crs}{Coordinate reference system of the tract lon/lat (default 4326).}
}
\value{
Tibble \code{demand_id}, \code{provider_id}, \code{band} (one row per containment).
}
\description{
Point-in-polygon overlay: each tract centroid is assigned to every provider
isochrone band whose polygon contains it. Produces the \code{membership} table the
base-R engine (\code{\link[=compute_e2sfca_access]{compute_e2sfca_access()}}) consumes. Requires \code{sf}.
}
\seealso{
Other spatial access data: 
\code{\link{load_provider_isochrones}()},
\code{\link{load_tract_demand}()},
\code{\link{real_access_surface}()}
}
\concept{geography}
\concept{spatial access data}
