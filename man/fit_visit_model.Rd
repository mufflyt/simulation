% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/26-utilization_models.R
\name{fit_visit_model}
\alias{fit_visit_model}
\title{Fit an outpatient visit model, respecting the survey design when possible}
\usage{
fit_visit_model(
  data,
  formula = UI_visits ~ Age + I(Age^2) + Obesity + VaginalParity + ChronicCough +
    HeavyJob + Race + Hysterectomy,
  weights_col = NULL,
  strata_col = NULL,
  psu_col = NULL,
  prefer = c("survey", "negbin", "quasipoisson")
)
}
\arguments{
\item{data}{Person-level MEPS-style data (already filtered to the population).}

\item{formula}{Visit-count model formula.}

\item{weights_col}{Person weight column (e.g. \code{PERWT21F}).}

\item{strata_col, psu_col}{Survey strata / PSU columns (e.g. \code{VARSTR},\code{VARPSU}).}

\item{prefer}{Method preference; see Details.}
}
\value{
The fitted model; \code{attr(., "hdmm_method")} records the path used.
}
\description{
Preference order: (1) \code{survey::svyglm} quasipoisson on an svydesign built from
the weight/strata/PSU columns (correct design-based SEs), used when the survey
package is installed and \code{weights_col} is supplied; (2) \code{MASS::glm.nb} when
\code{prefer = "negbin"} and MASS is installed; (3) a weighted \code{stats::glm}
quasipoisson (always available) with a warning that the SEs are not
design-based.
}
