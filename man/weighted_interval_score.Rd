% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-forecast_scorecard.R
\name{weighted_interval_score}
\alias{weighted_interval_score}
\title{Weighted interval score (WIS) for quantile forecasts}
\usage{
weighted_interval_score(y, quantiles, quantile_levels)
}
\arguments{
\item{y}{Numeric observation(s), length \code{n} (recycled if length 1).}

\item{quantiles}{Numeric matrix \verb{n x m} of quantile forecasts (rows = cases,
columns = \code{quantile_levels}); a length-\code{m} vector is treated as one case.}

\item{quantile_levels}{Numeric length-\code{m} vector of probability levels in
(0, 1); must include \code{0.5} and symmetric pairs (\code{a} and \code{1 - a}).}
}
\value{
Numeric vector of length \code{n}: the WIS per case (lower is better).
}
\description{
The proper multi-level generalization of the interval score (Bracher et al.
2021): a weighted average of the median's absolute error and the interval
scores of the central intervals implied by symmetric quantile pairs. Rewards
both calibration and sharpness, so a wide, uninformative forecast cannot win.
}
\details{
A proper scoring rule over a QUANTILE forecast (\code{quantiles} at
\code{quantile_levels}), not a single interval. It rewards being calibrated and
sharp at once, so a forecast cannot score well merely by being wide enough to
always contain the observation -- which is exactly how coverage alone can be
bought, and why coverage and WIS are reported together.

Lower is better, on the scale of the quantity forecast. For an observation of
100, a tight covering forecast scores 0.24, a very wide one that still covers
scores 5, and one that misses entirely scores 25.2.
}
\examples{
# WIS for one forecast: observation 1300 scored against a 50\% central interval
# (lower is better). quantile_levels must include the median.
weighted_interval_score(
  y = 1300,
  quantiles = c(1200, 1300, 1400),
  quantile_levels = c(0.25, 0.5, 0.75)
)
}
\seealso{
Other forecast scorecard:
\code{\link[=compare_forecasts]{compare_forecasts()}},
\code{\link[=forecast_scorecard]{forecast_scorecard()}}
}
\concept{forecast scorecard}
\concept{validation}
