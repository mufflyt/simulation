% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration-isochrone_access_response.R
\name{e2sfca_catchments_from_access}
\alias{e2sfca_catchments_from_access}
\title{Build provider-catchment loads from an E2SFCA access computation}
\usage{
e2sfca_catchments_from_access(
  access,
  workload_per_capita = 1,
  drop_zero_demand = TRUE,
  capacity_anchor = "medicare_procedure_volume_fem65"
)
}
\arguments{
\item{access}{A \code{\link[=compute_e2sfca_access]{compute_e2sfca_access()}} result, or its \code{provider_ratios}
tibble (columns \code{provider_id}, \code{weighted_demand}, \code{supply}).}

\item{workload_per_capita}{Positive scalar converting competed-for population
to workload units. Default \code{1} (population-as-workload; the scale is absorbed
by \code{wait_scale}).}

\item{drop_zero_demand}{Drop providers with \code{weighted_demand <= 0}. Default
\code{TRUE}.}

\item{capacity_anchor}{Label recorded on the result describing the \code{supply}
currency. Default \code{"medicare_procedure_volume_fem65"}.}
}
\value{
A tibble with one row per provider: \code{catchment} (the provider id),
\code{demand_workload}, \code{accessible_capacity}, \code{weight} (competed-for demand),
\code{adequacy_relative} (= capacity / workload), and \code{e2sfca_ratio} (the raw
\code{supply / weighted_demand}). The \code{capacity_anchor} and \code{workload_per_capita}
are attached as attributes.
}
\description{
Converts the per-provider ratios from \code{\link[=compute_e2sfca_access]{compute_e2sfca_access()}} into the
\code{catchments} table that \code{\link[=fit_wait_scale]{fit_wait_scale()}}, \code{\link[=forward_lizeth_adequacy]{forward_lizeth_adequacy()}} and
\code{\link[=fit_lizeth_inverse_adequacy]{fit_lizeth_inverse_adequacy()}} consume: one row per provider carrying
\code{demand_workload} and \code{accessible_capacity} in \code{\link[=clear_access]{clear_access()}} currency, plus
a demand \code{weight} for quantile matching and \code{adequacy_relative}, the
isochrone-derived adequacy dispersion (rank-preserving input to the fit).
}
\details{
For provider \eqn{j}, \code{accessible_capacity} is the provider's \code{supply}
(effective clinical capacity, e.g. realized Medicare procedure volume for the
fem65 population) and \code{demand_workload} is \code{weighted_demand * workload_per_capita},
the decay-weighted population that competes for the provider scaled to workload.
Utilisation is then \code{rho = demand_workload / accessible_capacity} and adequacy
is \code{accessible_capacity / demand_workload}. \code{workload_per_capita} is a pure
scale absorbed by the fitted \code{wait_scale}; it does not add a free parameter.

Providers with non-positive weighted demand carry an undefined ratio and are
dropped by default (\code{drop_zero_demand = TRUE}): a zero-demand provider cannot
inform a congestion fit.
}
\seealso{
Other isochrone access response:
\code{\link[=capacity_status_with_isochrone_response]{capacity_status_with_isochrone_response()}},
\code{\link[=fit_decay_sigma]{fit_decay_sigma()}},
\code{\link[=join_lizeth_to_catchments]{join_lizeth_to_catchments()}},
\code{\link[=lizeth_wait_response_loss]{lizeth_wait_response_loss()}},
\code{\link[=wait_response_region_holdout]{wait_response_region_holdout()}}
}
\concept{calibration}
\concept{isochrone access response}
